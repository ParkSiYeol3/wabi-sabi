# WABI-SABI 개발 진행기록 (DEVLOG)

> wasa.kr 와비사비 온라인 스토어 — 전체 흐름·기술스택·진행상황 종합 기록.
> 최종 업데이트: 2026-06-30. 관련 문서: [프로젝트 브리핑](WABI-SABI_프로젝트_브리핑.md) · [디자인 시스템](DESIGN_SYSTEM.md)

---

## 1. 프로젝트 한눈에

| 항목 | 내용 |
|------|------|
| 프로젝트 | WABI-SABI 온라인 스토어 (wasa.kr) |
| 성격 | 와비사비 리빙/식기/소품 이커머스 |
| 개발 | 1인 풀스택 (김은정 운영, Artist House) |
| 디자인 | 흑백 미니멀 + 웜그레이 |
| 카테고리 | Plate · Bowl · Cup · Cutlery · Life · Gift · Craft (7종) + 이 달의 상품(monthly 플래그) |
| 코드 위치 | `web/` 서브폴더 |
| 일정 | 14주 (현재 기반 셋업 완료 — 5~6주차 구간) |

---

## 2. 기술 스택

| 영역 | 채택 | 비고 |
|------|------|------|
| 프레임워크 | **Next.js 16.2.9** (App Router) | Turbopack 기본, `middleware`→`proxy` 개명, `cookies()` async 강제 |
| 런타임 | React 19.2.4 | |
| 언어 | TypeScript 5 | `src/` 디렉터리 구조 |
| 스타일 | Tailwind CSS v4 | `@theme` 디자인토큰 |
| UI 컴포넌트 | **shadcn/ui** (new-york, base color stone) | radix 기반, button/input/card 설치됨. init 대화형이 폰트·토큰 덮어 수동 구성함 |
| 백엔드/DB | Supabase (Auth + PostgreSQL) | `@supabase/ssr` SSR 패턴 |
| 상태관리 | Zustand | 인증 전역상태 |
| 이메일 | Resend | ⏳ 주문메일·뉴스레터 단계에서 설치 예정 |
| Rate limit | Upstash(Redis) | ⏳ 결제·보안 단계에서 설치 예정 |
| 폰트 | Noto Sans KR(Pretendard 대체) + Noto Serif JP(わび-さび 액센트) | next/font |
| 배포 | Vercel (예정) | |
| 버전관리 | Git (web/ 에 init됨) | |
| 디자인 | Figma (FigJam 보드) | REST API 연동 |

---

## 3. 전체 개발 흐름 (지금까지)

### 0단계 — 사전 기획 (웹 Claude, 이전)
- 요구사항(WSB-001~026), IA, ERD, 14주 일정 정의 → `WABI-SABI_프로젝트_브리핑.md`로 핸드오프.

### 1단계 — Figma 연동 (Claude Code, 2026-06-18)
- 방식: **Figma REST API + Personal Access Token** (Dev Mode MCP 아님).
- 대상: FigJam 보드 `te2FQ9BTmVogmqnzUihEUH` ("와비사비 웹 사이트").
- 보드가 이미지 무드보드라 디자인토큰 자동추출 불가 → 시안 7장 직접 분석해 수동 도출.
- 산출: `DESIGN_SYSTEM.md` (컬러토큰·타이포·레이아웃·컴포넌트·실콘텐츠).
- 시안에서 페이지 구조 확인: Home / Shop / About / Visit / Newsletter / Footer.

### 2단계 — 프로젝트 기반 셋업 (2026-06-18, 완료)
- `create-next-app`으로 `web/` 스캐폴딩 (TS·Tailwind·App Router·src).
- 의존성 추가: `@supabase/supabase-js`, `@supabase/ssr`, `zustand`.
- Next 16 브레이킹 체인지 대응 (번들문서 `node_modules/next/dist/docs/` 확인, AGENTS.md 지시 준수):
  - `middleware.ts` → `src/proxy.ts` (함수 `proxy`, nodejs 런타임).
  - `cookies()` async — 서버 클라이언트에서 `await`.
- Supabase 클라이언트 3종 + Zustand 인증 + 디자인토큰 + 폰트 + 메타데이터 구성.
- DB 마이그레이션·RLS·시드 SQL 작성.
- **검증: `tsc --noEmit` 통과, `next build` 성공.**

### 3단계 — 퍼블리싱 전 패키지 설치 (2026-06-18)
- shadcn/ui 수동 구성 (CLI init이 Nova 프리셋으로 폰트·디자인토큰 덮어쓰기 때문). 런타임 deps(class-variance-authority, clsx, tailwind-merge, lucide-react, tw-animate-css) 설치, `components.json`+`lib/utils.ts(cn)` 작성, globals.css에 stone 테마 + 브랜드 토큰 병합.
- 코어 컴포넌트 설치: button, input, card.
- 검증: tsc·build 통과. **UI 퍼블리싱 준비 완료.**
- Resend/Upstash는 퍼블리싱에 불필요 → 주문/결제 단계로 미룸.

### 4단계 — UI 퍼블리싱 (2026-06-18)
- nav 결정: **Shop / About / Contact** (시안 decision_6/5/3 기준). 휴무일 = **수요일**(고해상 Visit Us 시안 채택).
- 브랜드/내비 상수 `lib/site.ts` 단일 출처화.
- 공통 레이아웃: `site-header`(sticky, 모바일 토글, 장바구니·계정 아이콘, 접근성 aria) + `site-footer`(검정 바) → layout.tsx 배선.
- 재사용: `container`(max-w-300) + `product-card`(이미지/카테고리/이름/₩, next/image fill).
- 페이지 6종 시안대로 구현: `/`(Hero·Featured·Philosophy·Values·Visit·Newsletter), `/shop`(카테고리 필터+그리드), `/about`(Philosophy 전문+Values), `/contact`(Visit Us), `/cart`(빈 상태), `/auth`(로그인·회원가입 탭 카드).
- 상품·인증은 플레이스홀더/UI 골격 (Supabase 연동 전, 각 파일 TODO 주석).
- 트러블슈팅: lucide-react가 브랜드 아이콘 `Instagram` 제거 → `AtSign`으로 교체.
- 검증: tsc 통과, build 성공(전 라우트 정적 프리렌더). 상시 로그 `web/README.md` 신설.

### 11단계 — 결제 운영 보강 (2026-07-05, 완료)
- **결제 확정 보안 (PR #52)**: 취약점 — confirm_order 를 authenticated 가 직접 호출해 결제 없이 paid 전환 가능 + 승인 금액이 쿼리스트링.
  - migration `0009`: 구 confirm_order **drop**(revoke만으론 PUBLIC EXECUTE 기본권한으로 우회 — 실테스트로 발견). `confirm_order_paid`(금액검증+멱등, service_role 전용) 신설.
  - `lib/payments.confirmPayment`(공용): 승인 금액=DB total_price(클라 불신), ALREADY_PROCESSED 는 토스 조회 API 재검증.
  - `POST /api/webhooks/toss`: 브라우저 이탈에도 확정. 페이로드 불신(토스 API 재검증) → 위조 무해. **토스 개발자센터 웹훅 등록 필요**.
  - **보안 실테스트**: 사용자 JWT confirm_order → 404(함수 소멸), confirm_order_paid → 403, service_role → 실행. 우회 전면 차단.
- **pending 정리+남용 가드 (PR #53)**: cron `/api/cron/cleanup-pending`(24h pending→cancelled, CRON_SECRET 보호, 매일 03:00 KST). createPendingOrder DB 가드 — pending≥5 or 1시간 10건 이상 차단(Upstash 불요). **Vercel env `CRON_SECRET` 추가 필요**.

### 11.5단계 — 확정값·보안 헤더 (2026-07-05)
- **매장 정보 확정 (PR #54)**: 대표님 확인 — 수요일 휴무, 12:00–19:00, 충남 천안시 동남구 대흥로 338 1층 2호 (31122). DESIGN_SYSTEM §7 충돌 해소.
- **보안 헤더 (PR #55)**: HSTS(2y)·X-Frame-Options DENY·nosniff·Referrer-Policy·Permissions-Policy(payment=self). 프로드 실서빙 검증. CSP는 토스·Supabase 허용목록 검증 후 별도.

### 12단계 — 재고 동시성 (2026-07-05)
- **원자적 재고 검증·차감 (#56)**: 취약점 — 0009 confirm_order_paid 가 `greatest(0, stock-qty)` 클램프 차감. 재고 1개에 두 주문 동시 결제 → 둘 다 paid(초과판매), 재고 0으로 눌려 감지 불가. createPendingOrder 재고 체크는 TOCTOU(UX용, 강제력 없음).
  - migration `0010`: 주문 행 FOR UPDATE(새로고침·웹훅 동시 확정 직렬화) → 상품 행 id 순 FOR UPDATE(교착 방지) → 잠금 하 재고 검증. 부족 시 주문 cancelled + `out_of_stock` 반환, 충분하면 정확 차감(클램프 제거, check stock≥0 이 최후 방어선).
  - `lib/payments`: `out_of_stock` 수신 시 토스 결제 자동 취소(전액 환불) — 취소 실패 시 수동 환불 필요 로그. `ConfirmResult.final` 도입: 재시도 무의미한 확정 실패(재고 소진·상태 불일치)는 웹훅이 200 수신 확인 → 토스 무한 재시도 차단.
  - **적용 완료**: SQL Editor 실행 후 검증 — service_role RPC → `not_found`(동작), anon → `42501 denied`(잠금 유지). #56 close.
- **주문 취소·환불 (#57)**: 배송 전(paid) 주문 전액 취소 — P0 잔여(청약철회 대응).
  - migration `0011`: `cancel_paid_order` RPC(service_role 전용) — 주문 행 FOR UPDATE 로 배송 처리와 직렬화, paid→cancelled + 재고 복원(상품 행 id 순 잠금, 0010 과 동일). 멱등(already_cancelled 는 환불 재시도 허용).
  - `lib/payments.cancelPaidOrder`: **RPC 먼저 → 토스 환불 나중** — 환불 먼저면 배송 경합 시 "배송됐는데 환불" 가능. 토스 결제 조회(orderId→paymentKey) 후 취소, ALREADY_CANCELED 멱등. 환불 실패 시 수동 환불 로그+안내.
  - 서버 액션 `cancelMyOrder`(본인+paid 검증) + 주문 내역 취소 버튼(confirm 후 실행, paid 만 노출). shipping 이후 취소 불가 — 문의 게시판 유도. 부분취소는 후속.
  - **적용 완료**: SQL Editor 실행 후 검증 — service_role → `not_found`, anon → `42501 denied`. #57 close.
- **CSP 1단계 Report-Only (#58, PR #59 머지)**: 허용목록 — 토스 `*.tosspayments.com`(script/frame/connect/img)·Supabase(REST/Storage/wss). Daum 우편번호·외부폰트 미사용 확인. script-src `'unsafe-inline'`은 Next 인라인 필요(nonce 후속). 리뷰 반영: SUPABASE_HOST env 파생, report-to/report-uri → `POST /api/csp-report`(Vercel 로그). 프로드 위반 0 확인 후 강제 전환(2단계, #58 open 유지).
- **Zod 입력 검증 3부작 (#60·#73·#75, PR #61·#74·#76 머지)**:
  - 1차(#60): createPendingOrder(라인 uuid·수량 1~99·최대 30종·**중복 상품 id 거부** — 분할 라인으로 재고 체크 우회 차단)·배송지·선물, cancelMyOrder uuid.
  - 2차(#73): 어드민 액션 전체. **음수 stock 차단**(재고 검증 0010 무력화 방지). `numField` 헬퍼 — `Number(x||0)` 빈값 0 폴백이 재고 초기화 사고 유발 → undefined 반환해 검증 실패.
  - 3차(#75): 문의(200·5000)·리뷰(rating 정수 1~5·본문 2000)·마이페이지 주소·이름. 공용 `lib/validation.ts`(uuidSchema·parseUuid·numField).
- **Supabase CLI 연동**: `supabase link`(ref zeqtfrwjnlckyinjxjcu) 완료. 수동 SQL Editor 적용으로 비어있던 마이그레이션 이력을 `migration repair --status applied 0001~0012` 로 동기화. 이후 0013+ 는 `supabase db push` 로 적용.
- **Dependabot+audit 게이트 (#63, PR #64 머지)**: npm(web/) 주간 — 보안 패치 개별 PR·일반 minor/patch 그룹, Actions 월간. CI `npm audit --omit=dev --audit-level=high`. 현존 moderate 2건(next 내장 postcss)뿐.
- **주문 쓰기 서버 전용화 (#62, P0)**: 취약점 — 0003 RLS 가 본인 주문 order_items 직접 insert 허용(status 무관). 3천원 주문 생성 후 PostgREST 로 고가 항목 추가 → 확정은 orders.total_price 만 검증 → 3천원 결제로 전체 발송. orders 직접 insert 도 남용 가드 우회.
  - migration `0012`: orders/order_items/gift_options 사용자 insert 정책 전면 회수(select 유지).
  - createPendingOrder: 쓰기를 service_role 로 전환(user_id 는 서버 세션 확정). 항목 저장 실패 시 고아 주문 즉시 삭제(결제 전이라 안전).
  - **적용·검증 완료**: 배포 후 SQL Editor 실행. 실테스트 — 사용자 JWT 로 orders/order_items/gift_options insert 전부 `42501 RLS 위반` 차단, 본인 orders SELECT 정상, service_role orders insert 정상(테스트 행 삽입·삭제). #62 close.

### 13단계 — 운영 가시성·계정 기능 (2026-07-05)
- **어드민 감사로그 (#78, PR #79 머지)**: migration `0013` `admin_audit_logs`(select/insert 없음 — service_role 전용, RLS enable 만). 어드민 쓰기 액션 전부(상품 CRUD·재고·노출·송장·역할 변경) 기록: actor·action·target·변경 diff. `/admin/audit` 조회 화면(최근순·액션 필터).
- **CSRF 점검 + csp-report 가드 (#81 직커밋 b19b99f)**: Server Actions 는 Next Origin 검사 내장 + SameSite=Lax 로 CSRF 안전 확인(정리 문서화). `/api/csp-report` 는 content-type(`application/csp-report`·`application/reports+json`) 가드 — 임의 POST 스팸 차단.
- **클라이언트 에러 로깅 (#83, PR #84 머지)**: migration `0014` `client_error_logs`. `global-error.tsx`+`window.onerror`/`unhandledrejection` → `POST /api/log-error`(zod 검증·rate limit·본문 상한). `/admin/errors` 조회 화면. Sentry 도입 전 공백 메우기.
- **계정별 서버 장바구니 (#85, PR #86 머지)**: migration `0015` `cart_items`(본인 RLS)+`0016` product_id 인덱스. 게스트 로컬 유지→로그인 시 병합·서버 로드→로그아웃 시 로컬만 비움. `lib/cart-sync`(products 조인 — 가격 최신·비활성 제외), store write-through(낙관적), **사용자별 순차 큐 enqueueCartWrite**(out-of-order 방지), 신규 항목 99 cap. CodeRabbit 2회전 클린 후 머지. 0015·0016 db push 적용.
- **비밀번호 정책 강화 (#87, PR #88 머지)**: 회원가입 검증 `length<6` → 8자 이상+영문·숫자 포함. 기존 계정은 로그인 영향 없음(가입 시에만 적용).
- **인스타그램 피드 (#89, PR #90)**: About 플레이스홀더 → IG Graph API `/me/media` 실피드(서버 컴포넌트, 1시간 ISR). 토큰 미설정·실패 시 플레이스홀더 폴백. next/image 프록시라 CSP 변경 불필요. **활성화엔 user 의 Meta 장기 토큰(60일) 발급 + Vercel env `INSTAGRAM_ACCESS_TOKEN` 필요.** web/.gitignore `!.env.example` 예외 추가(템플릿 커밋 가능해짐).

### 111단계 — 배송지 우편번호 검색(다음/카카오)·입력 숫자폰트 통일·포트폴리오 카드 교체 (PR #472~#473, 2026-08-13)
- **우편번호 검색 (#472)**: checkout·마이페이지 배송지에 "우편번호 찾기" 버튼 → 다음/카카오 우편번호 위젯을 모달에 embed → 우편번호·주소 자동채움(상세만 수동). `components/common/postcode-button.tsx`(로더 지연로드·1회 프라미스 캐시·embed 레이어). 마이페이지 배송지 추가 폼을 client(`AddressAddForm`)로 분리해 위젯 재사용(server action `addAddress` 유지, postcode·address 만 controlled).
  - **CSP**: `script-src`+`t1.daumcdn.net`+`'unsafe-eval'`(로더가 eval 로 코드 조립), `frame-src`+`postcode.map.kakao.com`(카카오 리브랜딩)·`postcode.map.daum.net`(폴백), `connect-src`+`t1.daumcdn.net`.
  - **검증 교훈**: 로더가 iframe 을 **프로토콜-상대 URL**(`//postcode.map.kakao.com/search`)로 삽입 → 로컬 dev(http)에선 http iframe 이 돼 CSP `https://` 규칙과 불일치(로컬 한계). 로더 소스를 grep 해 **http:// 하드코딩 0** 확증 → 프로덕션 https 에선 https iframe 으로 매칭·정상. Vercel 프리뷰는 SSO 보호라 무인증 검증 불가 → 소스 확증 + eval 해소(로컬) 로 갈음. `next dev --experimental-https` 는 mkcert 자체서명 실패로 http 폴백돼 로컬 https 검증엔 못 씀.
- **입력 숫자폰트 통일 (#473)**: 배송지 **입력 필드**(연락처·우편번호·주소·상세)가 body 산세리프라 목록 표시(`font-numeric` Cormorant+마루부리 tabular)와 튐 → 입력 4종에 `font-numeric` 적용. 받는 분(이름)·배송 메모는 제외(숫자 아님). checkout·마이페이지 동일.
- **포트폴리오**: WABI-SABI 트러블슈팅 P.14 의 backdrop-filter 카드 → **비회원 결제 버그 카드**로 교체(대표님 선택 — 결제 차단이 임팩트 큼). overflow 0 검증 후 PDF 재생성(`~/Downloads/박시열_포트폴리오_v2.pdf`).

### 110단계 — 헤더/페이지 영어화 마무리·비회원 결제 버그 완결·토스 심사 답변 (PR #465~#470, 2026-08-11~13)
- **shop 구분선 제거+헤더 간격 (#465)**: 카테고리 탭·정렬 툴바 하단 border-b 제거(대표님 — 선 없는 게 담백), Container 상단 py-16→pt-24.
- **카테고리 탭 이격 (#466)**: MobileCategoryTabs 루트 mt-6 — 제목("상품")과 탭 붙어보임 해소.
- **모바일 드로어 분류 영어화 (#467)**: 햄버거 드로어의 SHOP 하위 분류가 아직 node.ko/c.ko 였음 → catLabel(name_en 우선, 대분류 "-"면 name_ko) 도입. TABLEWARE·Plate·Bowl…, "전체"→All. 이 달의 상품·오늘의 와비사비는 한글 유지. 이로써 데스크톱 사이드바·모바일 탭·드로어 3곳 영어 통일.
- **헤더 노출 라벨 영어화 (#468)**: 관리자→Admin(데스크톱 nav+드로어), 위시리스트→Wishlist, 주문 내역→Orders. aria-label(스크린리더용)은 한국 사용자 위해 한글 유지.
- **페이지 제목 H1 영어화 (#469)**: shop "상품"→Shop(카테고리 선택 시 heading 도 name_en, 소분류 접시→Plate), wishlist→Wishlist, orders→Orders. 이 달의 상품 heading·빈 상태 문구·푸터 분류는 대표님 지시로 한글 유지.
- **비회원 결제 버그 완결 (#470)** ⭐: 토스 심사 답변 준비 중 발견 — checkout 렌더 가드 `if(!mounted||authLoading||!user||빈장바구니)` 의 `!user` 때문에 게스트가 결제 폼 대신 "로딩 중"만 봄. **94단계(#362)가 useEffect 리다이렉트 게이트·토스 ANONYMOUS 키 분기는 풀었으나 렌더 가드의 !user(#27부터)를 안 고쳐 반쪽**이었던 것. 렌더 가드에서 !user 제거로 완결(하위 user 참조는 전부 user?. 또는 !user 가드, 저장주소 UI 는 addresses.length>0 조건부라 게스트 미노출 — null 안전). **로그아웃 상태 실결제(테스트키) 검증 통과** — 토스 위젯·카카오페이/카드 정상, 에그 트레이 4구 23,500원(20,000+배송3,500) 정상.
- **토스페이먼츠 심사 답변**: ① 상품 URL=https://wasa.kr/shop ② 환불정책=https://wasa.kr/legal/refund ③ 도자기·유리 기물/오브제 큐레이션 셀렉트샵(테이블웨어+액세서리·생활소품·공예 낱개 판매) ④ 단건 최고가=상품 20,000원(에그 트레이 4구), 옵션(선물포장4,000·쇼핑백700)+배송3,500 포함 ~28,200원 + **비회원 구매 가능**(테스트계정 불필요).

### 109단계 — 404 브랜드 로고·shop 분류 드릴다운/영어화 (PR #458~#463, 2026-08-11)
- **404 한자(侘) → 브랜드 로고(#458)**: 한 글자 侘은 와비사비 어원 아는 사람만 읽혀 "왜 한자?" 혼란(대표님·시열님). "와비" 라벨을 붙여도 미학에 관심 없으면 여전히 모름 → 해독 필요 없는 **브랜드 로고(붓 사발 마크 + WABI SABI 텍스트)**로 교체. 대표님 제공 원본(투명 배경)을 트림·640px 리사이즈해 `public/brand/logo-stacked.png`. 크림 배경 프리뷰로 가독성 확인. 侘의 시적 의미는 홈 철학(侘·寂·選)이 계속 담당.
- **⭐ shop 분류 드릴다운 + 영어화(#459~#463, 반복 조율)**: 대표님 피드백 여러 번 왕복 끝 최종형 확정.
  - **최종 동작(드릴다운)**: shop 기본 = `All · TABLEWARE · OBJECTS`. 대분류 클릭 = `All + 그 대분류의 소분류만`(다른 대분류·헤더 숨김, All 로 전체 복귀). `activeParent`(현재 대분류거나 그 소분류)로 판정. 데스크톱 사이드바·모바일 상단 인라인 동일.
  - **영어 라벨**: 전체→All, 소분류는 `name_en`(Plate·Bowl·Cup·Cutlery·Tea ware / Acc·Living·Craft). `catLabel`=name_en 우선, 비었거나 "-"(대분류 TABLEWARE·OBJECTS는 name_en="-")면 name_ko 폴백. **DB categories.name_en 직접 갱신**(accessory→Acc·life→Living·teaware→Tea ware, service_role REST). 트리는 캐시 없어 즉시 반영. 라벨은 어드민 카테고리에서 대표님이 계속 편집 가능.
  - **이 달의 상품·오늘의 와비사비**(카테고리 아닌 특별 컬렉션 분리): **모바일=우측 드로어**(인라인 바에서 제거), **데스크톱=사이드바 하단 상시**(드로어 없음). 
  - 반복 이력: 아침 아코디언(#453/#454) → "전부 노출"(#459) → 컨텍스트형(#460) → 그 그룹만+‹전체(#461) → 순수 소분류(#462) → 영어+All 복원+배치정리(#463). 매 단계 로컬 dev+Playwright/DOM 실측 검증.
- **낙관적 업데이트(문의)**: 대표님 요청했으나 삭제가 `revalidatePath`로 이미 즉시 반영돼 불필요 판단(무변경). 느려지면 useOptimistic 로 리스트 클라화.
- **포트폴리오(비코드)**: P.14 WABI-SABI 문제해결에 3번째 카드 — backdrop-filter가 fixed 자손 컨테이닝 블록이라 모바일 드로어가 헤더(64px)에 갇힌 버그(#415), 헤더 밖 형제로 해결. 렌더 검증(오버플로 0)·PDF 재생성.
- **👤 대표님 확인**: OBJECTS 순서(현재 DB정렬 Acc·Living·Craft vs 대표님 표기 Acc·Craft·Living)·"Tea ware"↔"Teaware"·웹 이달/오늘 하단배치 OK 여부.

### 108단계 — 배송비 정책 실적용·분류 아코디언·배경음 제거·모바일 푸터 버그 (PR #447~#456, 2026-08-10~11)
- **배송비 정책 실적용 (#448, 마이그 0049)**: 대표님 확정 = **7만원 이상 무료·미만 3,500원**. `lib/shipping`(FREE_SHIPPING_THRESHOLD·BASE_SHIPPING_FEE·shippingFeeFor·SHIPPING_NOTICE — 문구를 상수에서 파생해 안내≠청구 불일치 방지). **서버(checkout/actions)가 배송비 재계산해 total_price 합산**(클라 불신), confirm_order_paid RPC 는 저장 total 만 대조라 배송비 포함 안전(항목합 재계산 안 함). `orders.shipping_fee` 스냅샷 컬럼. 체크아웃 동적 배송비 라인 + "N원 더 담으면 무료배송" 유도, 주문상세·확인메일·비회원조회에 배송비 표기. **문구 2줄 교체(#450)**: "기본배송 3,500원 / 7만원 이상 무료배송"(대표님 표기). **원칙: 금액=lib/shipping 상수가 진실, 편집 필드는 표현만.**
- **분류 사이드바 아코디언 (#453·#454)**: 대표님 — 대분류 누르면 그 소분류만 밑에, 알약 없이 작은 글씨, 미니멀. **데스크톱(ShopSidebar, #453)**: 항상 전부 펼치던 트리 → 현재 대분류(또는 그 소분류)만 펼침(네비게이션=펼침, 무JS)·소분류 text-xs·border 제거. **모바일 드로어(SiteHeader, #454)**: 클라 컴포넌트라 인터랙티브 — openCat 상태로 대분류 탭 시 그 소분류만 펼침(한 번에 하나·ChevronDown), 펼친 뒤 '전체'로 대분류 전체보기, 드로어 닫으면 초기화.
- **배경음 완전 제거 (#451)**: 대표님 불필요 판단. AmbientPlayer 마운트·컴포넌트·음원 에셋(3.4MB)·힌트 keyframes(wabi-eq·wabi-hint-glow) 전부 삭제. localStorage 'wabi.bgm' 자연 소멸.
- **모바일 shop 푸터 덮는 버그 (#455·#456)**: 가끔 /shop 첫 진입 시 긴 푸터가 헤더 밑으로 올라와 화면 전체 차지. **원인**: 스티키 푸터가 `html.h-full→body.min-h-full` 높이 체인 + `main.flex-1`/`footer.mt-auto` 에 의존했는데, 로딩·CSS 적용 타이밍에 이 체인이 접히면 body 가 콘텐츠 높이로 줄어 남는 공간을 못 만들어 푸터가 위로 붙음. **수정**: body 를 뷰포트 기준 **`min-h-dvh`** 로(높이 체인 의존 제거). Playwright webkit/chromium+CDP 스로틀로 재현 시도(자동화론 극단 전환창은 못 잡았으나 취약 구조 확정). **#456**: #455 머지 후 Vercel 프로덕션 빌드 일시 실패(verify·로컬 build 통과·min-h-dvh 는 admin/layout 기사용이라 코드 무해)로 미반영 → 재배포 트리거 빈 커밋으로 라이브 확정. 대표님 실기기 확인 완료(정상). **원칙: 스티키 푸터는 뷰포트 기준 min-height(dvh)로, html 높이 체인 의존 금지.**
- **잔손질**: /today 모바일 분류 가로바 숨김(#447, shop 과 동일 tabletOnly) · About '오시는 길' 지도를 제목 높이로 정렬(#449, 제목을 좌측 열로) · 상품 설명 줄바꿈 보존(#452, whitespace-pre-line — 관리자 엔터가 상세에서 뭉개지던 것).
- **포트폴리오(비코드)**: WABI-SABI 문제해결 슬라이드(P.14)에 3번째 트러블슈팅 카드 추가 — **backdrop-filter 가 fixed 자손의 컨테이닝 블록**이라 모바일 드로어가 헤더(64px)에 갇힌 버그(#415)를 헤더 밖 형제로 빼 해결. 렌더 검증(오버플로 0)·PDF 재생성.

### 107단계 — 배송 안내/배송비 편집 필드·수공예 표현 제거·보안 감사 (PR #443~#445, 2026-08-10)
- **배송 안내 편집 필드 (#443)**: 토스 심사 보완요청 ①(배송기간 명시) 대응 — 사이트 어디에도 발송 소요기간이 없었음(환불 3영업일만 존재). `site_content.shipping_info` 편집값(미저장 시 기본 문구 폴백)으로, 대표님이 어드민에서 실제 발송 영업일 확정. 노출: **상품 상세 접이식 '배송 안내' 블록**(교환·반품 위) + **교환/환불 페이지 '1. 배송 안내' 신설**(async 전환, 이하 조항 번호·cross-ref 재정렬). 어드민 콘텐츠 '상품 상세' 그룹에 필드 추가(토스 심사 항목 안내문 포함).
- **배송비 안내 편집 필드 (#445)**: 토스 심사는 배송기간+배송비 **둘 다** 표시 요구. 배송기간과 같은 패턴 `site_content.shipping_fee`(기본값 **"배송비는 무료입니다."** — 현재 체크아웃 total = 상품+옵션이라 배송비 0원과 일치). 상품 상세·환불 페이지 배송 안내에 함께 노출 + **체크아웃 주문 요약에 "배송비 / 무료" 라인**(실 청구액 반영). 문구는 텍스트 안내일 뿐 총액 계산은 안 바꿈. **👤 대표님: 유료 배송 정책 원하면 총액 반영 실기능 별도 필요.**
- **'수공예/수작업' 표현 제거 (#444)**: 와비사비는 기물 셀렉트샵이라 전 상품이 수공예가 아님(대표님, 검색 메타·About 은 이미 제거됨). 약관 면책·환불정책·상품상세·주문확인 메일에 남은 하드코딩 정리. **색상·질감 개체차=하자 아님 소비자 안내(클레임 예방)는 유지**(시열님 확인) — 근거를 '수공예'→'도자기·유리 등 소재·제작 과정 특성'으로 리워딩. 환불정책 항목명 '수공예품의 특성'→'색상·질감의 개체차'. `수공예|수작업|손으로 만들` 잔존 0.
- **보안 감사 (신규 취약점 0)**: 핵심 표면 전수 확인 — 결제 금액(서버 DB·RPC 재검증·멱등·ALREADY_PROCESSED 재확인), 토스 웹훅(페이로드 불신·API 재검증), 어드민 인가(`requireAdmin` JWT+DB role, 8개 액션 전부), 비회원 주문조회(전화 스로틀 20/10분+열거방지 동일메시지+게스트한정), 체크아웃(상품·애드온 가격 서버 전량 재계산), Cron(CRON_SECRET Bearer fail-closed), 보안 헤더(HSTS·nosniff·COOP·Permissions·frame-ancestors none·CSP report), XSS sink(JSON-LD product 이스케이프·site 정적). **유일 잔존 하드닝 = CSP `script-src 'unsafe-inline'` → nonce 전환** — 잔여 XSS 위험 낮고(React 자동 이스케이프+JSON-LD 이스케이프) 토스 결제위젯·맵 스크립트 깨질 위험 커서 **토스 라이브 이후로 연기**(대표님·시열님 동의).

### 106단계 — 옵션/품절 UX·모바일 상품등록·주의사항 복수·홈 SEO (PR #432~#441, 2026-08-10)
- **옵션 에디터 사용성 픽스 (#432)**: 대표님 모바일에서 "저장 안 됨" 제보 → 원인은 옵션 이름칸에 "아이보리·블루"를 넣고 선택지칸 비워 값 없는 그룹이 직렬화에서 조용히 드롭(상품은 정상 저장, 옵션만 유실). 행을 이름/선택지 2줄로(모바일 안 잘림) + 예시 명시 + 값 없는 줄 빨간 경고. **원칙: 값 없는 그룹을 조용히 버리지 말고 저장 전에 알린다.**
- **어드민 라벨 (#433·#435)**: 사이드바 '상품'→'상품 관리', '주문'→'주문 관리'.
- **모바일 사이드바 뒤로가기 버그 (#434)**: 어드민 드로어 상단 링크가 `/`(홈)로 가던 것 → 데스크톱과 동일하게 `/shop`(ChevronLeft 추가).
- **품절 표시 (#435·#436·#437)**: 상품 재고 0 = **자동** 'Out of Stock'(카드 오버레이 영문화·상세 인라인). 상세 히어로 사진 위 오버레이 + 인라인 빨간색(대표님 — 상세에서 한눈에). **옵션 선택지별 품절 = 수동 토글**(옵션은 재고 무관이라 `options` JSON 안 `soldOut` 배열, **마이그 불필요**). 어드민 에디터 선택지별 '품절 표시' 칩, 상세는 비활성+취소선+"(품절)", `validateSelection` 이 서버에서도 품절 선택 거부.
- **주의사항 복수 선택 (#438)**: 단일 AttributePicker → CareMultiPicker(프리셋 복수 토글 + 직접 입력 칩 여러 개). 저장은 기존 `care` 컬럼에 **" · " 조인**(프리셋에 " · " 없어 되읽기 안전, 상세 렌더 그대로). care 검증 500→1000.
- **모바일 사진 여러 장 등록 (#439)**: 원인 = 원본 사진 여러 장이 서버액션 바디 15MB를 **합산 초과** → 등록 전체 실패(한 장은 통과). 근본해결 = **업로드 전 브라우저 리사이즈**(`lib/resize-image` 긴변 2000px+JPEG, 실패 시 원본 폴백, `resizeFormImages` 로 FormData 교체). `accept="image/*"`(iOS 다중선택·HEIC→JPEG). 상세 로딩도 빨라짐. **원칙: 대용량 업로드는 클라 리사이즈로 바디 한도·성능 동시 해결.**
- **사이즈 직접 입력만 (#440)**: 규격 제각각이라 프리셋(소/중/대) 무의미 → size 를 텍스트 Input 으로(등록·수정 폼).
- **홈 SEO 대표 페이지화 (#441)**: 네이버 "와비사비" 검색에서 `/` 대신 `/about` 이 먼저 뜸(홈·about description 거의 동일 + about 본문 텍스트 많음, 캐노니컬 둘 다 없었음). 홈 `canonical="/"`(브랜드 멘트 소유), about `canonical="/about"` + description 를 "소개/고르는 기준" 특화로 분리. **👤 대표님: 네이버 서치어드바이저에서 `https://wasa.kr/` 수집요청 + 사이트 대표주소 `https://wasa.kr` 지정(호스트용, 페이지순위는 수집요청이 직접적).** 색인 반영 며칠~2주.

### 105단계 — 모바일 UX 다듬기 + 상품 커스텀 옵션·추가옵션 토글 (PR #421~#429, 2026-08-09~10)
- **모바일 분류를 우측 드로어로 (#421)**: 모바일 /shop 진입 시 상품·정렬만 보이게(대표님). 가로 카테고리바를 모바일(<md)에서 숨기고(태블릿은 드로어 없어 유지) 분류를 우측 드로어로. **드로어 nav SHOP 을 토글 버튼화**(ChevronDown) → 클릭 시 분류 트리 드롭다운(전체·이 달·오늘의+대분류/소분류), 선택 시 `/shop?category=` 이동·드로어 닫힘. 트리는 layout 에서 병렬조회해 SiteHeader 주입. + /today 사진 업로드 라벨 가시성(muted xs → 본문색 굵게).
- **shop 검색창 완전 제거 (#422)**: 모바일에 이어 웹도(대표님). 툴바는 정렬만 우측. `?q=` URL 직접검색은 유지(빈 결과 추천 폴백).
- **/today 인트로 orphan 줄바꿈 (#423)**: max-w-xl 이 좁아 "남겨주세요."만 다음 줄 → max-w-3xl + text-pretty.
- **상품 상세 스캐터 갤러리 모바일 적용 (#424)**: 스캐터(폭·좌우 오프셋)가 md+ 만이라 모바일은 규칙적 → 모바일 폭 70~94%+오프셋+간격 변주. **표시 박스는 슬롯 고정 비율(4/5·정사각·3/4·5/6)+object-cover 크롭** — 원본 전체는 확대(🔍 object-contain)에서만.
- **모바일 구매 바 플로팅 (#425·#426)**: 화면 최하단에 딱 붙어 답답(대표님) → 좌우·하단 띄운 rounded-2xl 카드+shadow. 래퍼 pointer-events-none·카드만 auto. 하단 여백 0.5→1.5rem, 페이지 클리어 pb-32.
- **배경음 토글이 구매 바와 겹침 (#427)**: `/shop/[id]` 에서만 토글을 구매 바 위로(`bottom-[calc(6.5rem+env(safe-area-inset-bottom))]`), 데스크톱·타 페이지 기존 유지.
- **⭐ 상품별 커스텀 옵션 + 추가옵션 개별 토글 (#428, 마이그 0048)**: 상품마다 선택지(색상·모양 등)가 달라 미리 정의 불가 → **등록 시 대표님이 직접 옵션 정의, 손님이 상세에서 선택**. **결정: 옵션=선택만(가격·재고 영향 없음)·추가옵션=개별 on/off.** DB: `products.options`(그룹)·`products.enabled_addons`·`cart_items.options`·`order_items.options`(기존 상품은 추가옵션 둘 다 노출 시드). `lib/product-options.ts`(파싱·`validateSelection` — 서버가 상품 정의와 대조, 누락 시 결제 거부). 어드민 `ProductOptionsFields`(옵션 그룹 에디터+추가옵션 체크박스). 상세 옵션 칩 선택(미선택 시 담기/구매 비활성). 장바구니 store/cart-sync/cart 스레딩. 결제 서버 검증→order_items 스냅샷. **선택값이 마이페이지 상세·확인메일·관리자 주문목록(상품·옵션 컬럼 신설)까지 흘러 대표님이 발송 전 색상 확인.** 옵션은 서버로만 검증(클라 불신)·가격 미영향.
- **고르는 기준 섹션 제목 편집화 (#429)**: 섹션 이름("고르는 기준") 자체도 편집(대표님). `CRITERIA_HEADING_KEY` 신설(CONTENT_KEYS·저장검증 통과), About h2 를 site_content 값으로, 관리자 콘텐츠에 "섹션 제목" 필드.

### 104단계 — 브랜드 아이콘 대장정·모바일 shop·구조 리팩터·인스타 실연동·오늘의 와비사비 통합 (PR #406~#419, 2026-08-08~09)
- **이미지 추가 원클릭 (#406)**: 파일선택+추가 2단계가 헷갈림 → "이미지 추가" 버튼이 파일창 열고 선택 즉시 업로드(hidden input + requestSubmit).
- **브랜드 아이콘/파비콘/OG 통일 (#407~#410)**: 대표님 새 로고(붓 사발+WABI SABI, 투명 배경). **PWA manifest·apple-touch·favicon 다 새 로고로.** 핵심 버그 2건 잡음: ①원본이 **투명 배경(alpha)** → `convert("L")`이 투명→검정 만들어 bbox 오작동 → alpha 채널로 콘텐츠 검출+흰배경 합성(PIL). ②구글 검색 파비콘=옛 마크·iOS 공유시트=og:image 워드마크로 뜬 원인 = **`/favicon.ico`·`/apple-touch-icon.png` 가 404**(Next `app/icon` 규약이 해시 쿼리스트링 URL 을 붙여 크롤러가 무시·폴백) → **public 관례 경로에 clean 정적 파일 배치 + `metadata.icons` 로 clean 링크 emit**, app/icon·apple-icon 규약 제거. **파비콘=마크만(원형 크롭 안 짤리게 여백 넉넉)**, apple/manifest=풀로고, **OG 이미지=브랜드 로고 카드로 재구성**(iOS 공유가 og:image 를 써서). OG 로컬 이미지는 `readFileSync`(함수 내부·정적생성)로 base64 인라인 — `fetch(file://)` 는 빌드 깨짐(정적생성 중 미지원). **원칙: 파비콘/apple 은 public clean 경로가 정답(해시 URL 은 크롤러가 무시). backdrop-filter/transform 은 fixed 자식의 컨테이닝 블록이 된다.** 남은 건 외부 캐시(구글 수일·iOS 기기).
- **shop 정렬 개편·모바일 UX·About 편집화 (#411, 6건)**: 정렬 낮은/높은가격 → **신상품순·주문 많은 순·좋아요순**(마이그 **0047** `product_popularity` 뷰 — order_items/wishlist 를 상품별 **집계 숫자만** 공개, **정의자권한 뷰**라 anon 도 실합계 받되 PII 없음. 후보 상품 순위 받아 JS 후처리 정렬). 정렬 UI 알약→담백한 텍스트 링크. 모바일 검색창 숨김. 아이콘 ✦→✧. **모바일 메뉴 우측 슬라이드 드로어**(딤+닫기). About `わび-さび` 아래 연한 WABI-SABI. **About "고르는 기준" 관리자 콘텐츠 탭 편집화**(content.ts CRITERIA_* keys·site_content).
- **components 도메인 재배치 (#412)**: 평면 38개 → `product/ shop/ moment/ account/ map/ layout/ common/`(admin·home·ui 유지). git mv + `@/components` import 일괄 갱신(90파일). 동작 변화 0.
- **admin/content 페이지별 그룹화 (#413)**: 여러 페이지 문구가 뒤섞여 못 찾음 → 홈·About·상품상세·전역 4개 페이지 블록 + 상단 점프 내비.
- **모바일 shop 카테고리 가로 스크롤 (#414)**: 세로 그룹이 첫 화면 다 먹어 상품 안 보임 → 한 줄 가로 스크롤 바(스와이프·오른쪽 페이드)로 압축, 상품 즉시 노출.
- **모바일 드로어 컨테이닝 블록 버그 (#415)**: 드로어가 상단 64px 바만 뜨고 화면 못 덮음. 원인=헤더 `backdrop-blur` 가 fixed 자식 컨테이닝 블록 → `fixed inset-0` 이 헤더 박스에 갇힘. 픽스: 드로어를 `<header>` 밖 Fragment 형제로.
- **카테고리 아이콘 제거 (#416)**: 이 달의 상품·오늘의 와비사비 앞 ✧ 제거(대표님, 액센트 텍스트만).
- **인스타 실연동 활성화 + 레일 (#417·#418, 08-09)**: 라벨 "Instagram" 영어화. **@wasa.kr 브랜드 계정으로 토큰 발급**(graph.instagram.com, 개발모드라 계정을 **Instagram 테스터로 추가+인스타서 수락** 필요, 앱 게시 불필요). Vercel `INSTAGRAM_ACCESS_TOKEN`+재배포 → **라이브 실피드 확인**(cdninstagram). **웹훅 불필요**(읽기전용 폴링). 피드를 **자동 가로 레일(마퀴)**로(대표님) — 12개 2벌 이어붙여 이음매없이 무한(-50%), hover 정지, reduced-motion 정지. **토큰 60일 만료 → 갱신 필요.**
- **오늘의 와비사비 = shop 분류 노출 (#419, 대표님 결정)**: /today 가 shop 하위가 아니라 별개라 갇힘. 논의 결과(내 추천은 nav 승격이었으나 대표님이 **shop 분류 노출**로 결정) → /today 에 shop 과 동일 분류 내비(데스크톱 사이드바+모바일 가로탭) 추가, 분류 클릭 시 `/shop?category=` 이동. ShopSidebar/MobileCategoryTabs 에 `todayActive` prop(오늘의 와비사비 현재위치 표시·전체 비활성).
- **카카오페이 심사 안내(대표님 상담)**: 온라인 카카오페이 직접연동 심사 = **우리는 토스 PG 사용이라 해당없음**(카카오페이는 토스 승인되면 결제수단으로 포함). 대표님이 원하는 건 **매장(오프라인) 카카오페이** = 별도 신청. 보완요청 ①배송기간 명시(사이트에 문구 필요·토스심사도) ②결제버튼 무반응(토스 라이브 전이라). → 대표님께 "토스 사용, 직접연동 아님" 회신 권고.

### 103단계 — 상품 이미지 순서 재정렬(상세 스캐터 위치 의도 배치) + test 데이터 정리 (PR #404, 2026-08-08)
- **이미지 재정렬 (#404, 대표님 요청)**: 상세 스캐터 갤러리(ProductGallery)는 `images` 배열 순서로 위치 결정(첫 장=대표 히어로, 이후가 SLOTS/RATIOS/GAPS 순환 — 인덱스 기반 결정적). 어드민이 추가·삭제만 되고 순서를 못 바꿔 원하는 배치를 하려면 전부 재업로드해야 했음 → `moveProductImage` 서버액션(인덱스 스왑 ◀앞/▶뒤, url 중복 대비 인덱스 기반, 감사로그+shop/상세 캐시 무효화) + 썸네일 위치 배지(대표/상세N)·이동 버튼(끝단 숨김)·매핑 안내 문구. **흐름: 업로드 → ◀▶ 정렬 → 상세에서 그 순서대로 불규칙 슬롯 배치.**
- **test 데이터 정리(시열님 "정리해", DB 직접)**: "test" 상품(245829a0)·"블랙 도트 면기" 테스트 중복(5fbb72ca, desc="…수정 테스트") → `is_active=false`(되돌리기 쉬움, 진짜 37bf8106 monthly 유지). "테스트" 공지(3f00a127, body="테스트입니다.") → 삭제(백업 보관). anon 실측: 상품 9개·공지 0개, /notice 빈상태 정상. **is_active=false 라 RLS(is_active=true 만 노출)가 shop·sitemap 자동 제거** → 구글/네이버 "test" 색인·중복콘텐츠도 해소. 캐시 120s 후 라이브 반영.

### 102단계 — 실사용자 QA 5라운드 + 보안 방어 2종(COOP·문의 기본비밀글) + RLS 침투검증 (PR #401~#402, 2026-08-08)
- **보안 방어 ①COOP (#401)**: 보안헤더 실측 결과 CSP·HSTS·XFO·nosniff·Referrer·Permissions 는 강력하나 `Cross-Origin-Opener-Policy` 부재 → cross-origin opener 의 window.opener 창 핸들 탈취(탭내빙)·일부 XS-leak 노출. 결제(토스)·OAuth 팝업 통신은 살리는 `same-origin-allow-popups` 로 추가.
- **보안 방어 ②문의 기본 비밀글 (#402)**: 라이브 anon 침투 중 발견 — 문의가 "1:1 문의" 프레이밍인데 `is_secret` 기본 false(공개)라, 비밀글 체크 놓친 손님 본문(주문번호·연락처)이 anon PostgREST 로 공개 조회됨. RLS 는 정상(secret=owner 전용). 기본값 privacy footgun → 체크박스 `defaultChecked`(기본 비밀글)+안내 라벨. **원칙: 개인정보 담기는 채널은 기본값을 비공개로(fail-safe).**
- **라이브 RLS 침투검증(취약점 0)**: anon 키로 직접 REST 타격 — SELECT: profiles/orders/order_items/cart_items 0행, inquiries 는 `is_secret=false` 공개글만(정상), audit/error 로그는 API 스키마 미노출(404). WRITE: site_content·products·notices·reviews·inquiries INSERT 전부 42501 차단, products UPDATE 는 204지만 0행(RLS 필터·가격 실변조 없음 확인). **결론: anon 은 공개 콘텐츠만 읽고 모든 쓰기 차단.**
- **QA 5라운드(실사용자 관점)**: 상품상세 플로우·비회원 주문조회·404·/today·/gift·/makers 정상 동작 확인. **발견(코드 아닌 데이터·콘텐츠, 별도 처리):** 🔴 "test" 상품 라이브+sitemap 색인·"블랙 도트 면기" 중복행 2개·"테스트" 공지(유일 공지) — 오픈 전 대표님 데이터 정리 필요(삭제는 확인 후). 🟡 수공예/개체차 문구가 상품상세·/makers(전면 수공예 서사)에 라이브 — 리워딩 대기건 재확인. /gift·/makers 는 nav 제외·sitemap 미포함(고아지만 능동노출 X).

### 101단계 — about 스니펫 세미콜론 픽스·모바일 PWA 폴리시·audit 패치 (PR #398~#399, 2026-08-06~08)
- **네이버 스니펫 세미콜론 제거 (#398)**: 검색결과 about 스니펫이 `わび-さび ; 와비사비는 순간의…` 로 노출. 원인=`;` 는 우리 코드/DB 에 없고(DB philosophy 값 확인: `와비사비는 순간의…`, 세미콜론·わび-さび 접두 없음), 네이버가 짧은 meta description(`소개와 안내`)을 무시하고 페이지 본문(h1 `わび-さび` + 철학 문단)을 긁어 `[h1] ; [본문]` 로 이어붙임. 조치: about `metadata.description` 을 완결 브랜드 멘트 문장으로 교체 → 네이버가 이를 스니펫으로 채택 유도. **원칙: 스니펫을 통제하려면 완결된 meta description 을 준다(짧고 일반적이면 크롤러가 본문을 임의로 긁는다).** 최종 스니펫은 네이버가 결정 → 재크롤(며칠~2주)·수집요청으로 가속.
- **모바일 PWA 폴리시 보강 (#399)**: 사이트가 성숙했지만 모바일 웹앱/공유 폴리시에 구멍(대표님 모바일 우선). ①`app/manifest.ts` 신규 — name `와비사비 WABI-SABI`/short_name `와비사비`/브랜드 멘트/`theme_color`·`background_color` `#f3ebdd`/display standalone/512 아이콘(any+maskable, 기존 `public/brand/icon.png` 재사용). ②`viewport.themeColor #f3ebdd` — 모바일 브라우저 크롬(주소창)을 브랜드 크림으로(사이트는 다크모드 없이 항상 크림이라 단일색). ③`metadata.twitter`(summary_large_image) — X 링크 공유 큰 카드(og:image 폴백). ④`metadata.appleWebApp` — iOS 홈 화면 추가 제목·상태바. ⑤`openGraph.siteName`.
- **의존성 audit 패치 (#399)**: CI `verify` 의 audit 게이트(prod, high+)가 신규 어드바이저로 실패 — `nanoid <3.3.17` high(GHSA-2v37-7h3g-55p8, custom generator size=0 무한루프). `npm audit fix`(비파괴, `--force` 아님)로 nanoid 3.3.18·postcss 등 5개 패치 범위 갱신 → 0 vulnerabilities. **next 16.2.12 유지(major 미변경)**, package.json 무변경(lock 만).
- **노션 개발문서 갱신**: 허브 페이지에 82~101단계 히스토리 추가, 서치어드바이저 등록 체크, "수공예/개체차 고지 리워딩" 다음할일 추가.

### 100단계 — 저장/답변 완료 피드백·문의 직관성·아바타 고정·검색 메타 개편·이메일 게이트 완료 (PR #390~#396, 2026-08-05~06)
- **닉네임 저장 완료 피드백 (#392)**: mypage 닉네임 저장 시 "저장 중…"만 뜨고 완료 표시 없음 → `updateName` 을 useActionState(prev,formData)+결과 반환으로. `NicknameForm`(클라)가 "✓ 저장되었습니다"(초록)/오류(빨강) 인라인 표시.
- **문의 답변 완료 피드백+버튼 (#393)**: 어드민 문의 답변 저장에 완료표시 없고 버튼 밋밋 → `answerInquiry` useActionState 화, `AnswerForm`(클라)가 "✓ 답변이 저장되었습니다"(초록). 답변=adminAction solid, 삭제=danger.
- **문의 목록/상세 직관성 (#394·#395)**: 목록 행을 카드형 버튼(테두리+라운드)+press 피드백, 상태 배지 pill(답변완료=초록 green-50/700·답변대기=회색), 날짜 text-wabi-fg 로 또렷. 상세 페이지에도 동일 상태 배지 추가.
- **아바타 profiles 고정 (#390, 마이그 0046)**: 같은 이메일 소셜(구글·카카오)이 한 계정으로 자동연결(identity linking)되는데 헤더 아바타가 세션 메타(마지막 provider) 따라 흔들림 → `profiles.avatar_url`(트리거 저장+백필)로 고정. auth store `avatarUrl`, AuthProvider 가 role 과 함께 읽음. **원칙: 같은 이메일=한 계정은 Supabase 표준·권장(별도계정 강제 비권장).**
- **검색 노출 메타·카피 개편 (#396)**: 대표님이 직접 문구 편집 — 홈 제목 `와비사비 WABI-SABI`(지역명·수식 제거), 홈 설명=브랜드 멘트("순간의 아름다움보다 시간이 만들어내는 가치를 믿습니다…큐레이션합니다"). **"수공예" 전량 제거(편집샵=큐레이션, 우리는 수공예 아님)**, **"천안 신부동" 메타 텍스트 제거**(실제 주소=JSON-LD address·사업자정보·푸터는 유지). 상품·소개·공지·문의 설명·content.ts 철학기본문구도 개편. 검색 반영은 재크롤링까지 며칠.
- **이메일 게이트 완료 (👤 대표님, 08-06)**: Supabase Custom SMTP(Resend)+Confirm email ON+가짜계정 삭제 3종 모두 완료·검증(가짜 0, 사용자 10→7). 신규 가입=확인메일 링크 필요. 코드측(#388 isPlaceholderEmail·emailRedirectTo)은 이미 반영.
- **미완/후속(👤 대표님)**: **네이버 스마트플레이스 + 구글 비즈니스 프로필 등록**(지도·리뷰·영업시간 패널 — 코드 아님, 사업자번호로 업체등록). 서치어드바이저/서치콘솔 **URL 수집요청**하면 메타 반영 빨라짐. content.ts·상품상세·약관·주문메일의 "수공예/개체차 고지" 리워딩은 별도 논의(반품방어 목적이라 삭제 대신 문구조정 필요).
- 전부 tsc·eslint·build·CI 통과, 스쿼시 머지.

### 99단계 — nav 분류 재편·모바일 분류 드롭다운·admin 버튼·이메일/아바타·오늘의와비사비 픽스 (PR #381~#390, 2026-08-05)
- **nav 재편 (#381)**: 대표님 페이지 분류 확정 → 헤더 `HOME·SHOP·SHOWROOM·NOTICE·Q&A`. GIFT·MAKERS 는 nav 에서 제외(페이지 /gift·/makers 는 유지). NOTICE→/notice, Q&A→/inquiry. 오늘의 와비사비를 shop 사이드바 "이 달의 상품" 바로 아래로 이동. (라벨은 처음 COLLECTION 였다가 대표님 요청으로 **SHOP** 로 확정.)
- **상단 공지 바 제거 (#382)**: NOTICE nav 생겼으니 중복 → NoticeBar 삭제(레이아웃·컴포넌트). getNotices 는 /notice 가 계속 사용해 유지. **홈 공지 노출 안 함 확정.**
- **문구 다듬기 (#383)**: mypage 닉네임 안내·탈퇴 안내 가운뎃점→쉼표 등(시열님 의도 변경).
- **이 달의 상품·오늘의 와비사비 통일 + 모바일 분류=웹 동일 (#384)**: 사이드바에서 오늘의 와비사비가 버튼이라 튐 → 둘 다 `✦`+액센트 텍스트 링크로 통일. 모바일 "분류" 드롭다운 내부를 칩 대신 **ShopSidebar(데스크톱과 같은 컴포넌트) 재사용** → 웹과 똑같은 그룹(대분류 헤더+소분류). ShopSidebar 너비 `w-full lg:w-44` 반응형.
- **모바일 드롭다운 자동 닫힘 (#385)**: details 안 링크는 next/link 클라 내비라 열린 채 남아 결과를 가림 → 클라 래퍼 `MobileCategoryDropdown` 이 내부 `<a>` 클릭 감지해 `details.open=false`.
- **분류 클릭 피드백 + 정렬 버튼 (#386)**: 분류 SideLink `active:opacity-40` press 피드백(웹·모바일 공용). 정렬(신상품순/낮은 가격순/높은 가격순)이 희미 → 알약 버튼(선택=채움·나머지=진한 외곽선, `active:scale-95`). 라벨 띄어쓰기 수정.
- **admin 상품 버튼 재디자인 + shop 상품수 제거 (#387)**: 밑줄 텍스트 링크 → `adminAction` cva(solid/outline/danger, 브랜드 토큰) 버튼. 저장·수정=outline, 이 달/노출=상태 토글(on=solid), 삭제=danger, 이미지 추가=outline. shop 헤더 "N개 상품" 제거(대표님).
- **가짜 이메일 차단 + 로그인 폼 폰트 (#388)**: `isPlaceholderEmail`(test.com·example.*·.test 등) 가입·로그인 즉시 차단(**클라 UX 가드**). signUp 에 `emailRedirectTo`(콜백). 로그인 폼 입력·오류/안내 숫자 → font-numeric. **⚠️ 진짜 게이트=Supabase 대시보드 이메일 확인+Resend SMTP(👤 시열님 미완).**
- **오늘의 와비사비 등록 즉시 반영 (#389)**: MomentGrid 가 initial 을 useState 초기값으로만 써 stale → 폼 성공 시 `router.refresh()` + 그리드를 최신 글 id 로 keying(remount).
- **아바타 profiles 고정 (#390, 마이그 0046)**: 같은 이메일 소셜(구글·카카오)이 한 계정으로 자동 연결(identity linking)되는데 헤더 아바타가 세션 메타(마지막 provider)를 따라 흔들림 → `profiles.avatar_url`(트리거 저장+백필)로 고정. auth store `avatarUrl`, AuthProvider 가 role 과 함께 읽어 채움. 헤더가 스토어값 사용. **원칙: 같은 이메일=한 계정(Supabase identity linking)은 정상·권장. 별도계정 강제는 비권장.**
- 전부 tsc·eslint·build·CI 통과, 스쿼시 머지.

### 98단계 — UI 다듬기 묶음: 배경음·숫자폰트·About 영어제거·공지 바·Shop 재구조·월간상품 강조 (PR #372~#379, 2026-08-04~05)
- **배경음 볼륨↓ (#372)**: `ambient-player` `VOLUME` 0.06→0.035(약 40%↓, 대표님 — 조금 큼). 페이드/게인 없어 상수 하나가 전부.
- **숫자 폰트 통일 (#373)**: 어드민 대시보드·마이페이지 닉네임 숫자가 oldstyle(3·5 처짐)로 튐 → `.admin-numeric`(font-variant-numeric: lining-nums tabular-nums) 어드민 루트 셸에 부여(input/textarea/select 는 상속 안 돼 명시 타깃). mypage 닉네임 Input 은 `font-numeric`. 폰트는 그대로 Cormorant, 숫자 글리프만 라이닝.
- **About 영어 제거 (#374)**: `(Wabi-sabi)`·기준 영문(Trace of Hands 등)·`Showroom`·인스타 섹션 `Instagram`→`인스타그램`. 유지: `わび-さび`(일본어)·`wasa.kr`(인스타/도메인)·이메일·메타데이터 브랜드명.
- **상단 공지 바 (#375, #376)**: 공지가 푸터에만 있어 찾기 어려움(대표님) → 최신 공지 제목을 헤더 위 espresso 띠로(NoticeBar). 클릭→`/notice/[id]`, X 닫기(공지 id 기준 — 새 공지 오면 재노출). **홈·어드민 제외**(pathname self-hide, 홈=곡선 무드 유지). `useMounted`(useSyncExternalStore)로 hydration 안전. 레이아웃서 `getNotices()` 최신 1건 전달. **버그 2건 잡음**: ① env 없는 CI 빌드에서 `/_not-found` 프리렌더가 createPublicClient throw → notices 쿼리에 env 가드(#375). ② 공지 삭제/추가가 최대 120s 반영 안 됨 — `unstable_cache` 는 `revalidatePath` 로 무효화 안 됨(경로 캐시만) → `tags:["notices"]` + 서버액션서 `updateTag("notices")`(Next 16, 1인자·read-your-own-writes; `revalidateTag` 는 2인자化)(#376). **원칙: unstable_cache 무효화는 태그로. revalidatePath 는 데이터 캐시 안 지움.**
- **Shop 대분류 중복 제거 (#377, 2안)**: shop 진입 시 소분류 영역에 대분류(TABLEWARE·OBJECTS)가 중복 노출(위계 꼬임) → 헤더 nav 에서 TABLEWARE·OBJECTS 빼고 단일 진입점. 데스크톱 사이드바·모바일 칩 모두 **두 그룹 소분류를 항상 노출**, 대분류 헤더/`[그룹] 전체` 칩은 그룹 전체보기로 유지. `getCategorySlugs`(그룹→소분류 확장)·heading 로직 불변.
- **nav 라벨 SHOP→COLLECTION (#378)**: "SHOP" 은 노골적 판매 인상 — 상품을 팔되 드러내지 않는 자연스러움이 모토(대표님) → `COLLECTION`(모아둔 것을 둘러보는 결). 경로 `/shop` 유지.
- **이 달의 상품 특별 표시 (#379)**: 필터(사이드바·모바일 칩)에 액센트 색+`✦` 마크(모바일 칩은 활성 시 채워짐, FilterLink `accent`), is_monthly 상품 카드 좌상단 "이 달" 씰(espresso/크림, z-20). `getProducts`·`getShopBrowse` select 에 `is_monthly` 추가 → `ProductCardData.isMonthly`.
- 전부 tsc·eslint·next build 통과, CI(verify·Vercel) pass 후 스쿼시 머지. **현재 헤더 nav: COLLECTION·GIFT·MAKERS·SHOWROOM**(GIFT·MAKERS 는 대표님 컨펌 대기 예시).

### 97단계 — 장바구니 재로그인 99개 중복 버그 픽스 + 선물 소분류 상태 확인 (PR #370, 2026-08-04)
- **대표님 제보 버그**: 로그인할 때마다 특정 상품이 장바구니에 **99개**(상한)로 쌓임.
- **원인**: cart 스토어가 `items` 를 localStorage 에 영속하는데, 로그인 시 `bindUser` 가 **서버 카트**를 그대로 `items` 에 넣는다. 이 값이 게스트 캐시로 저장되고 → 다음 마운트(새로고침·재로그인)에서 `boundUserRef`(useRef) 초기화 후 `SIGNED_IN` 발생 시 `mergeGuestCart` 가 그 서버 사본을 "게스트 카트"로 오인해 **자기 자신과 합산**(`min(server+guest,99)`). 5→10→20→40→80→**99** 배가.
- **수정**: `partialize: (s) => ({ items: s.userId ? [] : s.items })` — 로그인 상태(userId≠null)면 items 를 영속하지 않는다(빈 배열). 로그인 카트는 **서버가 진실**이라 재병합할 로컬 사본이 없어야 함. 게스트일 때만 로컬 캐시 → 로그인 1회 병합은 유지. 원칙: **낙관적 로컬 캐시와 서버 소스오브트루스를 같은 필드에 담을 땐, 서버 사본이 다시 "로컬 입력"으로 역류하지 않게 영속 경계를 나눈다.**
- 기존에 서버 카트가 이미 99로 부푼 사용자는 장바구니에서 1회 삭제/수량조정 시 정리됨(write-through 서버 반영).
- **"오브젝트 > 선물 선택지 제거" 요청**: 조사 결과 **선물 카테고리는 이미 remote DB 에서 완전 삭제됨**(96단계 0045 는 숨김이었으나 이후 admin 에서 삭제된 것으로 확인, +부채 소분류 신설). admin 상품 폼 2곳(등록·수정)은 이미 `is_active=true` 필터라 미노출, 코드 폴백 트리(site.ts)도 선물 없음 → **어떤 선택지에도 "오브젝트 > 선물" 렌더 불가. 스샷은 캐시/구버전. 코드 변경 불요.**
- 검증: tsc/eslint 통과, CI(verify·Vercel) pass, 스쿼시 머지.

### 96단계 — 작가 소개(MAKERS) 예시 페이지 + shop 정리·선물 소분류 제거 (PR #368, 2026-08-04)
- **시열님**: 대분류 예시 페이지 더 만들어 대표님 컨펌 예정 → **/makers**(작가 소개) 신설. 히어로 + 작가 카드 3(만드는 방식·스타일 샘플 — 실제 데이터 없어 레이아웃 데모용) + CTA. 내비에 MAKERS 추가(예시, 미채택 시 제거). 컨펌 후 실제 작가 사진·이야기로 교체 또는 admin/DB 승격.
- **대표님 피드백 3건**: ① 사이드바 대분류 뒤 작대기(`TABLEWARE -`) 제거 — DB `name_en="-"`(대표님이 비운 값)이면 영문 부제 미렌더 가드(shop-sidebar). ② shop 전체 헤더 `"Shop"`(라틴 Cormorant 라 홀로 튐) → `"상품"`(한글 명조, 사이트 통일). ③ OBJECTS "선물" 소분류 제거(GIFT 대분류 /gift 중복) — **마이그 0045 로 DB `is_active=false` 숨김**(reversible). **소분류 상품(찻잔 세트)은 `getCategorySlugs` 가 숨김 분류도 확장하므로 OBJECTS 필터엔 계속 노출 → 상품 손실 없음.** 코드 폴백 트리(site.ts)에서도 제거. 0045 remote 적용 완료(db push). 대표님이 admin 에서 찻잔 세트를 다기 등으로 이동하면 마무리.
- 검증: tsc/eslint/next build 통과, shop·makers 스샷 확인. **현재 내비: TABLEWARE·OBJECTS·GIFT·MAKERS·SHOWROOM(GIFT·MAKERS 는 대표님 컨펌 대기 예시).**

### 95단계 — 대분류 신규 페이지 "선물(GIFT)" (PR #366, 2026-08-04)
- **대표님**: 대분류 내비에 신규 페이지 추가 요청 → 후보(선물·작가공방·저널·기획전) 중 **선물(GIFT)** 선택(매출 직결 + 기존 선물 포장 애드온 연결 + 1인 유지 부담 낮음).
- **/gift**: 히어로 카피 + 선물 포장 안내(선물 포장 4,000원·메시지 카드·쇼핑백 700원 — 가격은 `lib/addons` ADDONS 카탈로그를 소스오브트루스로 표기) + "선물하기 좋은 그릇" 큐레이션(`getProducts` 최신순 8점 자동 채움 → 유지 부담 0) + 전체 상품 보기 CTA. 카피 하드코딩(대표님 검토 후 조정, 원하면 admin 편집화 후속).
- **내비(site.ts)**: `TABLEWARE·OBJECTS·GIFT·SHOWROOM` — 헤더·모바일 메뉴 자동 반영. 기존 컴포넌트(ProductCard·CtaLink·Reveal) 재사용, wabi 색 토큰·serif 스택으로 정합. **교훈: 페이지 폰트는 전역 body `--font-sans`(=serif 스택 Cormorant→마루부리→NotoSerifJP)를 그대로 쓰면 한글이 마루부리로 정상 렌더 — 특정 `--font-noto-serif-jp` 강제하면 한글이 폴백으로 빠짐.**
- 검증: tsc/eslint/next build 통과, 데스크톱·모바일 반응형 확인. (그리드는 최신 상품 자동 — test 상품 정리하면 자동 갱신).

### 94단계 — 비회원(게스트) 상품 구매 허용 (PR #362, 2026-08-04)
- **대표님**: 비회원도 상품 조회·구매 가능하게. 회원 전용 기능(오늘의 와비사비 등)은 유지, 결제만 로그인 없이. **회원 결제 포인트 혜택은 추후 별도**("할 거래").
- **DB 무변경**: `orders.user_id` 는 0001 부터 nullable(`on delete set null`)이라 마이그레이션 없이 앱 레이어만.
- **checkout/page.tsx**: 로그인 리다이렉트 게이트(`if(!user)→/auth`) 제거. 토스 결제는 비회원이면 `customerKey=ANONYMOUS`(SDK 상수, 일회성), `customerEmail` 은 회원만(`user?.email`).
- **checkout/actions.ts `createPendingOrder`**: 로그인 요구 제거, `user_id = user?.id ?? null`. **rate limit: 회원=user_id / 비회원=전화번호. 비회원 주문은 RLS(own-orders-select, auth.uid()=user_id)로 사용자 클라이언트에서 안 보여 `admin`(service_role)으로 집계**(중요 — 게스트 주문 카운트는 admin 필수).
- **success/page.tsx**: 비회원은 "주문 내역" 버튼 숨기고 회원가입 안내. 서버 장바구니 비우기는 회원만(게스트는 로컬 ClearCart).
- **보안 검토**: 금액은 서버 DB 가격 재계산(클라 불신)→위조 불가 / 게스트 주문(user_id null)은 어떤 회원도 RLS 로 조회 불가, service_role 만 읽음→노출 없음 / 주문확인 메일(order-confirmed)은 `!order.user_id` 시 스킵→게스트는 메일 없음(크래시 없음). **후속 여지: 게스트 주문 추적·전자상거래법 §13 서면통지용 선택 이메일 필드(orders.email).**
- 검증: tsc/eslint/next build 통과.
- **비회원 주문조회(#364)**: 게스트는 계정이 없어 주문확인 메일 안 감(실무 표준) → 대신 **주문번호+전화번호 조회 페이지**(`/order-lookup`, 조회 전용). 서버 액션 `lookupGuestOrder`(admin — 주문은 RLS 본인전용이라 우회). **보안: ①주문번호+전화번호 둘 다 일치 + `user_id IS NULL`(게스트 한정, 회원 주문 노출 안 됨) ②열거 공격 방지(어느 필드 틀렸는지 안 알림·동일 실패 메시지) ③브루트포스 스로틀(0044 `guest_lookup_throttle`, 전화번호 10분 20회, cleanup 크론이 1일치 정리).** 결제 성공화면에 **주문번호 노출**(게스트가 이 번호로 조회) + 주문조회 링크·푸터 링크. **스로틀 테이블 미적용이어도 조회는 동작(count 없음→0→통과)**. **⚠️ 배포 후 `supabase db push` 필요 — 0044 + 대기중이던 0043(prep_notice)도 remote 미적용 상태였음(prod prep_notice 는 admin 토글로 동작 중인 듯).**

### 93단계 — 모바일 헬릭스 납작 타원·loops 7·텍스트 롤백 (PR #360, 2026-08-04)
- **대표님 반복 피드백 종합**(여러 라운드): 한자 위화감→축소 / 웹 로드 시 첫 멘트 노출(시간페이드가 공유 render 라 데스크톱까지 먹음)→모바일만 적용 / 웹·모바일 UX 통일 / 곡선이 웹과 달라(둥근 큰 원)→**납작한 타원** / 멘트 두 줄→한 줄 시도 / **웹에서 글씨 작음→배포본 크기 롤백** / **회전수 6~7 이 예쁨, 8 은 과함**.
- **최종 착지**: 텍스트는 배포본(한자 40/52·본문 14/17)이 제일 보기 좋아 롤백(축소했더니 웹 데스크톱까지 작아짐). 곡선은 **rYRatio 0.33**(웹 0.3 근접, 납작 타원) + **loops 7**. 멘트 극점 **k=[2,6,10] 전부 우측 극점** → 텍스트 전부 좌측 여백(납작 렌즈는 폭이 넓어 극점 반대쪽만 안전).
- **🔑 첫 멘트 페이드 = 모바일만 시간 페이드(render `i===0 && ci===1`)**: 시간 페이드는 리드인 코일 없이 첫 멘트를 상단(k2)에 둬 회전수를 6~7 로 억제. 데스크톱은 리드인+위치 페이드라 웹 로드 시 첫 멘트가 안 보인다(스크롤로 등장). **교훈: 시간 페이드를 공유 render 에 넣으면 두 캔버스 다 먹으니 캔버스 인덱스(ci)로 분기.**
- **🔑 곡선 걸림 3원칙 재확인**: ①납작 타원(낮은 rYRatio)은 교차 렌즈가 좌우로 넓어 극점 반대쪽 텍스트도 관통 → 멘트를 전부 우측 극점+좌측 텍스트로 ②loops 많을수록 렌즈 작아 걸림 심함 ③좁은 세로화면서 "웹처럼 납작"과 "회전 적음"은 상충(납작=세로 짧아 여럿). 큰 텍스트(2줄)라도 우측 극점 배치면 렌즈 사이 여백에 앉음.
- **검증**: 데스크톱 로드 시 侘 opacity 0, 모바일 시간 페이드, 각 멘트 peak 곡선 겹침 0. **현재 모바일 최종: 반경320→450·rYRatio0.33·피치520·loops7·멘트k=[2,6,10](전부 우극점·좌측텍스트)·첫멘트 모바일 시간페이드/데스크톱 위치페이드·fade0.16·텍스트 40/52·14/17.** "이게 최선, 다음에 또" — 대표님 잠정 승인, 추후 재론 여지.

### 92단계 — 모바일 헬릭스 첫멘트 시간페이드·회전수↓·곡선 걸림 해결 (#356→#358, 2026-08-03)
- **대표님 실기기 반복 피드백**: 회전수 과함(#356 loops8) → loops5(#358초안, 과성김) → loops7(걸림) → **시열님 핵심 지적: "첫 멘트 숨김이 회전수를 늘리는 것 아니냐"** = 정확.
- **🔑 근본 원인**: 첫 멘트가 스크롤하며 무→등장하려면 로드 시 화면중앙 **아래**에서 시작해야 함(위치기반 페이드) → 그 공간이 **리드인 코일 2~3바퀴** → 회전수의 큰 몫. **3중 제약(회전↓·첫멘트숨김·곡선 안 걸림)이 좁은 세로화면서 서로 상충.**
- **해결 — 첫 멘트 페이드인을 "스크롤 위치"→"로드 후 시간"으로 전환**(`render` i===0 분기): 로드 시 opacity 0 → smoothstep 1.6s 로 서서히 등장(선 드로잉과 함께), 스크롤 시 점이 화면 상단(≈6%) 이탈하면 소멸. **리드인 코일 불필요 → 첫 멘트 상단(k2) 배치 → loops 7→6.** 대표님 "무에서 서서히" 취지 그대로 충족.
- **곡선 걸림 해결**: 멘트 극점 **k=[2,6,10] 전부 우측 극점** → 텍스트 전부 좌측 여백(점은 우측 끝이라 안 겹침). 긴 본문 選 은 **텍스트 폭 51%→65%(left-7 right-28)** 넓혀 5줄→4줄로 줄여 교차 타원 렌즈에 들어가게. **교훈: 곡선 걸림은 loops 많을수록(렌즈 작아져) 심함 / 긴 본문은 폭 넓혀 줄 수 줄이면 렌즈에 맞음 / 우측 극점+좌측 텍스트가 가장 깨끗(점·텍스트 반대쪽).**
- **검증법**: 첫 멘트는 시간페이드라 scroll0 에서 2초 후 실측(로드 직후 0→1.6s 1.0), 나머지는 스크롤 peak. 멘트 간격 32%(≈408px)로 페이드 겹침 최소. **현재 모바일 최종: 반경320→450·rYRatio0.42·피치560·loops6·멘트k=[2,6,10](전부 우극점·좌측텍스트·폭65%)·첫멘트 시간페이드1.6s·fade0.16·vb3480.** 데스크톱 무변. **대표님 확인 대기 후 실운영.**

### 91단계 — 모바일 헬릭스 곡선 웹 동일화(피치 레버 발견) (PR #354, 2026-08-03)
- **대표님 재피드백**: 웹 곡선은 만족, 모바일 아쉬움 — "웹과 최대한 동일한 곡선 형태". 90단계(#352) 0.5 근사에 만족 못 하심.
- **실측 재진단(Playwright)**: 루프 종횡비 웹 **0.48** vs 모바일 **0.91** — 모바일 루프가 2배 세로로 늘어남. **원인은 rYRatio(타원 납작함)가 아니라 루프당 피치였다.** #352에서 rYRatio만 낮췄다가 슬라이버로 반려된 게 엉뚱한 레버를 잡은 것. → **90단계 "픽셀 동일 불가·0.5 최적" 결론을 정정한다.**
- **두 레버 동시**: `loops` 6→**11**로 피치 780→**400**(웹 390 수준) 낮춰 촘촘·납작 스프링 + `rYRatio` 0.5→**0.3**(웹과 동일). **핵심 교훈: 피치가 낮아 타원이 서로 가까워지면 낮은 rYRatio도 슬라이버가 아니라 웹처럼 연속된 납작 코일이 된다. 웹 납작함 = 낮은 rYRatio + 낮은 피치 세트. 지난 실패 = 낮은 rYRatio + 높은 피치(타원 고립).**
- **멘트 재배치**: 극점 6반바퀴 간격 `k=[8,14,20]`(모두 우측 극점 → 좌측 여백에 텍스트 깨끗·페이드 겹침 0). 첫 멘트 侘를 캔버스 **37%**로 내려(리드인 4바퀴) 로드 시 화면중앙 밖 시작 → 페이드인 유지. `MOBILE_MOMENT_NUDGE=[0,0,-10]`(選 종단 루프 회피). vb 4900→**4620**, steps 880.
- **검증**: `tsc`/`eslint` 통과. Playwright 페이드 실측 `scrollY 0`에서 侘·寂·選 모두 opacity 0(로드 숨김), 스크롤 시 순차 등장·겹침 없음. 데스크톱 지오메트리·nudge 무변.
- **SEO 노출 확인(대표님 스샷)**: 구글/네이버 실검색 노출됨. **검색 헤더=`<title>`·본문=`<meta description>`(layout.tsx metadata)로 제어**. **네이버는 우리 값 그대로 반영, 구글은 홈에 크롤 본문이 적어(곡선 무드) 자체 재작성**(제목 `Living Select Shop`←OG/site.ts tagline, 설명←About 소개 본문). 문구 수정은 대표님 확정 대기(지금은 확인만).
- **회전수 축소·멘트 여백 정렬(PR #356, 대표님 실기기)**: 웹 곡선 느낌은 좋으나 11 루프가 좁은 세로화면서 과하게 촘촘 → **loops 11→8**(피치 400→460 으로 밀도도 완화, rYRatio 0.3 유지=웹 납작 타원 그대로). **교훈: 좁은 세로화면은 웹 납작 타원이면 필연적으로 화면당 루프 多(납작=세로 짧아 많이 들어감). 회전수↓·첫멘트숨김(리드인)·멘트간격 동시 만족 하한이 loops 8.** 멘트 k=[8,14,20]→**[6,10,14]**(4반바퀴 간격, 우측 극점, 좌측 여백 정렬), 選 nudge -10→**-13**(종단 루프 회피), fade 0.20→**0.16**(로드 시 侘 opacity 0 실측). **검증법 정립: 비-reduced 로 각 멘트 dotY 를 화면중앙 스크롤(페이드는 점 기준이라 텍스트 아닌 점 중심)해 그 멘트만 곡선과 함께 캡처 = 텍스트-곡선 겹침 진짜 판정.** 멘트는 한 번에 하나만 페이드라 reduced-motion 동시표시 겹침은 착시.

### 90단계 — 브랜드 로고·곡선 웹매칭·모바일 스크롤 부드럽게 (#351~#353, 2026-08-03)
- **브랜드 로고 섹션(#351, 대표님 모바일)**: 영어 로마자(Wabi-sabi) 제거 → わび-さび만 가운데. 본문 글자 축소 + 곡선 멘트 본문과 크기 통일(14px/md 17px). **CtaLink `rounded-none`**(각진 사각형) — 상세·장바구니 등 다른 버튼과 통일. 홈 Shop CTA·shop·사이드바 "오늘의 와비사비" 버튼 공유라 한 번에 네모.
- **모바일 곡선 웹매칭(#352, 대표님)**: 앱 곡선을 웹과 최대한 동일하게. **원인=화면 비율(웹 가로넓음/앱 세로좁음)+타원비(앱 0.74 둥근/웹 0.3 납작)**. 타원비 **0.74→0.5**로 교차 고리를 납작-열린(웹 느낌)으로. **교훈: 0.35는 고리가 얇게 닫혀 오히려 달라짐(큰 피치+낮은 타원비=교차 닫힘). 좁은 세로화면은 웹의 촘촘-납작 고리 물리적으로 불가(잘게 여러 개=답답) → 0.5가 최적 근사. 픽셀 동일은 화면비라 불가.**
- **모바일 스크롤 부드럽게(#353, 대표님)**: 웹처럼. **터치 하이재킹 대신 "연출만 이징"**(시열님 선택) — 네이티브 스크롤·제스처(당겨서새로고침·주소창·탭정지) 그대로 두고, 스크롤 위치 읽어 그리는 연출(선 드로잉·멘트 페이드)만 rAF에서 이징(renderTop lerp). `pointer:coarse`(모바일)만 적용, 데스크톱은 SmoothScroll이 이미 스크롤 이징하므로 직접 따라가 **이중 스무딩(둥둥 뜨는 랙) 방지**. 글라이드 0.12(~0.3s). **교훈: 모바일 터치 스크롤 하이재킹은 floaty·제스처파괴 위험 — 네이티브 유지하고 연출만 이징이 안전한 웹느낌.**

### 89단계 — 모바일 헬릭스 대개편(멘트 여백·페이드·큰 원) (#348, PR #349, 2026-08-03)
- **대표님/시열님 실기기 피드백 여러 라운드**를 스크린샷 반복 튜닝으로 반영. 데스크톱 무변, 모바일 캔버스만.
- **멘트 좌측 여백 정착 + 후광 제거**: 큰 코일의 스파인이 세 극점 프레임 모두 우측에 놓임을 확인 → 멘트를 전부 좌측 열린 공간에 앉힘(중앙+크림후광 폐기). 마지막 極 選은 종단 루프가 본문 자리에 들어와 멘트 블록만 위로, 첫 極 侘은 상단 루프 아래로 살짝 내림(`MOBILE_MOMENT_NUDGE=[6,0,-9]`, 점은 곡선에 그대로 — 멘트가 여백에 뜨는 배치).
- **첫 멘트 페이드 "아무것도 없는 상태에서"**: 홈이 헬릭스-first라 첫 멘트 위에 스크롤 runway가 없어 정지상태에서도 위치기반 페이드로 반쯤 보이던 버그. 멘트를 극점 k=[5,8,11]로 옮겨(loops 4→6) 侘 앞에 리드인 코일을 둠 — 빈 화면이 아니라 선이 흘러내리며 채우고, 侘는 뷰포트 밖(0)에서 시작해 스크롤 시 등장. **페이드 구간을 캔버스별 분리**(`cfg.fade`: desktop 0.32 / mobile 0.20)해 좁은 간격 멘트 겹침 차단. **검증법: Playwright 비-reduced 로 스크롤 위치별 멘트 opacity 측정**(스크롤0=전부0 확인).
- **곡선을 로고까지**: 마지막 반바퀴 꼬리가 로고 쪽으로 내려오고 viewBox 하단 여백 축소 → 선끝~로고 간격 111→45~60px.
- **텀·원 크기 밸런싱**(여러 왕복): 피치↓(700)는 텀 단축되나 타원이 좁아짐(대표님 반려) → **피치 780 복귀 + 타원비 0.6→0.68→0.74**로 코일 길이(텀) 안 늘리고 루프만 크고 둥글게. 최종 **반경 300→430을 320→450(폭 5~95%)**, 타원비 0.74 — "크게 도는 입체 원". **교훈: 타원비(rYRatio)는 텀 무변으로 루프 로만 키우는 레버 / 피치는 텀·세로압축 동반**.
- **`momentPositions`에 ks 인자 추가**(극점 인덱스 커스텀 — 모바일 리드인 오프셋용). 데스크톱은 기본값 유지.
- **함정 재확인**: `momentPositions`·`helixSegments`·nudge·fade·vbH 가 전부 얽혀 스크린샷 없이는 못 맞춘다. 반드시 반복 캡처. **비-reduced 스크롤 측정으로 페이드 상태 정량 확인**(reduced-motion 캡처는 배치용, 페이드는 실측만 보임).
- **데스크톱도 첫 멘트 페이드(#350)**: 웹도 헬릭스-first라 동일 문제(측정 0.623) → 데스크톱 loops 6→7, 멘트 k=[3,8,13]로 리드인 2반바퀴. 배치·피치·간격 무변. 이제 모바일·데스크톱 둘 다 "아무것도 없는 상태에서" 첫 멘트 등장.

### 88단계 — 정식 오픈 준비중 안내 모달 (#346, PR #347, 2026-08-02)
- **배경(대표님)**: 토스 라이브 심사·실상품·가격 확정 전 손님이 결제를 시도했다 실패하지 않도록 진입 시 "정식 오픈 준비중" 안내. 네이버 스마트플레이스 홈피링크 등록 후 노출↑ 시점이라 방어책으로 적기. **선택지 A(닫기 가능 안내 모달) 채택** — 전체 차단(coming-soon)은 방금 시작한 색인·노출(구글 노출 8·우상향, 게재순위 11.1=2p) 리셋 위험이라 배제.
- **전역 layout 마운트**: 홈뿐 아니라 검색·링크로 상품 페이지 직접 진입한 손님도 결제 전 안내를 본다. `/admin`은 컴포넌트 내부 pathname 분기로 제외.
- **차단형 아님·SEO 안전**: "둘러보기"·바깥 클릭·ESC 로 닫으면 자유 열람 → 색인 무손상. 본문은 그대로 SSR, 오버레이만 위(크롤러는 본문만). 닫음은 세션 유지(sessionStorage). `createPortal`로 body 부착(변형·overflow 조상 회피). z-60.
- **어드민 on/off + 문구 편집**: 사이트 콘텐츠 화면 최상단 섹션. `PrepNoticeField`(체크박스 토글, `togglePrepNotice`) + 안내 문구 `ContentField`(`prep_notice_text`, CONTENT_KEYS 편입). 정식 오픈 시 체크 해제.
- **캐시**: `getPrepNotice`(anon 공개 클라 `createPublicClient`, `unstable_cache` revalidate 120s, tag `prep-notice`). 토글 시 `revalidatePath("/", "layout")` 전역 재검증(120s 만료가 안전망). **revalidateTag 는 Next16 서 `(tag, profile)` 2-arg 필수로 바뀜 → 코드베이스 관행대로 revalidatePath 사용.**
- **마이그 0043**: `prep_notice='on'` 시드(`on conflict do nothing`). 배포 후 곧바로 켜짐. 어드민 조작값은 덮어쓰지 않음.
- **CI 픽스(핵심 교훈)**: 로컬 빌드는 `.env.local` 있어 통과했으나 **CI(verify)는 Supabase env 없어 `/_not-found` 정적 프리렌더서 `supabaseUrl is required` 로 죽음** — 루트 layout 을 async+DB 조회로 만들면 **모든 정적 라우트가 빌드타임에 그 조회를 실행**하기 때문. `loadPrepNotice`에 **env 미설정 시 즉시 기본값(표시 안 함) 반환 + try/catch** 폴백 추가. **검증: `.env.local` 임시 이동 후 빌드로 CI 무-env 재현** → `/_not-found` ○ Static 복구 확인.
- **👤 배포 후**: 마이그 0043 적용 **또는** `/admin/content`에서 준비중 체크→저장 시 즉시 노출. 정식 오픈 시 체크 해제.

### 87단계 — 모바일 최적화 계속·SEO 지역키워드·헬릭스 큰 원 (#342~#345, 2026-08-01~02)
- **탭타깃 라운드2 + 탭 피드백(#342)**: 장바구니 삭제(X) 32→44·위시리스트 36→44·공감 히트영역↑. 공유 Button 에 `active:scale-[0.98]`(모바일 hover 없어 탭 피드백 부재 → 눌림 애니메이션).
- **SEO 지역 롱테일(#343)**: 일반명사 "와비사비" 경쟁 심해 신규 도메인 상위 난망 → 브랜드+지역(천안·신부동·그릇·편집샵)으로 타이틀·H1(sr-only)·설명·OG·JSON-LD(alternateName·description) 채움. About(쇼룸) 타이틀 "천안 신부동 쇼룸". **👤 비코드: 네이버 스마트플레이스 홈피링크 wasa.kr 등록·인스타 프로필 링크 교체(백링크). 색인≠랭킹**.
- **실기기 4건(#344)**: **CTA 채움 모바일 고정**(홈 Shop·오늘의와비사비 CtaLink — hover 없는 모바일은 판을 처음부터 채움, md+만 hover). 애드온 텍스트 밀림(썸네일 14→12·가격 whitespace-nowrap·이름 flex-1). 인스타 @@중복(`site.instagram` "@wasa.kr"→"wasa.kr", AtSign 아이콘이 @). About 01/02/03 font-numeric 누락분.
- **🎨 모바일 헬릭스 큰 원(#345, 대표님)**: 모바일 곡선을 데스크톱처럼 크게 원 그리며 하강. 반경 175→230 을 **285→405**(폭 ~57~81%), 타원비 0.52(진폭≈반바퀴y·0.6, 3D 겹침), loops 7→5, vb 5244→3850. **큰 원이라 좌우 포켓 소멸 → 철학 멘트 화면 중앙(렌더 ci===1 분기)**, 곡선이 글자 위 지나 **카드 아닌 크림 후광(radial-gradient 가장자리 투명)**으로 가독성. 데스크톱 무변. **교훈: helix 지오메트리는 스크린샷 반복 튜닝. 멘트 중앙은 left/right 인셋(translateX 불필요, JS transform 무변).**

### 86단계 — 배경음 클릭전용 트리거·추가옵션 사진 (#340~#341, 2026-07-31)
- **배경음 트리거 축소(#340)**: 자동시작 트리거에서 `scroll·touchstart` 제거 → `click`(+keydown)만. 모바일서 스크롤·화면 넘김엔 소리 안 나고 실제 탭에만 시작(힌트 "화면을 클릭하면…"과 일치). 자동재생 정책상 click 은 유효 제스처. (모바일 UX 개선 C 일부. C 나머지=홈 여정 길이는 대표님 시그니처라 보류.)
- **추가 옵션 사진(#341, 대표님)**: 선물 포장·쇼핑백 옵션마다 상세에 **썸네일**(size-14, 없으면 플레이스홀더로 자리 확보). **어드민 업로드**(사이트 콘텐츠 → 추가 옵션 사진, `AddonImageField`). 저장은 About 사진과 동일 패턴 — `site_content` 코드별 키 `addon_image_<code>` 에 스토리지 URL(`saveAddonImage`/`removeAddonImage`, service_role·이전파일 정리·12MB·png/jpg/webp). **가격·이름은 코드(lib/addons.ts) 진실, 사진만 DB → 마이그 불요.** 상세 동적 렌더라 업로드 후 다음 요청 자동 반영. next/image 는 기존 Supabase remotePattern 으로 허용(CSP 무변).
- **👤 인스타 연동 진행중**: 대표님 Meta 앱 셋업 완료(미게시). 남은 것=Instagram User 장기토큰(60일) 발급 → Vercel env `INSTAGRAM_ACCESS_TOKEN` → Redeploy. 코드·remotePattern(`*.cdninstagram.com`·`**.fbcdn.net`) 완비. **색인 점검**: 사이트맵 17URL 전부 200·noindex 없음, 게시판 3종 사이트맵 추가(#337). 색인≠랭킹.

### 85단계 — 모바일 UX(iOS 입력 확대 방지·탭 타깃 44px)·SEO 사이트맵·배경음 교체 (#336~#338, 2026-07-30~31)
- **배경음 교체(#336)**: the_mountain → golden-forest(물소리·산새). **원본 107초 중 앞 0~53초만 반복**(LOOP_END=53, onTimeUpdate 로 currentTime 53s 도달 시 0 되감기) — 파일 재인코딩 없이 재생 구간만 코드 제한(ffmpeg 미설치). 사운드 파일 스왑 커밋(구파일 삭제 의도됨).
- **SEO 사이트맵(#337)**: 색인 전수 점검 결과 사이트맵 17 URL 전부 200·noindex 없음(색인 차단 요소 0). 공개 게시판 **/today·/notice·/review 가 사이트맵 누락**(내부 링크로만 크롤) → 추가. 문의(/inquiry)는 비밀글 위주라 제외. **색인≠랭킹 — 노출은 시간·경쟁(‘와비사비’ 일반어)**.
- **모바일 UX 개선 A(#338, 사용자 관점)**: (1) **iOS Safari 포커스 줌 방지** — raw input/textarea/select 14px(text-sm)면 iOS 포커스 시 화면 자동확대 → `text-base md:text-sm`(모바일16·데스크톱14, 공유 Input 컴포넌트와 동일). 대상: 문의·결제(select·textarea)·리뷰(select·textarea)·댓글·게시글. (2) **탭 타깃 44px**(iOS/Google 권장) — 헤더 아이콘 p-2(36)→p-3(44)·수량±스텝퍼 30→size-11(44)·주소삭제 32→size-11(44). 소셜 버튼은 84단계에 이미 44px.
- **모바일 UX 개선 B(#339, 구매 전환)**: (1) **상세 모바일 스티키 구매 바** — 사진·스펙·리뷰 스크롤해도 하단 고정(가격+담기+바로구매, 수량·애드온 반영). 데스크톱은 인라인 유지(md:hidden), safe-area 홈인디케이터 회피, 상세 pb-28 md:pb-16 로 콘텐츠 안 가림, 배경음 토글 bottom-20 md:bottom-5 로 바 위. (2) **상품 이미지 탭 확대**(`ProductImageZoom`) — 히어로·스캐터 갤러리 탭→풀스크린 라이트박스(바깥탭·ESC·닫기·스크롤락). next/image 프록시라 CSP 무변. **핵심 함정: 오버레이를 `body`로 포털해야 변형/overflow 조상에 `fixed`가 갇히지 않음. headless 스크린샷은 반투명+backdrop-filter 합성 아티팩트로 헤더가 비쳐 보이나 elementFromPoint·불투명 테스트로 실제 전체 덮임 확인 → bg-black 불투명 채택. mounted 가드 불필요(open 초기 false라 SSR서 포털 미실행).** **미착수(제안만): C 홈 여정 길이·배경음 자동재생 트리거(scroll/touchstart).**

### 84단계 — 소셜 로고·홈 로그인링크 버그·숫자 폰트 통일 (#332, 2026-07-29)
- **소셜 로그인 브랜드 로고(대표님 실기기)**: 카카오(검정 말풍선)·구글(4색 G) **인라인 SVG**(self-host, CSP·외부요청 없음). 버튼 `min-h-11`(44px)로 키워 모바일 탭 타깃 확보, 로고+텍스트 중앙 정렬.
- **🔴 홈 CTA 로그인/회원가입 노출 버그**: 로그인 상태(푸터 로그아웃 O)인데 홈 CTA 아래 로그인/회원가입 링크가 계속 뜸. **원인**: 홈(서버 컴포넌트)이 `supabase.auth.getUser()` 로 판정 → 소셜 로그인 직후·클라 라우터 캐시로 서버가 홈 RSC 세션을 놓쳐 상태 불일치(footer 클라 store 와 어긋남). **수정**: 푸터 `LogoutButton` 과 동일하게 클라 스토어(`useAuthStore`)로 실시간 판정하는 `HomeAuthLinks` 컴포넌트로 분리, 홈 서버 `getUser` 호출 제거. **교훈: 로그인 여부로 갈리는 UI 는 서버 판정 시 라우터 캐시로 stale 될 수 있음 → 헤더·푸터처럼 클라 store 로.**
- **숫자 폰트 통일(대표님)**: 가격만 Cormorant 였던 것을 전 고객 화면 숫자로 확대. `font-numeric`(Cormorant→마루부리 폴백, lining+tabular). **핵심: 본문 기본폰트=마루부리라 font-numeric 을 씌워도 한글은 폴백으로 동일 렌더, 숫자만 Cormorant 로 바뀜 → 혼합 텍스트 블록에 안전 적용.** 적용: 날짜(공지·문의·리뷰·오늘의와비사비·주문 상세/목록), 수량(장바구니·상세·주문·checkout ×N), 주문번호·송장번호·연락처·배송지(Row), 상품 개수, 스펙(사이즈·원산지), 애드온 가격(won), 결제 버튼 금액, 헤더 장바구니 뱃지. 가격은 기존 `Price` 로 이미 적용. **admin 표는 tabular 유지 위해 제외(기존 방침).** **2차(#333)**: 실기기 재확인으로 놓쳤던 안내성 숫자 추가 — about 영업시간(12:00–19:00)·매장 주소, map-card 주소, mypage 이메일·닉네임 안내(2~20자)·배송지 전화·주소, footer 사업자정보(사업자등록번호·전화·주소·우편번호)·저작권 연도.

### 83단계 — 반응형 전수 점검 + LCP 이미지 보강 (#331, 2026-07-29)
- **반응형 전수 점검(전 페이지)**: 깔끔한 데스크톱 디자인이 모바일에서도 유지되는지 확인. Playwright 로 **가로 오버플로우 스캔 360/375/414px × 공개 13개 라우트**(홈·shop·상품상세·about·today·notice·review·inquiry·cart·auth·legal 3종) → 전부 `scrollWidth-clientWidth=0`. **실물 스크린샷 9페이지(390px)** 육안 확인 — 텍스트 잘림·요소 겹침·깨짐 없음. 정적 스윕: 어드민 외 고정 px 폭 0, 비반응형 그리드 0(auth 탭만 의도), 이미지 `sizes` 전부 지정, raw `<img>` 0(전부 next/image). **결론: 반응형 이상 없음** — 임시 감사 스펙은 점검 후 삭제.
- **성능 — LCP 이미지 2곳 보강**: `today/[id]` 상세 히어로(모바일 1열 첫 above-fold·단일) → **`preload`**(Next 16 API, priority 대체). `today` 목록(moment-grid) 첫 모바일 행 2장(LCP 후보 여럿) → **`loading=eager`**(문서 권고, shop 그리드와 동일 철학). shop·shop/[id]·about 는 이미 eager/preload 적용돼 무변경.
- **점검 메모(범위 밖)**: 홈 인트로 헬릭스 텍스트는 `opacity:0`→스크롤 JS reveal 이라 Lighthouse LCP render-delay 로 잡히나 **대표님 승인 무드**라 유지(스로틀 과장·필드값 양호). 공개 페이지 `cache-control:no-store` = auth 의존 동적 렌더(정당), 데이터층은 캐시됨.

### 82단계 — 검색엔진 등록·모바일 헤더 정리 (#326~#328, 2026-07-28)
- **SEO 검색엔진 등록(👤+코드)**: 구글 서치콘솔·네이버 서치어드바이저 wasa.kr 등록. 소유확인 meta 를 `metadata.verification`(google #326 / other=naver-site-verification #327)로 emit → 시열님이 콘솔에서 확인 클릭. **사이트맵 `sitemap.xml` 양쪽 제출 완료**(구글 17페이지 성공, 네이버 사이트맵+웹페이지수집). vercel.app→wasa.kr 301(#323)과 함께 검색 색인이 wasa.kr 로 통합되도록. 색인 반영은 며칠~2주(검색엔진 재크롤).
- **모바일 헤더(#328, 대표님/시열님 실기기)**: 로고 `WABI-SABI` 가 우측 아이콘 5개에 눌려 2줄 줄바꿈 → `whitespace-nowrap` + 위시/주문 아이콘을 모바일 상단바에서 제거(`hidden md:inline-flex`)해 공간 확보 → **한 줄**. **위시리스트·주문내역은 모바일에선 햄버거 메뉴 안**으로(참고 시안 TOC GUIDE — 구분선+아이콘+텍스트, 로그인 시). 데스크톱은 상단 아이콘 유지. 메뉴 max-h 64→96.

### 81단계 — 카테고리 통합·스펙 피커·모더레이션·모바일 헬릭스·성능/SEO (#311~#324, 2026-07-28)
- **대표님 2건**: OBJECTS 소분류 **키링·목걸이·팔찌·머리끈 → 액세서리** 통합(#311, 0042 — 해당 잎 상품 0 확인). **소재·사이즈·주의사항 프리셋 드롭다운+직접입력**(#312 — 원산지 패턴 일반화 `AttributePicker`, 프리셋 `lib/product-attributes.ts` 한 파일에서 조정, 저장값 문자열 그대로라 검증·상세 무변).
- **🟡 마무리 작업**: 마이페이지 닉네임 변경 UI 정리("이름"→"닉네임", 2~20자 정책 일치 #313 — 0041 grant 로 이제 동작). 관리자 **오늘의 와비사비 댓글 모더레이션**(숨김/삭제, service_role+감사로그+상세 revalidate #314 — 0039 hidden 준비돼 있던 것 화면화). 공감 토글 **rate limit** 분당 60(#315). 주문 확정 메일 **애드온 분해 표기**(order_items.addons 스냅샷 서브라인 #316).
- **픽스**: 닉네임 안내 가독성(흐린 한줄→라벨+보조설명)·**배경음 힌트 홈에서만**(usePathname, layout 마운트라 전 페이지 갑툭튀 방지 #317). 닉네임 차단 모달 **배경 스크롤 락**(a11y #319).
- **모바일 헬릭스(대표님/시열님 실기기)**: 코일 반지름 좁혀 멘트 포켓 확보+선 뚫는 문제 해결(#320) → 여전히 납작 1차원이라 **3D 복원**(loops·타원비↑ 진폭≈반바퀴이동 겹침 #322) → 너무 촘촘해 **압축감 완화**(loops 10→7·타원비 0.85 #324). 극점 좌표를 지오메트리에서 계산(`momentPositions`) — 캔버스별 반지름 달라도 점·멘트 정확히 얹힘, 데스크톱 무변. **교훈: 3D 코일=타원 진폭≈반바퀴 y이동. 좁은 폭은 loops↔타원비 트레이드오프.**
- **오픈점검(🟡-8) 코드측**: 신규 표면 보안 감사 취약점 0(RLS 본인제약·admin requireAdmin·grant 컬럼한정). **E2E 스모크 확장**(오늘의와비사비·마이페이지 게이트·배경음, 프로덕션 desktop·mobile 5/5 #318). `/security-review`·Lighthouse 는 브라우저/CLI 라 시열님 실행.
- **성능(Lighthouse 모바일)**: Perf **79 = 오직 LCP 5.6s**(TBT 0·CLS 0·FCP 1.6·SI 1.8 최상). **LCP element=홈 철학 본문 `<p>`, subpart=render delay** — 진입 페이드인 연출로 opacity:0 늦게 그려지는 것(폰트·용량 아님, 승인 연출이라 유지). 실기기 render delay 710ms 양호, 5.6s는 4x 스로틀 뻥튀기. **modern browserslist**로 레거시 JS 폴리필 ~14KiB 제거(#321, 안전 win).
- **SEO**: 네이버 검색 진입 시 주소가 vercel 배포 URL로 뜨던 것 → **`wabi-sabi-nu.vercel.app` → `wasa.kr` 301**(#323, 캐노니컬 통합). 👤 후속: 네이버 서치어드바이저·구글 서치콘솔 등록+사이트맵 제출.

### 80단계 — 가격 원 위계·배경음·닉네임 모달·뉴스레터 제거 (#300~#309, 2026-07-28)
- **가격 "원" 위계(#300~#302)**: 숫자(Cormorant)와 "원"(마루부리)이 같은 크기로 부딪혀 지저분 → `Price` 컴포넌트로 분리(숫자 부모크기 상속, "원"만 0.78em·muted·`align-middle`). baseline 광학차로 낮아 보여 `relative top-[-0.09em]` 미세 상향. 고객 화면 가격 전부 교체(카드·상세·장바구니·결제·주문내역·완료), 각 파일 로컬 won()/font-numeric 인라인 정리.
- **잔잔한 배경음(#303·#306·#307·#309)**: `AmbientPlayer`(layout, 어드민 제외) — river 자연음 메인 먼저 → 나머지 3곡 셔플 루프(대기열 ref, river는 인트로만). 브라우저 자동재생 차단 회피: **첫 상호작용(pointerdown·keydown·scroll·touchstart)에 낮은 볼륨(0.18→0.06)으로 시작**, 우하단 스피커 토글, 선호 localStorage(끈 사람 자동시작 안 함), 페이지 전환에도 안 끊김. **클릭 유도 힌트**(#307): 하단중앙 글래스 pill + 이퀄라이저(@keyframes wabi-eq, motion-safe), 0.9s 지연 fade+slide 등장·첫 조작에 fade-out, pointer-events-none(아래 클릭 통과). **강조**(#309): 글로우 브리딩 그림자(wabi-hint-glow)·링·테두리. CSP: 오디오 same-origin → default-src 'self' 폴백 커버.
- **닉네임 설정 모달(#304, 0040)**: 소셜/가입 직후 실명 노출 방지. `profiles.nickname_set`(기본 false, 기존회원 backfill true 면제, 신규만 모달). `NicknameGate`(layout, 저장 전 닫기 불가, /auth 미노출), `setNickname` 서버액션(2~20자, name+nickname_set 갱신). 이후 글·댓글·리뷰 author_name 이 별명 사용.
- **닉네임 저장 42501 픽스(#308, 0041)**: 저장이 `permission denied for table profiles`로 실패. RLS 정책은 있었으나 **authenticated 롤에 테이블 UPDATE GRANT 부재**(지금껏 name 은 트리거 SECURITY DEFINER 로만 채워져 안 드러남). **`grant update (name, nickname_set) on profiles to authenticated`** — ⚠ 컬럼 한정(전체 GRANT 면 사용자가 본인 role→admin 권한상승, RLS 는 행만 제한). 실측: 닉네임 update 200 / role 변조 403 차단.
- **뉴스레터 제거(#305)**: 미사용 폼·구독 액션·어드민 목록·수신거부 페이지·nav 항목 삭제, 탈퇴 안내 문구 정리. newsletter_subscribers 테이블은 dormant 유지(데이터손실·마이그 위험 회피), 탈퇴 시 잔여 정리 로직 존치.
- **부수**: Dependabot #295 minor·patch 묶음 머지(next 16.2.11→12 등). audit 9 high = eslint 툴체인 devDep 전이(brace-expansion ReDoS), 런타임 무관 방치.
- **함정/교훈**: **profiles 클라 update 는 컬럼 한정 GRANT 필수**(권한상승 방지) — 이후 프로필 편집 추가 시 grant 확장. react-hooks/refs·set-state-in-effect — ref 갱신·setState 는 effect 동기 본문 밖(async 콜백)으로. Tailwind 임의값은 canonical 우선(`w-0.75`·`top-[-0.09em]`). 배경음 힌트 필수(첫 클릭 재생을 사용자가 알 수 없음).

### 79단계 — 숫자 폰트·로그인 지속시간 제한·게시판 고도화 (#296~#298, 2026-07-28)
- **숫자 전용 폰트(#296, 대표님 — 1~3순위 중 택1)**: 1순위 **Cormorant Garamond** 채택(이미 로고용 self-host 중이라 신규 import 0, 라틴 세리프 라이닝 숫자가 우아). `globals.css` `.font-numeric` 유틸(Cormorant→마루부리 폴백, `lining-nums tabular-nums`) → 고객 화면 가격 전부 적용(상품카드·상세·장바구니·결제·주문내역·완료). "원" 등 한글은 스택 폴백이 담당(숫자 글리프만 Cormorant). admin 표는 `tabular-nums` 컬럼 정렬 유지 위해 제외. 상세 가격 `text-xl→text-2xl` 강조 병행.
- **로그인 지속시간 제한(#297, 개인정보보호 보안 정책)**: **미활동 30분 + 절대 7일**. Supabase 세션 타임박스·유휴 만료는 Pro 대시보드 기능이라 플랜 무관하게 앱에서 직접 강제 → `SessionTimeout`(client, layout 마운트) `localStorage` 타임스탬프(멀티탭 일관)로 30초마다 점검. 활동 인정=`pointerdown`/`keydown`/`scroll`(마우스 이동만으론 연장 안 함). 만료 시 개인영역(/mypage·/checkout·/admin)이면 `/auth?reason=timeout&kind=idle|max`, 공개페이지면 `router.refresh()`. `visibilitychange` 탭 복귀 즉시 점검(백그라운드 인터벌 스로틀 보완)+중복 만료 가드. auth 페이지 만료 사유 배너.
- **오늘의 와비사비 구조 고도화(#298, 대표님 지시)**: 평면 그리드(전체 일괄 로드) → 구조화 게시판. `0039` 마이그: `moment_likes`(공감 PK 유일)·`moment_comments`(hidden 모더레이션) 각 RLS(공개 read/본인 insert·delete). **더보기 페이지네이션**(첫 12 서버 + `loadMoreMoments(offset)` 이어붙임, 중복 가드). **상세 `/today/[id]`**(큰 사진·전문·공감·댓글, generateMetadata OG). **공감(하트)** `MomentLikeButton` 낙관적 토글 카드·상세 공용, 비로그인 로그인 유도. **댓글** 작성(rate limit 30/시간)·본인 삭제, 카드에 공감/댓글 수. 상세 글 삭제→`redirect=/today`(그리드는 revalidate만). 집계는 세션 클라(RLS)로, UI 엔 총 개수+본인 공감 여부만(누가 눌렀는지 비공개).
- **함정/메모**: react-hooks/refs — ref 는 render 중 갱신 금지(`nav.current=…` 를 별도 effect 로 옮겨 CI 통과). Cormorant 기본 oldstyle 비례숫자라 admin 표엔 부적합(tabular 정렬 깨짐) → 고객 가격만 적용. 후속(별건): 관리자 댓글 모더레이션 UI(hidden 컬럼·RLS 준비됨, 화면 추후).

### 78단계 — 폰트 역할 분리·소셜 로그인 활성·헤더 단축·shop UX (#288~#292, 2026-07-26)
- **소셜 로그인 실연동(👤 대시보드)**: 구글·카카오 OAuth 활성화 완료(대표님 카카오 비즈앱 전환+동의항목 검수, 구글 OAuth 클라이언트). 헤더에 소셜 프로필 아바타 정상 표시. **카카오 프사 깨짐 픽스(#289)**: 카카오 프로필 이미지가 http://*.kakaocdn.net 라 remotePattern https만이면 next/image 거부 → http 프로토콜 remotePattern 추가 + onError 아이콘 폴백.
- **폰트 역할 분리(#290, 대표님 지정)**: 로고=Cormorant / 메뉴=Pretendard / 메인문구=MaruBuri. Pretendard 는 Google Fonts 없고 self-host 시 메뉴 하나에 ~610KB(한글 완성형)라 과함 → 사실상 동일한 Noto Sans KR 로 대체(변수 --font-pretendard). 헤더 로고/내비에만 각 폰트 적용.
- **헤더 단축·로그아웃(#291)**: 로그인 시 장바구니 옆 위시리스트(하트→/mypage/wishlist)·주문내역(영수증→/mypage/orders) 아이콘. 로그아웃 빨간 글씨로, 마이페이지(빨간 아웃라인)+푸터(빨간 링크). LogoutButton 이 auth store 로 로그인 시에만 렌더 → 서버 렌더 푸터에도 안전, variant(button/link).
- **shop UX(#288·#292)**: 사이드바 활성 대분류만 노출(다른 대분류 통째 숨김), "[대분류] 전체" 링크 제거(헤더가 그 역할), details/summary→조건부 렌더. 페이지 타이틀 "Shop"→선택 카테고리명(TABLEWARE/OBJECTS/소분류/이 달의 상품). 상품 카드 가격 강조(text-xs 흐림→15px 진하게).
- **함정**: 소셜 아바타는 호스트·프로토콜(http)이 제각각 → next/image 프록시(CSP 무변경)+remotePattern http/https 둘 다+onError 폴백이 안전. Song Myung 은 붓 느낌 강하나 두껍고 가독성↓ → 마루부리 Light 가 얇고 잘 읽힘(#285, 폰트 여정 Noto Serif→Song Myung→마루부리 Light→역할분리).

### 77단계 — 마루부리 Light 폰트·소셜 프로필 아바타 (#285~#286, 2026-07-26)
- **폰트 3차(최종)**(#285): Song Myung 이 두껍고 가독성↓ → **마루부리 Light**(네이버, OFL — 얇은 붓 부리 명조)로. 예전 홈 전용이던 마루부리를 전 사이트로 승격. **풀 woff2(434KB, 한글 완성형 11172자 전부)를 hangeul.pstatic.net 에서 받아 src/fonts 에 self-host**(서브셋 아님 — 커버리지 갭 없음). 라틴 Cormorant·가나 Noto Serif JP 폴백 유지. 폰트 여정: Noto Serif(밋밋)→Song Myung(두꺼움)→**마루부리 Light**(얇음+붓+가독성).
- **소셜 프로필 아바타**(#286): 대표님 — 소셜 로그인 시 마이페이지 아이콘 대신 소셜 프로필 사진. 헤더가 user_metadata.avatar_url(구글)/picture(카카오) 를 아바타로 렌더(없으면 User 아이콘), 링크는 /mypage 유지. next/image 프록시라 CSP img-src 'self' 유지 + 아바타 CDN(googleusercontent·kakaocdn) remotePatterns. OAuth 코드(signInWithOAuth→콜백)는 기존 완비 — **실제 활성화는 Supabase 대시보드+구글/카카오 콘솔 설정 필요(👤)**.
- 요령: 네이버 마루부리 풀 woff2 는 hangeul.pstatic.net/hangeul_static/webfont/MaruBuri/MaruBuri-Light.woff2 에서 재획득. 외부 아바타는 next/image 프록시로 CSP 무변경.

### 76단계 — CTA 버튼 통일·재고 숨김·붓 명조 폰트·홈 로그인 (#279~#283, 2026-07-26)
- **대표님 피드백 다수**:
  - **CtaLink 공용 버튼**(#279): "오늘의 와비사비" 버튼을 홈 Shop CTA(채워지는 아웃라인 pill, 호버 시 먹색 판 차오름)와 동일하게 — CtaLink 컴포넌트 추출해 홈·shop 사이드바·모바일 공유. 푸터에 로그인·회원가입(auth ?tab=signup 지원).
  - **상품 상세 재고 수량 숨김**(#280): "재고 N개" 제거, 품절만 표시. 수량 클램프는 유지.
  - **폰트 통일 1차 → 2차**(#281→#283): 1차 Noto Serif KR 통일했으나 대표님이 밋밋하다며 **붓 획(필기체) 느낌** 요구 → **2차: 한글 Song Myung(송명조)·라틴 Cormorant·가나 Noto Serif JP 폴백**. 스택 순서로 글자별 담당. 마루부리·Space Mono·Noto Sans KR 전부 제거.
  - **홈 CTA 아래 회색 로그인/회원가입**(#282): 푸터 계정열 제거 → 홈 CTA 밑 저대비 소자(비로그인만, 홈은 searchParams 로 이미 동적이라 무비용). 푸터 사업자 주소 끝에 '와비사비' 추가.
- **핵심 지식**: next/font/google 은 `subsets` 목록에 korean 이 없어도 **CJK 글리프를 unicode-range 청크로 self-host**(빌드 후 @font-face 수로 확인 — Noto Serif KR 498·Song Myung 90·Noto Serif JP 250). Song Myung 은 한글 전용이라 `subsets` 인자 자체를 넣으면 타입 에러 → 생략. CSP·외부요청 없이 웹폰트 통일 가능.

### 75단계 — 2대분류 재편·소개 통합·"오늘의 와비사비" 게시판 (#275~#277, 2026-07-26)
- **대표님 지시 3건**:
  - **소개+오시는 길 통합**(#275): /contact 를 /about 에 합침 — About 하단 방문 안내(SHOWROOM) 섹션(영업시간·위치·인스타·문의+지도). /contact→/about 영구 리다이렉트, contact 페이지·sitemap 제거.
  - **내비 TABLEWARE|OBJECTS|SHOWROOM**(#275): TABLEWARE=식기+다도, OBJECTS=액세서리·리빙·선물, SHOWROOM=소개. **0037 카테고리 2대분류 재편** — 상품은 잎에만 연결(확인)이라 잎 slug·FK 불변, 부모만 교체+빈 대분류(tea·accessory·living) 삭제. site.ts categoryTree·nav 동기, 푸터 안내 열 명시화.
  - **"오늘의 와비사비" 커뮤니티 게시판**(#277, 자유게시판): 사용자가 일상 속 그릇 사용을 사진+글로 공유. **0038 wabi_moments**(RLS 읽기=hidden아닌것 공개/작성·삭제=본인, 관리자 숨김·삭제 service_role). /today(목록+로그인 게이트 작성폼, 사진 필수·글 선택, 서버 업로드, user당 시간 10건 rate limit) · /admin/moments(모더레이션) · shop 사이드바/모바일 칩/푸터에 **accent 강조 버튼**.
- 부수: admin 사이드바 뒤로가기 / → /shop(#276, 시열님).
- 요령: 카테고리 부모 재편은 상품이 잎에만 붙었는지 먼저 확인(node 스크립트) 후 안전. 사용자 사진 업로드는 서버 액션(service_role)으로 처리하면 사용자 스토리지 RLS 불요.

### 74단계 — 상품 상세 이미지 진짜 불규칙 스캐터 (#273, 2026-07-26)
- **대표님 피드백**: #271 갤러리는 중앙 열 맞춤이라 정렬돼 보임. 중앙정렬 빼고 레퍼런스처럼 불규칙하게만. 스크롤 내리면 첫 사진+정보는 위로 사라지고 사진만 쭉 나열 — 정보를 우측 sticky 로 두지 말 것.
- **히어로 분리**: 첫(메인) 사진 + 정보 2열, **정보 sticky 제거** → 스크롤 시 첫 사진과 함께 사라짐. `main` 사진은 페이지에 직접 렌더(Image fill preload).
- **스캐터**: ProductGallery 를 나머지 사진(`images.slice(1)`) 전용으로 재작성 — 전체 폭에 폭(md:w-[44~64%])·좌측 오프셋(md:ml-[0~50%])·비율·세로 간격 층마다 달리, 중앙 정렬 없음(합 100% 이내). 모바일은 전체 폭 세로 나열, 흩뿌림은 md+.
- 요령: 스캐터용 arbitrary Tailwind(md:w-[…]·md:ml-[…])는 배열에 **완전한 리터럴 문자열**로 둬야 JIT 가 생성(빌드 후 CSS 에 width/margin-left % 확인).

### 73단계 — 내비 한글화·푸터 사이트맵·불규칙 갤러리·인스타 안내 (#271, 2026-07-26)
- **대표님 피드백 3건 + 질문 1**:
  - **내비 한글화**(직관성): Shop→상품·About→소개·Contact→오시는 길·Admin→관리자. 경로·slug·metadata title 반영. site.ts nav + site-header Admin 라벨.
  - **푸터 전면 개편**(tableofcraft 참고): 좌측에 모든 분류 페이지 한눈에(상품=전체·이 달·대분류들 / 안내=소개·오시는 길·공지·문의·리뷰 / 약관), 우측 끝에 브랜드+사업자정보. `SiteFooter` async 로 전환, 카테고리 `getCategoryTree`(DB).
  - **상품 상세 이미지 불규칙 배치**: 폭(w-[68%~full])·좌우 정렬(self-start/center/end)·비율·간격을 층마다 교차 — 흩어진 느낌이되 가운데 열 안에서만 움직여 중앙 유지. 인덱스 기반 결정적.
  - **인스타 실연동 질문**: 코드 완비(lib/instagram.ts `graph.instagram.com/me/media`) — `INSTAGRAM_ACCESS_TOKEN`(Vercel Production)만 넣으면 됨. **2024.12 Basic Display 종료로 프로페셔널 계정 필수**, Meta 앱>Instagram API 장기토큰(60일 만료). 개발 작업 0, 대표님 계정 설정 필요.
- **함정**: 푸터가 async(DB) 되면서 정적 페이지(/cart·/_not-found) 프리렌더가 env 없이 Supabase 클라 생성→"supabaseUrl is required" 크래시. `fetchRows` 에 env 가드+try/catch → 코드 트리 폴백으로 해결.
- **Next 16 이미지**: `priority` deprecated → **`preload`**(v16). #269 에서 preload→priority 로 바꾼 게 회귀였음 — 되돌림(CodeRabbit).

### 72단계 — 버튼 로딩 피드백 전수·상품 이미지 화질 진단 (#269, 2026-07-26)
- **대표님 피드백**: 버튼 누른 뒤 대기 중 피드백(업로드 중 등)이 좋다 → 피드백 없는 버튼 전수 점검·추가.
- **`SubmitButton`(useFormStatus)** 신규 — useActionState 없이 서버 액션 직결 폼에도 붙이면 제출 중 pendingText 로 바뀌고 비활성화. `styled` 플래그로 인라인 텍스트 버튼(저장/삭제 링크)·주요 버튼(buttonVariants) 겸용. **핵심: bare `<form action={서버액션}>` 는 자체 pending 이 없어 이걸로 감싸면 로딩 표시가 생긴다.**
- 적용(피드백 없던 것): 어드민 상품(재고저장·지정·노출·이미지×·삭제)·카테고리 노출토글·주문(송장저장·배송완료)·공지(등록·삭제)·리뷰(숨김·삭제)·문의(답변·삭제)·마이페이지(이름·주소 추가/삭제)·About 사진 제거 / 고객 문의 등록·리뷰 등록/삭제. 상품 상세 담기 "담겼습니다 ✓" 1.5초. restock pending "처리 중…".
- 이미 피드백 있던 것 유지(결제·로그인·재입고·뉴스레터·탈퇴·주문취소·상품 등록/수정·콘텐츠·카테고리 편집/추가).
- **product-gallery 버그**: 잘못된 prop `preload` → `priority`(next/image).
- **상품 이미지 화질 저하 진단(대표님 질문)**: 코드 아님 — **업로드 원본 저해상도**(실측 360×540·40KB, 724×1086). 상세는 정사각 ~700px+ 로 크게 표시해 확대되어 깨짐. About 매장사진(4416px)은 선명. → **고해상도 재업로드 필요**(👤 운영, 긴 변 1500px+ 권장). next.config 이미지 설정엔 문제 없음.

### 71단계 — 원산지 드롭다운·위시리스트 담기 제거·About 매장 사진 (#265~#267, 2026-07-25~26)
- **대표님 실검수 피드백**:
  - **#265 원산지 드롭다운 + 위시리스트 담기 제거**: 원산지를 "Made in ○○" 통일(등록·수정 폼 select). 위시리스트 카드 담기 버튼 제거(#252 연장 — 옵션 선택 위해 상세로 유도) → `AddToCartButton` 완전 미사용이라 컴포넌트 삭제(죽은 코드).
  - **#266 원산지 한·일·중만 + 직접 입력**: 나머지 나라 불필요 → `ORIGINS` 를 대한민국·일본·중국으로 축소 + "직접 입력…" 옵션. 공용 `OriginPicker`(드롭다운↔텍스트 전환, name="origin" 하나만 렌더). CodeRabbit Major: 모드 전환 시 언마운트 컨트롤 포커스 유실 → userSwitched ref+useEffect 로 새 컨트롤 포커스 이동(최초 마운트 제외).
  - **#267 About 매장 사진**: 소개 문단 옆 이미지 자리에 매장 사진 어드민 업로드. **채팅 붙임 이미지는 바이트 접근 불가 + 90° 회전이라 어드민 업로드 방식이 정답**(웹편집 방향 일치). `site_content(about_image)` 에 URL 저장(마이그 불필요), 스토리지 product-images 버킷 `site/` 하위 재사용(`uploadSiteImage`). 이전 사진 저장 성공 후 정리. About 은 저장 사진 정사각 object-cover, 없으면 로고 폴백. png·jpg·webp·12MB.
- **함정**: 붙여넣은 채팅 이미지는 파일 바이트로 접근 불가 → 레포 직접 저장 못 함. 이미지 자산은 어드민 업로드(스토리지+site_content URL) 패턴으로.

### 70단계 — 원산지·상품 수정·카테고리 관리 (#260~#263, 2026-07-25)
- **대표님 지시 3건**:
  - **#260 원산지**(0035 `products.origin`): 등록 폼 입력 + 상세 스펙 "원산지" 행(소재 다음, 비우면 생략). 상품정보고시 항목.
  - **#261+#262 상품 본문 수정**: `/admin/products/[id]` + `updateProduct`(zod·감사로그·캐시 무효화). 범위 = 이름·가격·카테고리·이달상품·설명·소재·사이즈·주의·원산지. **재고·이미지는 의도적 제외**(재고는 목록 updateStock — 재입고 알림 판정 경로 #166, 이미지는 목록 추가/삭제로 일원화). 등록 폼에 소재·사이즈·주의 입력 보충(상세가 표시하던 컬럼인데 입력 경로가 없었음). CodeRabbit Major 2건 후속(#262): 현재 카테고리가 잎 선택지에 없으면 select 폴백으로 조용히 초기화 → 현재 값 선택지 추가 / update 대상 row 부재를 성공 오인 → select().maybeSingle() 로 검증.
  - **#263 카테고리 추가·숨김·삭제**(0036 `categories.is_active`): **트리 구조 진실을 코드→DB 승격** — getCategoryTree 가 DB(parent_id·sort_order·is_active)로 구성, 코드 categoryTree 는 폴백. 필터 확장 site.categorySlugs → queries/categories.getCategorySlugs(DB 판). **"제외"=숨김 기본**(내비만 빠지고 상품 연결 보존), 삭제는 빈 분류만(하위·상품 0 — 렌더 가드+서버 재검증). 추가는 slug 영문 자동 생성(충돌 -n)·3계층 금지·sort_order 형제 뒤. 상품 폼 선택지에서 숨김 제외.
- 0035·0036 원격 적용 완료. 마이그레이션 0036.

### 69단계 — 홈 헬릭스 미세조정·About 히어로 제거 (#254~#258, 2026-07-25)
- **대표님 홈 피드백 4건** (실캡처 검수 기반):
  - **#254 리드인 단축**: 곡선이 첫 글씨(侘) 직전에 시작하도록 위쪽 빈 코일 2바퀴 제거(10→8바퀴, 리드인 5반→1반바퀴). 侘 27.6%→10.4%.
  - **#255 등간격(캔버스 대칭)**: 상하 여백 비대칭(위150/아래40)으로 트리오가 위로 쏠림 → yStart=vbH−yEnd 로 중앙 대칭, 寂 정확히 50%. **단, 간격 크기는 그대로라 체감 무변화** — 실캡처 재검수에서 확인.
  - **#257 멘트 간 스크롤 간격 축소(진짜 해결)**: 멘트 간격 7반→**5반바퀴**(홀수 유지=좌우 교차), 8→**6바퀴**. 멘트 간 스크롤 ~1414px→~1010px(-29%). 피치(코일 밀도)·중앙 대칭 유지.
  - **#258 가운데 수직 점선(시간 축) 복원**: #197 원안 그대로(x=500, dash "1 6", opacity 0.55, 상단 화살촉) — 현 기하에 맞춰 축 범위 재산정. 정적 요소(스크럽 비대상).
- **#256 About 히어로 제거**: "불완전함의 미학" 헤드라인 블록 삭제 — わび-さび 철학 본문으로 바로 진입. 헤딩 h2→h1 승격(SEO·접근성).
- **요령**: 극점 재계산 시 MOMENT_POS 는 스크립트 검산(데스크톱=모바일=목표값 일치 확인 후 커밋). 멘트 간격은 홀수 반바퀴만 가능(좌우 교차). 다음 축소 단계는 3반바퀴(-57%).

### 68단계 — 애드온 라인 단위 전환 (#253, 2026-07-25)
- **대표님 시안 충실**: 애드온을 주문 단위(#250)에서 **상품 상세에서 선택 → 장바구니 라인마다** 로. 시열님 방식 확정: **라인당 옵션 1세트**(같은 상품은 한 라인, 재담기 시 최신 옵션으로 덮어씀 — cart_items UNIQUE(user,product) 유지) + **선물 메시지는 주문당 1개**.
- 0034: `cart_items.addons`·`order_items.addons` jsonb. `orders.selected_addons`(#250)는 레거시로 남김(새 주문 미사용).
- 흐름: `store/cart`(CartItem.addons, add(item,qty,addons)) · `cart-sync`(upsert/load/merge 애드온 동기화) · 상세 `ProductDetailActions`(애드온 체크박스 + 담기) · `cart`(라인별 애드온·합계) · `checkout`(주문단위 체크박스 제거, 라인별 합산, 선물 포장 라인 있으면 메시지) · 주문 상세(order_items 라인별 애드온).
- **보안(결제 금액)**: 애드온 금액은 서버 `addons.ts` 정가로 라인마다 재계산 → subtotal 합산 → total_price. `confirm_order_paid` 가 total_price 검증하므로 클라 변조 무해. 라인당 1회 부과(수량 무관).
- 후속: 상품 상세 이미지 사이 애드온 시각 배치(시안) · 확정 메일 애드온 분해 · 가격 대표님 확정.

### 67단계 — shop 카드 담기 제거(상세에서만 담기) (#252, 2026-07-25)
- **대표님 시안 흐름**: 옵션(애드온)을 상품 상세에서 선택 후 담게 하려면 목록에서 바로 담기가 아니라 상세로 유도해야 한다 → **shop 목록 카드 아래 담기 버튼 제거**(추천 섹션 포함). 카드 클릭은 상세로만.
- E2E 스모크는 상세 페이지 담기를 검증(카드 담기 아님)이라 무영향. AddToCartButton 은 위시리스트에서 계속 쓰여 컴포넌트 유지.
- **다음(2단계)**: 애드온을 상세로 이동(라인 단위) — cart_items(unique user·product)·cart store·checkout·order_items 를 라인별 애드온으로 재작업. 시안 충실하나 장바구니·결제 대수술이라 별도로 신중히.

### 66단계 — 주문 추가 옵션(애드온) (#250, 2026-07-25)
- **대표님 시안(tableofcraft)**: 선물 포장·쇼핑백 같은 **추가금 애드온**(전역 공통·주문 단위, variant/재고 아님 — 시열님 방향 확정).
- **기존 선물 포장 인프라 확장**: 이미 있던 주문 단위 유료 옵션(GIFT_PRICE 3000·gift_options·서버 금액 재계산·confirm_order_paid 검증)을 다중 애드온으로. `gift_options.order_id` 가 UNIQUE(주문당 1행)라 여러 애드온을 못 담아 → **0033 `orders.selected_addons` jsonb 스냅샷**(code·name·price, 주문 시점 고정).
- `lib/addons.ts`: 전역 애드온 상수(선물 포장 4,000·쇼핑백 700 — **가격은 대표님 확정 대기, 이 표만 수정**) + `addonsTotal`/`addonSnapshot`(서버 재계산·불신). checkout: 다중 체크박스, 선물 포장 선택 시 메시지. createPendingOrder: 서버 정가 합산 → total_price, 선물 포장은 gift_options(메시지) 유지. 주문 상세에 애드온 목록.
- **보안**: 애드온 가격은 서버 addons.ts 정가로만 재계산 — 클라이언트가 준 값 불신(기존 결제 금액 재계산 원칙 그대로). confirm_order_paid 가 total_price 검증하므로 변조 무해.
- 후속: 주문 확정 메일에 애드온 항목 분해(현재 total 엔 포함, 항목 표시만 없음) · **가격 대표님 확정**.

### 65단계 — 어드민 저장 피드백 (#249, 2026-07-25)
- **시열님 피드백**: 카테고리·콘텐츠 저장 버튼이 눌러도 화면 무변화(서버 액션 void)라 저장됐는지 알 수 없었다. → **pending("저장 중…") + 성공/실패 메시지**.
- `saveContent`·`updateCategoryName` 을 `ActionResult` 반환으로(upsert/update 에러도 메시지로), 폼을 클라이언트 컴포넌트(`content-field`·`category-row`, `useActionState`)로 분리. 성공 시 "✓ 저장되었습니다", 실패 시 사유 표시.

### 64단계 — 상품 상세 세로 스크롤 갤러리 (#248, 2026-07-25)
- **대표님 시안(tableofcraft)**: 상세 이미지를 썸네일 클릭 전환(4장 제한) → **세로로 죽 나열해 스크롤**로 보기.
- `product-gallery`: 클라이언트 상태 제거(서버 컴포넌트), 전 이미지 세로 스택. 첫 장만 eager(LCP), 나머지 lazy. 갯수 제한 해제.
- shop/[id]: `items-start` + 정보 컬럼 `md:sticky md:top-8` — 이미지가 길어도 가격·담기 버튼이 늘 보인다.
- **다음(대표님 시안 2)**: 상품 추가 옵션(선물 포장·쇼핑백 등 **추가금 애드온** — variant/재고 아님). 스키마 + 상세 선택 UI + 장바구니·주문 저장 + **서버 금액 재계산**(클라 변조 방지)이 한 세트.

### 63단계 — 홈 철학 제목(라벨) 편집 (#247, 2026-07-25)
- **대표님 추가 요청**: 홈 철학 멘트의 **제목(01 — 와비 / WABI 등)** 도 편집 가능하게. 3-A(#245, 본문·CTA) 확장.
- 한자(侘·寂·選)만 브랜드 상징으로 코드 고정(`PILLAR_HANJA`), **제목·본문 모두 site_content 편집**. `HOME_PILLAR_LABEL_KEYS` 3키 + `DEFAULT_PILLAR_LABELS` 폴백. `getHomeData` 가 `pillarLabels` 파생, `HelixJourney` 가 라벨·본문 props 로 받음.
- admin/content: 철학 3주를 한자별 카드로 묶어 제목·본문 2필드 편집.

### 62단계 — 대표님 지시 3-B: 카테고리 이름 웹 편집 (#246, 2026-07-25)
- **범위 = 이름만 편집**(시열님 선택): slug·구조(트리)·순서는 코드가 소유(불변 — slug 는 URL·상품 `category_id` 에 묶임), **이름(한글·영문)만 DB `categories` 에서 편집**. 추가/삭제/완전 DB화는 오픈 임박 회귀 위험이라 제외.
- `lib/queries/categories.ts getCategoryTree()`: 구조는 `categoryTree`(코드), 이름은 DB 오버라이드(slug 매칭). **캐시 없이 직접 조회** — 16행 소테이블·shop 은 이미 동적이라 부하 미미, 편집 즉시 반영.
- shop 프론트 전환: `shop/page`(칩·소분류)·`shop-sidebar` 가 `categoryTree` 직접 참조 → `tree` props. `categorySlugs`(필터)는 slug 만 쓰므로 무변경.
- `admin/categories`(신설): 대분류·소분류 트리로 이름 편집 폼(slug 읽기전용 표시) + `updateCategoryName`(service_role, slug 로 where·이름만 update, 감사로그). 어드민 nav '운영'에 추가.
- **후속(대표님 추가 요청)**: 홈 철학 멘트 **제목(label)** 도 편집 가능하게 — 3-A 확장으로 별도.

### 61단계 — 대표님 지시 3-A: 홈 문구 웹 편집 (#245, 2026-07-25)
- **웹에서 홈 텍스트 직접 편집**(대표님 지시 3의 Part 1 — 홈). 하드코딩이던 홈 철학 멘트 3주 본문(侘·寂·選)·하단 CTA 문구를 `site_content` 로 이전, 어드민 `/admin/content` 에서 편집.
- **한자·라벨은 코드 고정, 본문만 편집**: `helix-journey` 는 `PILLAR_META`(han/label 고정) + `pillarBodies` props. page 가 `getHomeData` 로 조회해 주입. 미저장이면 `DEFAULT_PILLARS`·`DEFAULT_HOME_CTA` 폴백.
- `content.ts`: `CONTENT_KEYS` 단일 출처(액션 enum·타입), `getPublicContent(keys)`(쿠키리스 anon, 캐시 로더용 다중 키 조회). `getHomeData` 가 상품 쿼리 없이 편집 문구만 읽어 캐시.
- CTA 화살표 슬라이드 연출은 편집 문구(→ 텍스트 포함)와 양립 안 돼 제거 — 주 연출(배경 차오름)은 유지.
- **다음(3-B)**: 카테고리 편집 UI — categories DB ↔ categoryTree 하드코딩 이중 진실 해소가 선행.

### 60단계 — 대표님 지시: 상품 설명칸·표시정보 정리 (2026-07-25)
- **상품 등록 설명칸(#243)**: `products.description` 컬럼은 0001 부터 있고 상세는 렌더했지만 **등록 폼에 입력 필드가 없어** 항상 비어 있었다(대표님: "등록할 때 설명 써야 함"). ProductCreateForm 에 textarea(최대 2000자) + `productSchema.description`(nullable) + insert 배선. 기존 상품 설명 수정 UI 는 후속.
- **스마트스토어 제거 + 이메일 교체(#244)**: 대표님 — 홈페이지에서 스마트스토어를 따로 연결 안 해도 됨 → `naverStoreUrl`·푸터 링크·Contact Item·JSON-LD sameAs 전면 제거. 연락 이메일 `info@wasa.kr` → `0_wabisabi@naver.com`(표시·문의 안내용, 발송 EMAIL_FROM 은 env 라 무관). E2E contact 스모크의 스마트스토어 검증을 인스타그램 헤딩으로 교체.
- **대표님 지시 3(웹에서 홈 문구·분류 직접 편집)**: 범위가 커 별도 설계 — 홈 하드코딩 문구(철학 멘트·CTA·About)를 site_content 로 이전 + admin/content 확장, 카테고리 편집 UI(categories DB ↔ categoryTree 이중 진실 해소)가 한 세트. 착수 전 범위 합의 예정.

### 59단계 — 검색 빈 결과 추천 (#242, 2026-07-25)
- **갭 분석 4번**: 검색 결과가 0이면 "검색 결과가 없습니다"로 끝나 이탈했다. → **최신 상품 4개 추천 + "전체 상품 보기" 링크**.
- 추천은 검색 실패 시에만 `getShopBrowse({sort:"newest"})`(캐시된 탐색 쿼리 재사용) 실행 — 정상 경로 부하 없음. 카테고리 빈결과("준비 중")는 추천 없이 그대로.

### 58단계 — 상품 상세 교환·반품 안내 (#241, 2026-07-25)
- **갭 분석 3번**: 구매 결정 순간(상세)에 교환·반품 정보가 없어 `/legal/refund` 까지 가야 했다. → 상세 스펙 아래 **접이식 "교환·반품 안내"** 블록(details/summary 무JS).
- **확정된 것만 노출**(허위표시 방지): 배송비 개념이 코드에 아예 없어(상품가만 결제) 금액은 제외. 수록 = ① **수공예 개체차는 하자 아님**(도자기라 클레임 예방에 핵심) ② 수령 후 7일 청약철회 ③ 단순변심 고객 부담·판매자 귀책 무료. + legal 자세히 링크.
- 배송비·기간이 대표님 확정되면 "배송 안내" 블록 추가 예정.

### 57단계 — 배송 추적 링크 (#240, 2026-07-25)
- **신규 사용자 갭 분석 1번**: 주문 상세의 송장번호가 텍스트로만 있어(#137) 고객이 번호를 복사해 직접 택배사를 찾아가야 했다. → **통합 배송조회 링크** 추가.
- **택배사 컬럼 없이 구현**: 어드민은 `tracking_number` 만 저장한다(스키마 최소). `lib/orders.ts trackingSearchUrl` — 네이버 통합검색에 송장번호를 넘기면 택배사를 자동 감지해 조회 위젯을 띄운다. **마이그레이션·어드민 입력 UI 변경 불필요.** 외부 링크(새 탭)라 CSP frame/connect 무관.
- 배치: 주문상세 송장번호 옆 "배송조회 →" 링크 + `shipping` 상태면 눈에 띄는 "배송 조회하기" CTA 버튼. 어드민 주문 목록에도 저장된 송장 옆 "조회" 링크(운영 확인용). 모두 `sr-only` 새 창 안내.
- 갭 분석 나머지(상세 배송/반품 요약·FAQ·검색 빈결과 추천·게스트 체크아웃 정책)는 백로그.

### 56단계 — 어드민 매출 차트 애니메이션 (#239, 2026-07-25)
- **시열님 제안**: CSS 막대 대시보드 매출을 recharts(shadcn 차트 기반) 애니메이션 차트로. `components/admin/revenue-chart.tsx`(클라이언트) — 그라데이션 채움 AreaChart, 진입 시 그려지듯 자라나는 애니메이션(900ms ease-out), 크림 톤(먹색 선/채움·옅은 그리드·크림 카드 툴팁).
- **번들 격리**: recharts(~클라 전용)는 이 차트 컴포넌트에서만 import → **어드민 대시보드 청크에만** 코드 스플리팅. 고객 페이지(`/shop` 등) First Load 무영향(grep 으로 import 경로 revenue-chart 단일 확인).
- CSP 무변경 — `script-src`·`style-src` 가 이미 `'self' 'unsafe-inline'` 이라 번들 recharts·인라인 스타일 통과. 취약점 0.

### 55단계 — 어드민 UI 고도화 (#238, 2026-07-25)
- **시열님 피드백** "어드민 UI 너무 별로" → 좌측 사이드바 셸 + 크림 브랜드 톤 유지(고객 화면과 동일 팔레트).
- **셸(사이드바)**: `components/admin/{nav.ts, sidebar.tsx}` — 메뉴를 운영/고객/운영도구 3그룹으로. 데스크톱은 좌측 고정, 모바일은 상단바+슬라이드 오버레이. active 는 `usePathname` 으로 먹색 배경 강조(`/admin` 은 정확 일치, 나머지는 하위 경로까지 startsWith). 상단 텍스트 링크 9개 나열을 대체.
- **공통 프리미티브** `components/admin/ui.tsx`: `PageHeader`·`SectionHeading`·`Panel`·`StatTile`·`TablePanel`·`EmptyState` — 페이지마다 제각각이던 `<h2 text-lg>`+생 border 를 통일. 크림 배경 위 라운딩(rounded-xl)+옅은 그림자.
- **어드민에서 사이트 헤더/푸터 숨김**: 자체 셸을 쓰므로 중복 제거. 헤더는 자체 게이트에 `/admin` 추가, 푸터는 `HideOnAdmin`(클라이언트 게이트)로 감쌈 — 푸터 자체는 서버 렌더 유지. **비어드민 페이지엔 영향 없음**(로컬 스모크로 shop 헤더·푸터 잔존 확인).
- **대시보드**: StatTile(아이콘+tone) 8종, 7일 매출 막대 라운딩, 재고/최근주문 Panel. `LinkGrid` 는 사이드바로 대체돼 제거.
- **일관화**: 주문 목록이 `statusLabel` 생 텍스트를 쓰던 것 → `OrderStatusBadge`(대시보드와 동일). 감사·에러·뉴스레터 로그 시각 **KST 고정**(`timeZone: Asia/Seoul` — 서버 UTC 라 어긋났던 표시 교정). 상품·공지·문의·리뷰·콘텐츠 폼 요소 라운딩·포커스 스타일 통일.
- 순수 UI — RPC 집계·RLS·서버 액션 로직은 무변경.

### 54단계 — Next 보안 패치 · 브랜치 보호 · CTA 버튼 (#231~#233, 2026-07-25)
- **Next.js 16.2.10 → 16.2.11 보안 패치(#233)**: `npm audit` high 3건 검출(라이브 결제 사이트라 우선 처리). Next 자체 취약점 9건 해소 — 그중 **Middleware/Proxy bypass in App Router**(`proxy.ts` 로 세션 갱신 중이라 직접 해당)·**인증 없이 내부 Server Function 엔드포인트 노출**·SSRF(Server Actions·rewrites)·**캐시 혼동**(공개 데이터 캐시 사용 중)·Image Optimization DoS 가 우리 구조와 직결.
- **하위 의존성은 overrides 로 상향**: `npm audit` 이 제안하는 유일한 해법이 "next@9.3.3 다운그레이드"였다(Next 가 번들한 버전이라 그렇다). `postcss` 8.4.31→8.5.22, `sharp` 0.34.5→0.35.3(libvips CVE 4건). sharp 는 이미지 최적화 런타임 경로라 실동작 검증 — `sharp 0.35.3/libvips 8.18.3` 로드 + `/_next/image` 200. dev 체인(brace-expansion·js-yaml)은 `npm audit fix`. **결과 prod·dev 전부 0건.**
- ⚠️ **Dependabot 보안 알림이 꺼져 있었다** — 정기 업데이트 PR 만 오고 보안 PR 은 안 오는 상태라 이 취약점들이 6일간 방치됐다. 발견 경로는 수동 `npm audit`. (👤 Settings 에서 활성화 필요)
- **main 브랜치 보호 룰셋 "와비사비 규칙"**(시열님 설정, active): PR 필수·상태 검사 `verify`·force push 차단·삭제 차단, 우회는 Repository admin. 첫 효과가 바로 나왔다 — CTA PR(#232)의 verify 가 **audit 3건 때문에 FAILURE** 로 막혔고, 보안 패치 머지 후 rebase 하니 통과. 취약한 의존성인 채로는 main 에 못 들어간다.
  - 주의: 상태 검사 규칙은 PR 뿐 아니라 **브랜치 업데이트 전체**에 걸린다 → main 직접 푸시가 막힌다(문서 커밋도 PR 로). admin 우회로 열어둘 수 있다.
- **홈 Shop CTA 버튼(#231~#232)**: 밑줄+모노 텍스트 → **채워지는 아웃라인 필**. 호버/포커스 시 먹색 판이 아래에서 차오르며 화살표가 오른쪽으로. **색 전환을 따로 주면 판이 절반 올라온 순간 크림 배경 위 크림 글씨가 되어 문구가 사라진다** → 반전된 문구를 판 위에 얹어 함께 슬라이드(마스킹). 판 레이어 `aria-hidden`(같은 문장 2회 낭독 방지), 모바일은 두 줄 가능성 때문에 `rounded-3xl`·데스크톱만 `rounded-full`.
- **머지 위임**(2026-07-25): 1인 개발이라 PR 머지는 Claude 가 승인 없이 진행(CI 통과 전제). 실결제·대량 메일·DB 파괴 작업은 여전히 사전 확인.

### 53단계 — 헬릭스 카드 → 브랜드 철학 멘트 (#225~#226, 2026-07-19)
- **대표님 피드백**: 곡선 따라 등장/소멸하던 상품 카드 대신 **철학 멘트 3주(侘 와비·寂 사비·選 큐레이션)** 로 교체. 엔딩 섹션의 pillars 그리드는 곡선 여정 안으로 이동(같은 멘트 중복 금지 — 엔딩은 로고+소개문+CTA 만 남음).
- **슬롯 6→3**: 극점 k=5(좌 27.6%)·12(우 60.9%)·19(좌 94.2%) — 간격 7반바퀴(홀수=좌우 교차 유지). 첫 1/3 곡선만 빌드업·종점 94.2% 는 카드 시절 확정 위치 그대로. 슬롯 스타일은 엔딩 그리드와 동일(한자 대형 + 모노 라벨 + 세리프 문장). 등장/소멸 연출(뷰포트 중앙 종형 vis·포켓 배치·inert)·진입 드로잉·스무스 스크롤 변경 없음.
- **홈 DB 의존 경량화**: journey 상품 파생(products 12행 쿼리·firstSentence·firstImage) 소비처 소멸로 제거 — `getHomeData` 는 소개문구 1쿼리만. 상품 변경이 홈 캐시와 무관해짐. 홈 LCP 이미지도 사라져 priority/eager 로직 자체가 불필요해짐.
- 상품 카드 복원이 필요하면 git 히스토리(#213~#224 시점 helix-journey)에서 회수 — MOMENT_POS 6슬롯·JourneyMoment 타입·home.ts journey 파생이 한 세트다.
- **About 역할 분리(#227~#228)**: Our Values(영문 3종 — 홈이 흡수한 철학의 변주) → **"고르는 기준"**(손의 흔적·쓰임·시간)으로 교체. 홈=철학을 느끼는 곳, About=셀렉션 기준을 설명하는 곳. 카피는 대표님 검토 전제 초안. 공방·작가 소개는 대표님 자료 수급 후 별도 이슈.

### 52단계 — E2E 스모크 도입 + 모바일 줌·홈 길이 (#221~#224, 2026-07-19)
- **E2E 스모크(#222)**: Playwright(크로미움, 데스크톱+모바일 Pixel 7) 5시나리오 — 홈 헬릭스 렌더·shop 목록/검색·**상세→담기→장바구니 배지→주문하기 auth 게이트**·푸터 전자상거래법 표시·contact 채널 링크. 대상은 기본 **프로덕션 wasa.kr**(프리뷰는 SSO) — CI 는 main push 후 90s 대기 뒤 스모크. 로컬 `E2E_BASE_URL` 로 변경 가능. 실측 10/10(5.8s). 결제 위젯 이후는 실결제 위험이라 범위 밖. 리뷰 반영: 워크플로 최소 권한·담기 버튼 하이드레이션 5s 대기(오탐 방지).
- **모바일 줌아웃 픽스(#224)**: ① 핀치 줌아웃 시 문서 밖이 흰색(html 투명·body shadcn white, 크림은 페이지 래퍼만) → **루트(html/body)에 크림**. ② **viewport minimum-scale=1** 로 축소 자체 차단(좁은 컬럼 현상) — 확대는 허용해 WCAG 1.4.4 유지. ③ 홈 캔버스 15% 단축(4800→4100·10000→8500, 바퀴 유지 피치 촘촘, 시열님 "길다" 피드백).
- 토스 라이브 키 전환은 시열님 진행 중 — env 명: NEXT_PUBLIC_TOSS_CLIENT_KEY(live_ck)·TOSS_SECRET_KEY(live_sk), 웹훅 https://wasa.kr/api/webhooks/toss, Production 환경만 교체.

### 51단계 — 개통 후속·성능 검사 (#215~#220, 2026-07-19)
- **개통 후속**: 네이버지도 콘솔에 wasa.kr 등록(지도 복귀 — 폴백 설계가 안전하게 동작함 확인) · Resend Verified + `noreply@wasa.kr` 실발송 200(메일 개통, 발송전용 키라 API 목록 조회 불가 → 실발송으로 검증) · 결제 후 장바구니 부활 레이스 픽스(#216 — ClearCart 가 auth 바인딩 전에 돌아 서버 cart_items 잔존 → 확정 직후 서버에서 직접 삭제) · 새로고침 스크롤 리셋(#218 — scrollRestoration 은 히스토리 엔트리 단위라 원복 클린업 불요).
- **성능 검사(로컬 Lighthouse — PSI 무키 쿼터 429)**: 모바일 68점, TBT 0·CLS 0(연출이 레이아웃·메인스레드 무부하 입증). 발견 2건:
  1. **🔥 함수 리전 미국(iad1)**: `x-vercel-id: icn1::iad1` — 엣지 서울, 함수 미국. 동적 TTFB 350~700ms 주범. Supabase 는 아시아(웜 14~51ms) → `vercel.json regions:["icn1"]`(#220).
  2. **LCP 7.2s**: 첫 화면이 곡선뿐이라 LCP 가 숨은 첫 카드 이미지에 귀속, lazy 라 Load Delay 3.7s → 첫 카드 priority·둘째 eager(#220).
  3. **www 리다이렉트 +760ms**: Vercel 도메인 Primary 가 www 로 설정돼 apex→www 301. canonical(wasa.kr)과 서빙 도메인 불일치 = SEO 중복 리스크. 👤 Vercel Domains 에서 wasa.kr 을 Primary 로 전환 필요.
- **종결(Primary 전환 후)**: wasa.kr 직접 서빙 200(135ms)·www→308→apex·redirect 0ms. 최종 모바일 지표 — TTFB 135ms·TBT≈0·CLS 0·redirect 0. LH 점수(67)는 "첫 화면=곡선만" 무드 특성상 숨은 카드 이미지가 LCP 로 귀속되는 지표 구조 문제로, 실 UX 와 무관해 여기서 종결.
- 교훈: **국내 서비스는 Vercel 함수 리전 확인이 1순위 성능 항목** — 기본값(iad1)이면 태평양 왕복. 도메인 Primary 방향(apex vs www)도 canonical 과 일치시켜야 성능·SEO 둘 다 지킨다.

### 50단계 — 홈 곡선을 원뿔 나선(3D 코일)으로 (#213, PR #214, 2026-07-19) — 대표님 피드백, 12차 왕복
- **대표님**: 1차원 선형 말고 입체 스프링(Pinterest "The Path To Change" 레퍼런스). 최종형:
  - **원뿔 나선**: `helixSegments(cx, rTop 300, rBot 430, 납작비 0.30, 10바퀴)` — R(t) 선형 증가로 원근감. 레퍼런스 실측 비율(첫 고리 = 화면 폭 ~60%, 화면당 ~2바퀴).
  - **앞/뒤 반바퀴 세그먼트 분할**로 누적 순서 드로잉(스크럽·진입 2.6s 유지). 선 스타일은 **완전 균일**(옅기/굵기 차이는 얼룩처럼 보여 기각) — 입체감은 교차 기하로만.
  - **카드 6슬롯 좌우 교차**(극점 k=4·7·10·13·16·19): 데스크톱은 점 바깥 여백 포켓(테이퍼로 층마다 다른 극점 x를 CSS 변수로), 모바일은 점 안쪽. 간격 3반바퀴 = 교차 유지 최소 홀수 간격이라 **6이 구조 상한**(더 늘리려면 바퀴 수 증가). 카드 축소(max 256px)로 겹침 최소화, 곡선 끝 99.2%→로고 밀착.
  - 휠 이징 0.06(~0.8s 글라이드) 확정 — 시열님 "완벽한 이상향".
- 왕복 로그: 지그재그(바퀴당 세로>화면) → 촘촘화 → 균일 스타일 → 반경/포켓/슬롯 조정. **교훈: 코일이 코일로 보이려면 "한 화면에 고리 1.5개 이상" — 반경보다 피치가 지배 변수.**

### 49단계 — 🎉 도메인 wasa.kr 개통 (2026-07-19)
- **https://wasa.kr 라이브** (www 포함). 경과: 호스팅케이알 A레코드가 파킹 IP 였던 것을 `@ → 76.76.21.21`(Vercel)·`www → cname.vercel-dns.com` 으로 교체. **함정: 호스트 이름 칸에 `wasa.kr` 을 넣으면 `wasa.kr.wasa.kr` 중복 노드가 생긴다 — 루트는 `@` 또는 빈칸.** TTL 180 이라 전파 수분.
- `NEXT_PUBLIC_SITE_URL` 은 이미 wasa.kr 로 설정돼 있어 sitemap·robots·JSON-LD 즉시 실도메인. CSP 헤더 정상(토스·네이버지도 호스트 포함). TTFB ~350ms(동적 렌더, 내부 캐시 42ms).
- **Resend 인증**: DKIM(resend._domainkey)·MX/SPF(send)·(DMARC 선택) 레코드를 호스팅케이알에 추가, 전파 실측 확인. 코드 발신 기본값이 이미 `WABI-SABI <noreply@wasa.kr>` 라 EMAIL_FROM env 불필요 — **Resend Verify 클릭이 마지막 스텝**.
- 남은 실도메인 확인(👤): Contact 지도(안 뜨면 네이버 클라우드 콘솔에 wasa.kr URL 등록)·토스 결제위젯 진입.
- 참고: 팀 *.vercel.app 은 SSO 보호(외부 접근용은 이제 wasa.kr 하나), `wabi-sabi.vercel.app` 은 타인 프로젝트.

### 48단계 — 홈 성능 최적화 + 진입 드로잉 복원 (#211, PR #212, 2026-07-19)
- **마루부리 서브셋**: fonttools 로 KS X 1001 완성형 2350자+라틴·약물 → **425→201KB(-53%)**. 함정: Python `euc-kr` 코덱은 UHC 확장까지 통과(11172자 전부) — **완성형은 인코딩 바이트 영역(0xB0A1~0xC8FE)으로 필터**해야 정확히 2350자. 서브셋 밖 희귀 글리프는 세리프 폴백. Cormorant 미사용 600 웨이트 제외. **폰트 합계 567→344KB(-39%)**.
- **헬릭스 path 스텝 720→240**: 1.3px 획에선 시각 동일, 인라인 path 19.5→6.5KB → **홈 HTML 65→52KB(-20%)**. 폰트 preload 는 Next 16 이 Flight 스트림(:HL)으로 이미 주입(문자열 검사에 안 잡힐 뿐).
- **진입 드로잉 복원**(시열님 지적 — 스크럽 전환 때 소실): 로드 후 2.6s ease-out 계수를 스크럽 진행도에 곱해 0부터 그려진 뒤 자연 인계. 진입 동안만 rAF, 완료 시 자동 정지(상시 비용 0). 카드는 즉시 추종(이중 스무딩 없음).
- 발견: `wabi-sabi.vercel.app` 은 타인 프로젝트(네임스페이스 선점), 팀 URL 은 전부 SSO 보호 → **wasa.kr DNS 연결 전까지 외부 공개 URL 없음**. 성능 측정은 로컬 프로드 서버로.

### 47단계 — 데드코드 정리 + 홈 폰트 마루부리 (#207 PR #208 · #209 PR #210, 2026-07-19)
- **데드코드 정리(#208)**: 헬릭스 리디자인 여파 — HeroParallax·HeroSlideshow·ScrollShowcase 삭제(사용처 0), home.ts featured/heroImages/showcaseItems 파생 제거(select 슬림화 → 캐시 페이로드 ↓), products.ts getProduct/getRelatedProducts(#181 번들로 대체)/getFeaturedProducts 제거. 완료 이슈 12건 일괄 클로즈.
- **홈 폰트 확정(#210, 대표님 "더 얇은 궁서" 요청)**: 시스템 궁서는 굵기 고정이라 웹폰트 전환 필수. Hahmlet Light 시도 → **시열님 기각(진지함 부족)** → **마루부리(MaruBuri) Light 확정** — 네이버 무료(SIL OFL), 붓 부리가 살아있는 전통 세리프라 궁서의 진지함 + 얇음. `src/fonts` 셀프호스팅(next/font/local, 425KB, 외부요청·CSP 무관, display:swap). PC·모바일 동일 렌더.
- 폰트 CDN 메모: 네이버 공식 `hangeul.pstatic.net/hangeul_static/webfont/MaruBuri/` (ExtraLight~Bold woff2) — 서브셋 미제공(통파일)이라 셀프호스팅과 비용 동일.

### 46단계 — 전 페이지 크림 톤 통일 (#205, PR #206, 2026-07-19) — 브랜드 가이드 반영
- 대표님이 브랜드 가이드(로고·컬러 팔레트·타이포) 공유 — 팔레트(먹색·다크브라운·웜그레이·크림·오프화이트)가 홈 헬릭스 톤과 같은 계열. "홈 크림톤 매우 좋다" 확인 후 **전역 wabi 토큰을 크림으로 교체**: bg #f3ebdd·fg #423c30·subtle/muted 크림 계열·border 잉크16%(#d7cfc1)·accent 잉크·footer 다크브라운 #241f17. 홈↔쇼핑 화면 전환 이질감 제거.
- **대비 보정 원칙**: 흰 배경 기준으로 잡았던 강조색(text-red-600·amber-700·blue-700)은 크림 배경에선 WCAG AA(4.5:1) 미달 → 전수 한 단계 진하게(red-700·amber-800·blue-800, hover red-800). **배경 밝기를 낮추면 전경 강조색도 재검해야 한다.**
- 폰트 방침 확정(시열님): **홈만 궁서 유지**, 다른 페이지는 기존 그대로(브랜드 가이드 KR Noto Serif KR 통일은 추후 대표님 조율).

### 45단계 — 푸터 정비·네이버 스토어 연동·스크롤바 진동 버그픽스 (PR #200·#202·#204, 2026-07-19)
- **푸터 사업자정보 무신사·컬리식(#199, PR #200)**: 세로 7줄 스택 → **파이프(|) 구분 한 문단**(11px, white/40, 좌측 정렬). 사업자정보 표시는 전자상거래법 §10 의무(모든 국내 커머스가 푸터에 저대비로 표기 — 시열님 질문에 확인). 공정위 통신판매사업자 공개 팝업 **"사업자정보확인" 링크** 추가(무신사·컬리 공통 관행). 파이프 aria-hidden·새 창 sr-only 안내(CodeRabbit).
- **네이버 스마트스토어 연동(#201, PR #202)**: smartstore.naver.com/wasa_kr 판매 채널 노출 — site.ts `naverStoreUrl` + Contact 항목 + 푸터 링크 + **JSON-LD `sameAs`**(검색엔진이 자사몰·스토어를 같은 브랜드 엔티티로 인식).
- **스크롤바 드래그 진동 버그(#203, PR #204)**: 휠 글라이드 rAF 루프 중 스크롤바를 끌면 루프(옛 목표점 scrollTo)와 브라우저(드래그 위치 강제)가 프레임마다 싸우며 진동. **수정: 루프가 마지막에 쓴 위치(lastSet)와 실제 scrollY 가 1.5px 초과로 다르면 외부 개입 판정 → 애니메이션 즉시 중단·양보.** 자기 스크롤 이벤트는 lastSet 일치라 오탐 없음. 교훈: **스크롤 이징 구현엔 반드시 외부 개입(스크롤바·키보드) 양보 경로가 필요.**

### 44단계 — 홈 전면 리디자인: '하루의 결' 헬릭스 스크롤 여정 (#197, PR #198, 2026-07-19) — 대표님 피드백 3탄
- **컨셉(대표님)**: 노골적 판매가 아닌 "자연스레 구경하고 만져보는" 셀렉샵. 진입 화면엔 로고·코멘트·상단바 전혀 없이 **곡선 하나만**. 스크롤을 내리면 나선 곡선이 그려지면서 모멘트(아침~저녁)마다 상품 사진+한 줄 코멘트가 빈 여백에서 서서히 커지며 등장했다가 작아지며 사라지고, 여정의 끝에서야 로고·철학(侘寂選)·따뜻한 Shop CTA 가 나온다. 클로드 디자인 목업 v2(.dc.html) 소스를 기반으로 이식.
- **구조**: `components/home/helix-journey.tsx`(클라). 나선 경로는 `helixPath()` 순수 계산(SSR/클라 동일 — 하이드레이션 안전). 데스크톱(vb 4600)·모바일(vb 9600, 세로 피치 확대) SVG 를 **둘 다 프리렌더하고 CSS 로만 전환** → JS 분기 없음, 레이아웃 시프트 없음. 시작/끝 여백 비율을 공유해 모멘트 좌표(나선 극점)가 양쪽 정합.
- **선 드로잉**: `stroke-dasharray/offset` 스크럽. 카드 등장은 **점의 뷰포트 위치 기준**(화면 중앙 ±0.32vh 종형) — 캔버스 진행도 기반이면 뷰포트 크기에 따라 첫 화면에 카드가 걸리는 사고가 나는데, 위치 기준은 어떤 화면에서도 "첫 화면=곡선만"이 보장된다. 카드 간 스크롤 간격 > 등장 구간이라 앞 카드가 완전히 사라진 뒤 다음이 시작. 점(dot)도 카드와 함께 페이드.
- **★ 휠 스무스 스크롤(`smooth-scroll.tsx`) — 이번 단계의 핵심 기술**. 마우스 휠은 OS 가 스크롤 델타를 큰 스텝 단위로 던져서(터치패드는 미세 연속값) 페이지가 뚝뚝 점프한다 — 연출(lerp)만 다듬어서는 페이지 자체가 튀는 걸 못 가린다(5차에서 실증). 해법은 애플류 무드 사이트들이 쓰는 **Lenis 방식을 외부 라이브러리 없이 자체 구현**(~40줄):
  1. `wheel` 이벤트만 `preventDefault`(passive:false)로 가로채 목표 스크롤 위치를 누적 (deltaMode 픽셀/줄/페이지 정규화, 문서 끝 클램프)
  2. rAF 루프에서 `cur += (target-cur)*0.1` 지수 이징으로 `window.scrollTo` — 실제 페이지가 60fps 글라이드
  3. **접근성·UX 안전장치**: 터치·키보드·스크롤바 드래그·핀치줌(ctrlKey)은 일절 안 건드림, `prefers-reduced-motion` 은 통째로 비활성, 다른 입력 감지 시 목표를 현재 위치로 동기화, 홈 이탈 시 리스너 해제. 수렴하면 rAF 자동 정지(유휴 비용 0).
  4. 이중 스무딩 금지 — 페이지가 이미 부드러우므로 연출은 스크롤을 직접 추종(겹치면 둥둥 뜨는 랙).
  - 시열님 확인: "진짜 훨씬 부드러워지고 좋아졌다."
- **무드**: 크림(#f3ebdd)/브라운(#423c30). 폰트는 **궁서체**(빈티지·진지함, 시열님 지정) — 웹폰트가 없어 시스템 궁서(Windows Gungsuh·macOS GungSeo) 최우선 + 모바일 폴백 Song Myung(옛 활판 명조), 라틴 이탤릭은 Cormorant Garamond, 가격·라벨은 Space Mono. **전역 토큰 미변경**(홈 래퍼 CSS 변수만 — 리스크 격리).
- **상품 연결**: `getHomeData.journey` — 사진 있는 상품 우선 5개, 코멘트는 상품 설명 첫 문장(46자 컷) 또는 모멘트별 기본 시문장. 카드 클릭 → 상세.
- **제거**: 히어로 카피·시간축·화살표·시어·"당신의 식탁" 노드·뉴스레터·방문 안내(Contact 에 있음)·홈 상단바(SiteHeader 가 `pathname==="/"` 면 null). 기존 HeroParallax·ScrollShowcase 파일은 유지(롤백 용이).
- CodeRabbit 반영: 숨은 카드가 Tab 포커스 받던 것 → `inert` 토글(초기 SSR 포함). 피드백 6차 왕복 전부 preview 로 시열님 눈 확인.

### 43단계 — Shop 좌측 사이드바 토글 내비 (#195, PR #196, 2026-07-19) — 대표님 피드백 2탄
- biomedium.kr 참고. 데스크톱(lg+) 좌측 사이드바: 전체/이 달의 상품 + 대분류 `details/summary` 토글(**무JS·무라이브러리 — CSP·번들 무관**) → "대분류 전체"+소분류. 현재 선택 소속 대분류 자동 열림.
- 모바일: 대분류 칩(그룹 활성) + 선택 시 소분류 칩 2줄. `lib/shop-url.ts` URL 빌더 공용화.
- CodeRabbit 반영: 소분류 선택 시 부모 칩 `aria-current` 중복 → FilterLink 에 `current`(정확 일치) prop 분리, 부모는 시각 강조만.
- preview 눈 확인 후 병합. **대표님 추가 요구: 모바일 호환(반응형) 전반 중요** — 이후 모든 작업(특히 홈 리디자인) 모바일 우선 검증.

### 42단계 — 카테고리 2계층 트리 (#193, PR #194, 2026-07-19) — 대표님 피드백 1탄
- **대표님 미팅 피드백 3종 접수**: ① 홈 전면 리디자인(진입 즉시 곡선만 → 스크롤 따라 상품·코멘트 등장/소멸 → 끝에 로고·철학. 크림/피치·빈티지 톤, 목업 v2 소스 전달 예정) ② 카테고리 소분류 체계(biomedium.kr 좌측 사이드바 토글 참고) ③ 상품군 확장(다도 도구·부채·키링·목걸이·팔찌·머리끈). 순서: ②데이터 → ③사이드바 → ①홈.
- **마이그 0032**: `categories.parent_id`(self FK). 대분류 4종(식기·다도·액세서리·리빙) + 기존 7종 소분류 재배치(life 라벨만 '생활 소품') + 신규 6종(다기·부채·키링·목걸이·팔찌·머리끈). '선물'은 하위 없는 단독 대분류. **기존 상품 연결 무손실(slug 불변)**.
- `lib/site.ts` `categoryTree`+`categorySlugs()`(대분류→하위 확장). **DB 와 트리가 함께 진실 — 트리 변경 시 마이그 동반.**
- shop 쿼리: 대분류 필터 `.in("categories.slug", 확장목록)` 하위 포함. 어드민 폼: 소분류만 선택("식기 > 접시" 라벨).
- 검증: 트리 실측(4+12+1)·필터 정합(tableware=6=하위 합). CodeRabbit 지적 0건.

### 41단계 — 어드민 대시보드 보강 (#191, PR #192, 2026-07-19)
- 숫자 카드만으론 어떤 상품·주문인지 한 번 더 들어가야 했다 → 첫 화면 정보량 보강:
  - **최근 7일 매출 추이**: 마이그 0031 `admin_sales_trend(p_days)` RPC — KST 하루 경계 일별 주문·매출, 빈 날 `generate_series` left join 0 채움, security definer + service_role 한정(0024 관례). 렌더는 **CSS 막대**(외부 차트 라이브러리 없음 — CSP·번들 무관), 만원 축약 라벨.
  - **재고 주의 상품 목록**(품절 빨강·부족 amber-700, 8개) + **최근 주문 5건**(OrderStatusBadge 재사용·KST 일자).
  - 4쿼리 Promise.all 병렬. 목록 2종은 행 수 작아 RPC 없이 직접 조회.
- 함정: RPC 가 json_agg 단일 json 을 돌려주면 supabase-js `.returns<T[]>()` 가 "Cannot cast single object to array" 타입 에러 → `.returns` 빼고 data 캐스트.
- 검증: RPC 실측(0 채움 + 실데이터), anon 42501 차단, 빌드 통과. CodeRabbit 지적 0건.

### 40단계 — 접근성 페이지 제목·시맨틱 보강 (#189, PR #190, 2026-07-19)
- 폼 전수(34단계)에 이은 나머지 a11y 갭 점검. 기반이 이미 탄탄해 실제 갭 3종만 수정:
  - `/auth` h1 부재 → `sr-only` h1 + 로그인/회원가입 탭 버튼 `aria-pressed`
  - **"use client" 페이지(auth·cart·checkout)는 metadata 수출 불가라 페이지별 `<title>` 없음**(WCAG 2.4.2) → 라우트 `layout.tsx` 3개 신설로 title 제공(로그인/장바구니/주문하기)
  - shop 카테고리 필터 `aria-current` 추가(정렬 링크와 통일)
- 점검 통과: Image alt 14/14 · 아이콘 버튼 aria-label 전수 · skip link · aria-expanded · role=alert/status 12곳 · 장식 이미지 aria-hidden · focus-visible 링 전수 · cart/checkout 은 robots.txt disallow 라 noindex meta 불요
- CodeRabbit "role=tab 으로 바꿔라" 는 **의도적 스킵**: ARIA APG 상 tab 은 화살표 키 이동 구현이 전제 — 키보드 핸들링 없는 role 만 달면 반쪽 시맨틱. 2개짜리 전환은 aria-pressed 토글 쌍이 정직한 패턴.

### 39단계 — Supabase 기반 rate limit 저장소 (#187, PR #188, 2026-07-18)
- 기존 rate limit(`lib/rate-limit.ts`)은 Upstash Redis(미설정)→인메모리 폴백 2단. **인메모리는 서버리스 인스턴스별 독립(Map)이라 Vercel 에서 상한이 인스턴스 수만큼 느슨하고 콜드스타트마다 리셋.** 외부 인프라(Redis) 없이 인스턴스 간 공유되는 상한을 Supabase 테이블로 확보.
- **마이그 0029**: `rate_limits` 테이블(RLS on·정책 없음 → anon/authenticated 직접 접근 전면 차단, service_role 만 우회) + `check_rate_limit` RPC(원자적 `upsert` 고정창 카운터, `security definer`, execute=`service_role`만). 낮은 확률(0.5%)로 1시간 지난 창 청소. **마이그 0030**: `window_start` 단일 인덱스(청소 DELETE 가 PK 선두 bucket 없이 전수 스캔하던 것 해소, CodeRabbit).
- **백엔드 3단화**: Redis → **Supabase** → 인메모리. Vercel 은 `SUPABASE_SERVICE_ROLE_KEY` 있고 Upstash 없어 자동으로 `supabase` 선택. 원격 실패 시 인메모리 fail-open(부가 기능이 레이트리밋 장애로 죽지 않게).
- `reportReview` 에 사용자당 시간 20건 상한(서로 다른 리뷰 대량 신고 brigading 방어). 거절은 기존 설계대로 조용히 무시(ReportReviewButton 은 중복·자기신고·RLS 실패를 모두 "신고 접수됨" 으로 표시 — 신고 여부 비노출).
- **CodeRabbit 리뷰**: `adminConfigured()` 가 키만 검사해 `NEXT_PUBLIC_SUPABASE_URL` 빠진 배포에서 매 요청 인메모리 폴백하던 것 → URL 도 검증하도록 수정.
- **검증(anon/service_role 실측)**: 카운터 1→6 증가·limit 초과 판정·창 분리·anon RPC `permission denied(42501)`·anon 테이블 SELECT 빈결과·INSERT 차단.

### 38단계 — 상품 목록 탐색 캐시 + 카테고리 룩업 왕복 제거 (#185, PR #186, 2026-07-18)
- `/shop` 탐색(검색어 없음)을 홈·상세·공지·리뷰에 이어 **anon 클라 + `unstable_cache`(120s)** 로 캐시(`getShopBrowse`). 카테고리(약 10)×정렬(3)로 캐시 키 유한. **이로써 모든 공개 read 경로 캐시 완료.**
- **카테고리 슬러그→id 룩업 왕복 제거**: 기존 `getProducts` 는 category 필터마다 `categories` 테이블을 먼저 조회(별도 왕복)했는데, `categories!inner(slug, name_en)` 조인 + `.eq("categories.slug", …)` 로 **2쿼리→1쿼리**. (PostgREST 는 embed 컬럼 필터가 부모행을 좁히려면 inner 조인이어야 함.)
- 검색(q)은 입력이 무한이라 캐시 밖 — 기존 `getProducts`(서버 클라) 유지.
- **함정 회피(37단계 교훈 적용)**: `/shop` 은 `searchParams` prop 으로 선-dynamic → anon 클라가 빌드 프리렌더에서 실행 안 됨. **sitemap 이 쓰는 `getProducts` 는 서버 클라 그대로** 둬(전역 교체 시 sitemap 빌드가 DB 에 접근하는 함정 회피).
- 어드민 상품 액션 7개에 `revalidatePath("/shop")` 추가(생성·수정·삭제·활성토글·재고·monthly토글 즉시 반영).
- **검증**: anon 키로 DB 직접 대조 — 카테고리별 개수 전부 일치(MISMATCH 0)·잘못된 슬러그 빈 배열·정렬 정상. Vercel preview 는 SSO 보호라 익명 fetch 불가해 이 방식으로 확인.

### 37단계 — 공지·리뷰 공개 목록 캐시 (#183, PR #184, 2026-07-18)
- 공지 목록/상세(`getNotices`·`getNotice`)와 리뷰 목록(`getRecentReviews`)을 홈·상세와 동일하게 **anon 클라(`createPublicClient`) + `unstable_cache`(120s)** 로 전환. 매 요청 DB 왕복 제거.
- 무효화는 기존 쓰기 액션에 이미 배선된 `revalidatePath("/notice"|"/review")` 에 편승(어드민 공지 생성/삭제·리뷰 숨김, 사용자 리뷰 작성/삭제/신고). 공지 삭제에 `/notice/${id}` 상세 무효화만 추가.
- 안전성: anon 은 RLS(0023)상 `hidden=false` 리뷰만 조회 → 본인 숨김 리뷰 누출 없음(쿼리 `.eq("hidden", false)` 는 이중 방어).
- **함정 — cached-public 쿼리를 순수 정적 라우트에 쓰면 빌드가 DB 에 접근한다**: 캐시 전환 후 `/notice`·`/review` 가 쿠키·searchParams 를 안 읽어 **빌드 타임 정적 프리렌더** 대상이 됨. 프리렌더가 페이지 본문을 실행 → `createPublicClient()` 호출 → CI Build 스텝엔 공개 env(`NEXT_PUBLIC_SUPABASE_URL`) 미주입이라 `supabaseUrl is required` 로 빌드 실패. (`/review` 는 `getUser()`(쿠키)로 결국 dynamic 이지만 캐시 쿼리가 **그보다 먼저** 실행돼 dynamic 판정 전에 throw.)
  - **판별 규칙**: 홈(`searchParams` prop → 선-dynamic)·`[id]`(dynamic route, generateStaticParams 없음 → 빌드 프리렌더 안 함)는 자동 안전. **순수 정적 라우트**에서 cached-public 쿼리를 쓰면 `export const dynamic = "force-dynamic"` 필요. 쿼리는 `unstable_cache` 라 dynamic 렌더여도 DB 왕복은 그대로 없다(성능 이득 유지, 빌드는 DB 비의존 → 더 견고).
  - 로컬 빌드는 `.env.local` 있어 프리렌더가 통과해버려 못 잡음 → **CI(env 없는 환경)가 이 클래스의 회귀를 잡는다.**

### 36단계 — SEO 구조화 데이터 + 상세 캐시 + 보안 더블체크 (2026-07-18)
- **SEO 리치결과(#179, PR #180)**: Product JSON-LD 에 `aggregateRating`(리뷰 평점, reviewCount>0 일 때만 — 0이면 구글이 리치결과 거부) + `brand`, OnlineStore 에 telephone·email. 구글 검색결과 별점(★) 노출로 클릭률 개선.
- **상품 상세 캐시(#181, PR #182)**: 상세 TTFB 0.45~1.6s → **0.33~0.60s**. `getCachedProductDetail`(anon + unstable_cache 120s, id별 키)로 공개 데이터(상품·관련상품·평점) 통합, generateMetadata·페이지가 캐시 공유. 사용자별(리뷰·위시·재입고)은 캐시 밖. 어드민 toggleActive·deleteProduct 에 `/shop/[id]` 무효화 추가.
- **보안 더블체크 5방향(취약점 0)**: 보안 체크리스트 2026-07-18(2) 참조 — 번들 시크릿 노출 0·인증 IDOR 없음·어드민 액션 16/16 requireAdmin·결제 RPC 클라 차단·무인증 엔드포인트 가드.

### 35단계 — 홈 성능 캐시 + RLS 전수 보안 점검 (2026-07-18)
- **홈 캐시(#177, PR #178)**: 홈만 TTFB 1.6~3.9s(shop 0.4s·정적 0.65s) — 매 요청 공개 데이터를 캐시 없이 4~5쿼리 왕복이 병목. `lib/supabase/public.ts`(쿠키 없는 anon 클라이언트 → `unstable_cache` 안에서 사용 가능, 서버 클라이언트는 cookies() 라 캐시 밖 전용). `lib/queries/home.ts`: 활성 상품 12개 + 소개문구 **2쿼리로 통합**, featured·히어로 이미지·쇼케이스 파생, `unstable_cache(revalidate 120s)`. 어드민 상품·콘텐츠 변경 시 `revalidatePath("/")` 즉시 반영. **결과: TTFB 1.6~3.9s → 0.04~0.39s(캐시 히트 42ms).**
  - `revalidateTag` 는 Next 16 에서 `(tag, profile)` 2인자(Cache Components 용)라 `unstable_cache` 태그 무효화와 안 맞음 → `revalidatePath` 로 처리(cacheComponents 전환은 별도).
- **RLS 전수 우회 매트릭스(2026-07-18)**: OWASP Top 10:2025 A01(권한통제, 1위) 대응. anon JWT 로 **전 18개 테이블 × SELECT/INSERT/UPDATE/DELETE** 공격 → **유출 0·의도치 않은 write 0**. 공개 read 5개만 노출, 소유자/서버전용 13개 격리(0건). 신규 테이블(0022·0026·0027) 포함 안전. 보안 체크리스트 진행 로그에 기록.

### 34단계 — 폼 접근성 전수 점검 (#175, PR #176, 2026-07-17)
- **placeholder 는 접근 가능한 이름이 아니다** — 입력을 시작하면 사라져 스크린리더·인지 보조 사용자가 필드 용도를 잃는다. `/auth` 만 `Field`(label+htmlFor+autoComplete+aria-describedby)로 제대로 돼 있었고 나머지 폼은 placeholder 만 의존했다.
- 전 폼 스캔 후 `aria-label` 부여 — 고객: 마이페이지 배송지 5종·리뷰 작성·문의(제목·내용)·결제 선물 옵션. 어드민: 공지·상품 등록(상품명·가격·재고)·문의 답변·송장번호.
- **목록 안 반복 필드는 대상까지 식별**되게(예: "주문 WSB… 송장번호") — 같은 이름이 여러 개면 스크린리더가 구분 못 한다.
- `delete-account-section` 은 `label htmlFor` 로 이미 연결돼 있어 수정 없음(카운트 휴리스틱의 오탐).

### 33단계 — 로딩 스켈레톤 확충 (#172, PR #173, 2026-07-17)
- 데이터 조회 화면인데 `loading.tsx` 가 없어 전환 시 빈 화면이 노출됐다(shop·mypage 만 있었음).
- 추가: `/mypage/orders`(카드+썸네일)·`/mypage/wishlist`(그리드+담기)·`/review`·`/notice`·`/inquiry`. **실제 레이아웃에 맞춰** 작성 — 스켈레톤이 실제 구조와 다르면 전환 시 오히려 튄다.
- **CSP nonce 전환 검토 후 보류**: Next 16 nonce 는 페이지가 동적 렌더여야 주입된다. 우리는 /about·/legal/* 등이 정적(`○`)이라 nonce 켜면 그 페이지 스크립트가 차단되거나 **전 페이지 동적 렌더 강제**(TTFB·비용↑). 얻는 건 `unsafe-inline` 제거인데 XSS 표면은 이미 React 이스케이프+JSON-LD 수동 이스케이프로 차단됨 → **정식 오픈 후 트래픽·리스크 보고 재판단**.

### 32단계 — 마이페이지 주문내역 다듬기 (#170, PR #171, 2026-07-17)
- 주문 내역이 정보 나열이라 어떤 주문·어떤 상태인지 한눈에 안 들어왔다(상태가 작은 회색 텍스트).
- `OrderStatusBadge`(내역·상세 공용): 상태별 색 구분(대기 amber·결제완료 차콜·배송중 blue·완료 muted·취소 red). **작은 텍스트라 WCAG AA 대비 700 계열**, 배경 없이 테두리로 미니멀 유지. 주문 카드 대표 썸네일(상품 삭제 시 플레이스홀더 — `order_items.product_id` 는 on delete set null). 링크 중복 제거 + `aria-label`.
- CodeRabbit: 취소만 red-600 이던 것 700 계열로 통일.

### 31단계 — 히어로 패럴랙스 + 스크롤 스크럽 쇼케이스 (#168, PR #169, 2026-07-17)
- **시열님 방향**: 애플 제품 페이지처럼 스크롤 상호작용 강화(에어팟 360° 회전 같은).
- **히어로 패럴랙스**: 배경(상품 슬라이드쇼) 0.18배 속도로 느리게 따라오고, 로고·텍스트는 스크롤 진행도로 페이드아웃. passive scroll + rAF. **LCP 보호 — 초기 렌더는 변형 없음**(히어로 로고가 LCP 후보). CodeRabbit: 중간 로드(scrollY>0, 새로고침·뒤로가기)에선 초기 1회 반영.
- **ScrollShowcase**: sticky 로 화면 고정 + 스크롤 진행도 → 상품 인덱스 매핑, 크로스페이드·스케일 전환·캡션·인디케이터. **이것이 애플 360° 회전과 동일 메커니즘** — 상품 턴테이블 360 촬영 프레임(36~72장) 확보 시 items 만 교체하면 회전 스크럽으로 확장.
- 성능: 상태는 인덱스만(연속 진행도를 state 로 두면 프레임마다 리렌더). reduced-motion 시 비활성. 사진 있는 상품 2개 미만이면 섹션 미렌더.
- ⚠️ **진짜 360 회전은 에셋 문제** — 코드는 준비됨, 대표님 턴테이블 촬영 필요.

### 30단계 — 재입고 알림 (#166, PR #167, 2026-07-16)
- **이탈 지점**: 품절 상품을 사려던 고객이 재고 복구를 알 방법이 없었다.
- 마이그 `0027` `restock_subscriptions`(상품당 1인 1건 unique, 본인 RLS). 상품 상세 품절+로그인 시 신청/취소. 발송 주소 = 계정 이메일(별도 수집 안 함 → 검증·스팸 문제 회피). **어드민 재고가 0 → 양수일 때만** 1회 발송 후 구독 해제.
- **CodeRabbit 🔴 Critical**: `sendMail` 은 실패 시 `false` 를 반환하는데 무시하고 구독을 삭제 → **발송 실패해도 재시도 불가**. 성공한 구독만 해제로 수정. 🟠 구독/취소 액션의 error 무시(버튼만 토글돼 알림 받는 것처럼 보임) → boolean 반환·실패 시 상태 유지. 🟠 재고 update 결과 미확인(저장 실패 후에도 감사로그·메일) → 조기 반환. 🟡 `0028` unique 제약과 중복되는 인덱스 제거. 스킵: RPC 트랜잭션(어드민 단일 운영, 부수효과 차단으로 실질 위험 해소).
- 검증: anon insert 401 차단·select 격리. ⚠ 실발송은 도메인 DNS·Resend 인증 후 고객 전달(코드는 개통 즉시 동작).

### 29단계 — 사업자 정보 입력·도메인 결정 (#106, PR #165, 2026-07-16)
- **대표님 확인 완료**: 사업자등록번호 `411-74-00574`·통신판매업신고번호 `2026-충남천안-1000` 입력 → 푸터·법적고지 3종 자동 노출(렌더는 값 있을 때만). 전자상거래법 §10 의무 이행.
- **도메인 wasa.kr 구매 결정**. 코드는 `lib/site-url.ts` SITE_URL 로 준비 완료 → 시열님 연결 작업(도메인 구매·Vercel 도메인/`NEXT_PUBLIC_SITE_URL`·Resend DNS 인증·`EMAIL_FROM`) 후 고객 메일 발송·실도메인 SEO 개통.

### 28단계 — 결제 저장 배송지 불러오기 (#162, PR #163, 2026-07-16)
- **UX 갭**: 마이페이지에 배송지 저장해도 결제 시 매번 수동 재입력.
- `getMyAddresses` 서버 액션(본인 addresses, RLS 소유자 전용 → 타인 주소 미노출). 배송지 필드 controlled 전환, 저장 배송지 있으면 최근 것 자동 채움 + 드롭다운 선택.
- **CodeRabbit**: 🟠 자동채움 `useRef` 1회 제한(user 참조 갱신 시 입력 중 값 덮어씀 방지) · 🔵 '직접 입력' 옵션(필드 초기화) · 🟠 배송지 Input `aria-label`.

### 27단계 — 애플식 스크롤 애니메이션 + 콘텐츠 편집 (2026-07-16)
- **시열님 방향**: 애플 제품 페이지처럼 스크롤 시 요소가 부드럽게 등장, 이목 끄는 디자인.
- **Reveal 컴포넌트(#155·157, PR #156·158)**: IntersectionObserver 로 뷰 진입 시 등장(once). 외부 라이브러리 없이 클래스 토글 → CSP·번들 영향 0. `prefers-reduced-motion` 이면 motion-reduce override 로 즉시 표시. variant(up/left/right/scale/blur)·stagger delay. **About·홈·Shop 확산** — 히어로는 LCP 위해 제외, Shop 첫 행도 즉시. CodeRabbit: `blur-0`→`blur-none`(Tailwind 정확성), setState-in-effect lint 회피(CSS override).
- **히어로 크로스페이드(#147, PR #159)**: 시열님 피드백 — 3초 텀 유지, 전환 1s→2.5s 서서히.
- **철학 멘트 어드민 편집(#160, PR #161)**: 대표님 요청 — 홈·About 철학 문구를 코드 수정 없이 직접 편집. 마이그 0026 `site_content`(key-value, 공개 read·**write service_role 만**). `/admin/content` textarea. 홈·About 은 DB값 렌더(폴백=기본 문구), 문단은 빈 줄 구분. 실측: anon read 200·**anon write 401 차단**·service_role 201.

### 26단계 — Shop UI 다듬기 1차 (#151, PR #152, 2026-07-16)
- **디자인 축 시작**(기능 견고화 완료 후). 목록: 헤더 결과 수, 카테고리→[검색+정렬] 툴바 그룹화·구분선, 그리드 세로 여백. 상세: `ProductGallery`(클라) 썸네일 클릭 시 메인 전환(기존 정적).
- **CodeRabbit**: 🟠 검색 폼 `next/form`(클라 내비, 전체 새로고침 방지)+`role="search"` · 🟠 썸네일 `focus-visible` 링(키보드) · 🟡 `ProductGallery key={product.id}`(상품 전환 시 썸네일 선택 초기화) · 🟡 정렬 활성 `aria-current`.
- ⚠️ **상품 사진이 테스트 이미지**(찻잔 세트에 도롱뇽 등) — 실오픈 전 실상품 사진 교체 필요(시열님).

### 25단계 — 위시리스트 장바구니 담기 (#149, PR #150, 2026-07-16)
- **기능 갭**: 위시리스트에 제거 버튼만 있고 담기 수단이 없어 상세로 다시 들어가야 했음. 각 상품에 담기 버튼(`AddToCartButton` 재사용) + 위시 쿼리에 stock 추가(품절 표시·담기 차단).
- **CodeRabbit 🔴 Critical**: `null <= 0` 은 `true` — PostgREST 가 미관리 컬럼을 null 로 주면 재고 무제한 상품이 전부 품절로 오판. (현 스키마 stock 은 not null 이라 실무상 안전했지만 방어적으로) stock 을 `number|null` 로 받아 `?? undefined` 정규화 + 품절 판정 `typeof === "number"` 가드. **교훈: 조인/옵셔널 값의 `x <= 0` 은 null 을 반드시 배제할 것.**

### 24단계 — 홈 히어로 상품 이미지 슬라이드쇼 (#147, PR #148, 2026-07-15)
- **시열님 UI 요청**: 홈 히어로가 로고+텍스트만 있는 정적 화면. 로고 뒤 배경에 상품 사진이 계속 바뀌며 나타나게.
- `HeroSlideshow`(클라이언트): 상품 이미지 크로스페이드 순환(3초), `aria-hidden` 장식. 1장 이하면 순환 안 함, `prefers-reduced-motion` 이면 첫 이미지 고정. 소스 = 이미지 등록된 활성 상품에서 수집·**중복 URL 제거**(최대 6장), 0장이면 `bg-wabi-subtle` 폴백.
- **시각 반복(시열님 피드백 3회)**: ① 너무 뿌옇다·3초 → 크림 오버레이 대폭 완화(중앙 /5, 상하 35~40%) + 로고·워드마크 크림 drop-shadow 후광. ② 사진 더 선명·카테고리 줄이 배경에 묻힘 → 태그라인·카테고리 `text-black`·굵게 + 흰 이중 text-shadow.
- **CodeRabbit**: 🟠 홈 데이터 페칭 워터폴 → `Promise.all`(featured·slidePool·searchParams 병렬, TTFB 단축).
- ⚠️ **데이터 의존**: 현재 DB 상품 9개 중 사진 등록은 1개(`test`)뿐 → 순환 효과는 상품마다 사진 등록해야 나타남(어드민 업로드 시 자동).

### 23단계 — 저재고 경고 (#145, PR #146, 2026-07-15)
- **문제**: 품절(재고 0)만 표시. 재고 소진 임박 상품을 미리 못 봐 재입고·발주 타이밍을 놓칠 수 있었다.
- `lib/inventory` `LOW_STOCK_THRESHOLD`(=5) 단일 출처 + `isLowStock()`. **0025**: 대시보드 RPC 에 `low_stock` 카운트(`0<stock<=임계 and is_active`). 임계는 앱이 단일 출처라 **RPC 파라미터로 전달**(기본 5) — 시그니처 변경이라 무인자 함수 drop 후 재정의, execute service_role 제한 유지.
- 대시보드 "재고 부족" 카드(주황 tone — 품절 빨강과 급함 구분) + 어드민 상품목록 재고 셀 품절/부족 뱃지.
- **CodeRabbit 반영**: 🟡 `amber-600`(대비 ~3.2:1)은 작은 텍스트 WCAG AA(4.5:1) 미달 → `amber-700`. 스킵: products 부분 인덱스(데이터 소량, CodeRabbit 도 optional 명시).
- 검증: RPC `low_stock=2`(파라미터·기본값 동일), anon `401` 차단 유지.

### 22단계 — 어드민 대시보드 요약 (#143, PR #144, 2026-07-15)
- **문제**: 어드민 홈(`/admin`)이 정적 링크 그리드뿐 — 로그인해도 **지금 뭘 처리해야 하는지** 알 수 없었다.
- **처리 대기 카드**: 발송 대기(paid)·미답변 문의·신고된 리뷰(미숨김)·품절 상품. 0이면 회색, 있으면 빨강 + 관리 화면 링크. **오늘 현황**: 주문·매출·배송 중(KST 기준).
- **CodeRabbit 반영(중요)**: ① 🟠 **JS 집계가 Data API 1,000행 제한에 걸림** — 원시 행을 가져와 Set/reduce 로 세면 매출·건수가 조용히 낮게 표시(매출은 돈이라 하루 주문 많아지는 시점에 대표님께 틀린 수치 노출) → **DB RPC(0024 `admin_dashboard_summary`)로 이관**, `count(distinct)`·`sum` 을 SQL 에서. 하루 경계도 SQL 에서 KST 고정(`date_trunc(... now() at time zone 'Asia/Seoul')` → UTC 환산) → JS 유틸 제거. ② 🟠 **조회 실패를 0 으로 은폐** — 모든 error 를 버려 권한/스키마/네트워크 실패가 정상 0건처럼 보였음 → 단일 `.throwOnError()` 로 에러 경계 위임. ③ 🟡 접근성 h1 계층 복원. ④ 🟡 service_role 미설정 경고에 "요약 조회" 비활성 명시.
- **보안**: RPC 는 `security definer` 라 RLS 우회 → execute 를 `service_role` 로만 제한(anon/authenticated revoke). **실측: anon 호출 `401 permission denied`**(매출·미답변 수 노출 차단), service_role 200(발송대기 3·오늘주문 1·매출 86,000, KST 경계 정확).

### 21단계 — 리뷰 모더레이션: 신고·숨김 (#141, PR #142, 2026-07-15)
- **문제**: 어드민이 할 수 있는 건 리뷰 **hard delete 뿐**이었고, 고객이 부적절 리뷰(욕설·광고·허위 후기)를 알릴 수단이 없었다. 별점은 상품 평균으로 노출돼 가짜·악성 리뷰가 곧 판매에 영향. 정식 오픈 전 최소 모더레이션 체계 필요.
- **migration `0022`**: `reviews.hidden`(되돌릴 수 있는 soft-hide — 삭제와 달리 원문·신고 맥락 보존) + `review_reports`(신고자·사유·시각, `unique(review_id,reporter_id)` 중복 방지, 신고자 탈퇴 시 `set null` 로 기록 보존).
- **RLS-first**: 공개 read 정책을 `hidden=false` 로 교체 → 상품 상세·목록·평점 집계에서 **쿼리 코드 변경 없이 DB 레벨 자동 제외**. 신고 insert 는 `로그인 본인·자기 리뷰 신고 불가·중복 차단`. `review_reports` 는 **select 정책 없음 → 신고자 신원 비공개**(어드민만 service_role 조회).
- **UI**: 고객 — 상품 상세·/review 목록에 신고 버튼(사유 프리셋 select, 로그인·비본인일 때만). 어드민 — 신고 카운트 배지·사유·신고순 정렬, 숨김/해제 토글(삭제와 별개).
- **PostgREST 직접 공격 검증**: 숨김 리뷰 anon 조회 0 · 신고내역 anon read `[]` · 자기외 계정 신고 insert `401` 차단.
- **CodeRabbit 반영**: ① 🟠 **신고 사유가 enum 이 아니라 임의 1~500자** — Server Action 직접 호출로 `<select>` 제약을 우회해 임의 텍스트 저장 가능 → `REPORT_REASONS` 공유 모듈 + `z.enum` 서버 재검증. ② 🟠 **작성자 본인이 자기 숨김 리뷰를 못 봐 재작성 시 unique 위반 silent-fail**(리뷰가 사라진 것처럼 보임) → `0023` RLS 예외(`hidden=false OR auth.uid()=user_id`), 통계·/review 는 `hidden=false` 명시 제외로 본인 숨김 리뷰가 평균·공개목록에 누출 안 되게, 상세엔 "숨김 처리됨" 라벨. ③ 🔵 신고 로그인 리다이렉트 복귀 경로 유지. 스킵 — 인덱스 CONCURRENTLY(reviews 소량, 락 무의미)·adminConfigured read 경고(레이아웃 배너 기존).
- 0023 적용 후에도 anon 은 숨김 리뷰 조회 0(작성자 예외 비누출) 재확인.

### 20단계 — 고객 통지·주문 가시성·도메인 (2026-07-15)
- **주문/배송 메일 (#129, PR #130)**: 결제가 끝나도, 송장이 등록돼도 **고객에게 아무 통지가 가지 않았다**(전자상거래법 §13 계약내용 통지). `lib/email.ts`(Resend REST, SDK 의존성 없이 fetch). **중복 발송 방지**: 확정 RPC 가 `confirmed`(최초 확정)를 반환할 때만 발송 — 성공 페이지·웹훅이 동시에 확정하면 한쪽은 `already_paid` 를 받으므로 이 조건이 곧 멱등성. 키 미설정·발송 실패가 결제/송장 저장을 되돌리지 않는다. HTML 이스케이프(주소에 `<script>` 넣어 실측).
- **문의 답변 알림 (#133, PR #134)**: 답변이 달려도 고객이 몰랐다. **답변 본문은 메일에 싣지 않는다** — 문의는 비밀글일 수 있어 본문을 실으면 평문 유출. 제목+링크만(로그인 후 열람). 탈퇴 회원(user_id null)은 대상 제외.
- **Resend 연동(👤 시열님 키 발급)**: 실제 발송 확인 완료. 다만 **`wasa.kr` 도메인 미보유 → 발신 도메인 인증 불가** → 현재는 `onboarding@resend.dev` 로 계정 소유자에게만 발송 가능. 고객 발송은 도메인 확보 후.
- **품절 표시 (#131, PR #132)**: 재고 0 상품이 목록에서 정상 상품과 구분되지 않고 담기 버튼도 활성. 담아도 결제 시 0010 재고 검증에 걸리지만 **담고 결제까지 시도한 뒤 알게 되는 건 나쁜 경험**. ProductCard 품절 오버레이(0 과 undefined 구분) + 목록 담기 버튼 비활성.
- **주문 상세 (#137, PR #138)**: 어드민이 **송장번호를 저장하는데 고객이 볼 화면이 없었다**(배송 메일에만 실림). 배송지·요청사항·선물포장·항목 전체도 주문 후 확인 불가. `/mypage/orders/[id]` 신설 — 타인 접근 시 RLS 로 조회 자체가 비어 404(실브라우저 확인, 정보 노출 0).
- **죽은 도메인 링크 제거 (#135, PR #136)**: **`https://wasa.kr` 이 7곳에 하드코딩돼 있는데 이 도메인은 등록조차 되어 있지 않다**(whois 확인). sitemap·robots 가 검색엔진에 접속 불가 URL 을 제출 중이었고, 방금 만든 메일들의 링크가 전부 죽은 사이트를 가리켰다. `lib/site-url.ts` 의 `SITE_URL` 단일 출처(`NEXT_PUBLIC_SITE_URL` → Vercel 프로덕션 도메인 → localhost).
  - CodeRabbit 지적 반영: ① `VERCEL_PROJECT_PRODUCTION_URL` 은 **서버 전용** — site.ts 를 임포트하는 클라이언트 번들에선 폴백이 localhost 로 굳어 조용히 틀린다 → `server-only` 모듈로 분리. ② 프로토콜 없이 `wasa.kr` 만 넣으면 `new URL()` TypeError 로 **앱 전체 렌더 실패** → https 정규화(프로토콜 없이 넣고 실측, 홈 200).
  - 👤 **도메인 결정 대기**: `wasa.kr` 미등록·구매 가능(.kr 연 1~2만원). 대표님과 상의 중. 구매 시 `NEXT_PUBLIC_SITE_URL` + Resend 도메인 인증 → 고객 메일 발송 개통.

### 19단계 — 주문 생애주기·리뷰 무결성 (2026-07-15)
- **배송완료 처리 (#124, PR #125)**: 어드민은 송장 입력으로 `paid → shipping` 까지만 가능했고 **`delivered` 로 가는 경로가 없어 모든 주문이 영구히 "배송 중"** 이었다. 표시 문제가 아니다 — 교환·환불 안내(#106)가 "수령일부터 7일 청약철회"라 명시하는데 **수령일을 기록하지 않으면 기산점이 없다**(분쟁 시 근거 없음).
  - migration `0020` `orders.delivered_at`. `markDelivered`(paid·shipping → delivered, 감사로그). 대면거래도 있어 송장 없이 paid→완료 허용.
  - 가드: 취소·미결제는 완료 대상 아님(멱등) + **배송완료 주문의 송장을 고쳐도 shipping 으로 되돌아가지 않음**(되돌아가면 수령일과 모순 → 청약철회 기산점이 흔들림).
  - 고객 주문내역에 수령일 + 청약철회 마감일(수령+7일) 안내.
  - **CodeRabbit 지적 반영(중요)**: ① **시간대** — 서버 컴포넌트는 Vercel(UTC)에서 렌더돼 `toLocaleDateString` 이 KST 와 최대 하루 어긋난다. 수령일·마감일은 법적 기산점이라 하루 오차가 곧 분쟁 → `lib/orders` 에 `formatDateKST`·`withdrawalDeadlineKST`(Asia/Seoul 고정) 도입, 어드민·마이페이지 전부 교체. ② **허위 감사로그** — `setTracking` 이 조건에 걸려 0행 갱신됐을 때도 로그를 남겨 "바꾼 적 없는 변경"이 기록되고 있었다 → 변경 행 확인 후에만 기록.
- **리뷰 구매 검증 (#126, PR #127)**: `0007` insert 정책이 `auth.uid() = user_id` 만 봐서 **로그인만 하면 사지 않은 상품에도 별점·리뷰 작성 가능**. 별점은 상품 평균으로 노출돼 판매에 직결.
  - **서버 액션 검증만으론 못 막는다** — 실측: 미구매 계정 JWT 로 `POST /rest/v1/reviews` → **201 생성**. UI·액션을 우회해 DB API 를 직접 부를 수 있다.
  - migration `0021`: RLS insert 정책에 구매 조건 — 결제된 주문(paid·shipping·delivered)에 해당 상품 포함 시에만 허용. **pending·cancelled 제외**(주문만 만들고 결제 없이 리뷰 다는 우회 차단). `order_items(product_id)` 인덱스.
  - `hasPurchased()`(RLS 로 본인 주문만 조회, 실패 시 fail-closed) 를 서버 액션·UI 양쪽에 사용 — 미구매자에겐 폼 대신 안내.
  - **적용 후 재공격 검증**: 미구매자 insert → `403 42501` 차단 · 미결제 주문자 → 403 차단 · 구매자 → 201 허용(정상 영향 없음).

### 18단계 — 지도 (시열님 피드백, 2026-07-14)
- **네이버 지도 상시 표시 + 주소 검색 실패 수정 (#119, PR #120)**: 시열님(개발자) 피드백 — ① 네이버 지도가 지도 칸에 상시 떠 있을 것 ② 네이버·카카오에서 주소가 검색이 안 됨 ③ 구글은 잘 나옴 ④ 카카오·구글은 선택 링크.
  - **검색 실패 원인**: 링크 쿼리에 `상호+전체주소+층/호+우편번호`를 통째로 넣어(`와비사비 충남 천안시 동남구 대흥로 338 1층 2호 (31122)`) 네이버·카카오가 못 찾았다. `site.ts` 에 `roadAddress`(도로명만)·`addressDetail`·`postcode` 분리.
  - **네이버는 검색 자체를 안 거친다** — 등록 플레이스 직접 진입(`place/2012676632`). 검색 랭킹 무관, 항상 정확.
  - **상시 임베드**: Maps JS SDK, 좌표는 하드코딩 대신 **Geocoder 로 도로명 변환**(주소 변경 시 site.ts 한 곳만). `NEXT_PUBLIC_NAVER_MAP_CLIENT_ID` 필요(👤 NCP 무료, 웹서비스 URL 에 배포 도메인 등록 필수).
  - **폴백**: 키 미설정·인증 실패·지오코딩 실패·SDK 무응답 → 구글 키리스 임베드. 잘못된 키는 SDK 가 200 으로 내려오되 `maps` 네임스페이스가 없어 조용히 빈 칸이 됨 → `navermap_authFailure` 콜백 + 5초 타임아웃 가드. 실브라우저로 폴백 동작 확인.
  - **CSP 확장 필수**: #58 강제 전환 후라 안 열면 외부 지도가 전부 차단. `oapi.map.naver.com`(script/connect)·`*.map.naver.com`·`*.pstatic.net`(타일)·`maps.google.com`·`www.google.com`(frame). 프로드 위반 0 확인.
- **실키 투입 후 렌더 실패 3겹 (PR #122)**: 시열님이 NCP Client ID(`9vvft0waax`)를 Vercel env 에 넣었는데도 네이버가 안 뜨고 구글로 폴백됐다. 실측으로 원인 셋 확인.
  1. **geocoder 서브모듈 타이밍** — `maps.js` 의 load 직후엔 `naver.maps.Service` 가 아직 undefined(서브모듈은 그 뒤 별도 스크립트로 로드됨) → `Cannot read properties of undefined (reading 'geocode')`. Service 준비까지 폴링(최대 6초) 후 지오코딩, 폴백 타임아웃 10초로 상향.
  2. **CSP 가 지도 스타일 JSON 차단(진짜 범인)** — SDK 는 `nrbe.pstatic.net/styles/*.json` 을 **JSONP `<script>`** 로 로드한다. `img-src` 에만 `*.pstatic.net` 을 열어둬 스타일이 막혔고 **타일이 한 장도 렌더되지 않았다** → `script-src` 에 `*.pstatic.net`·`*.map.naver.net` 추가. 지오코딩 경로(`maps.apigw.ntruss.com`)와 SDK 오류수집(`nelo.navercorp.com`, 막으면 페이지뷰마다 CSP 위반이 `/api/csp-report` 로그를 오염)도 허용.
  3. **Geocoding API 미등록** — 403. NCP 는 Dynamic Map 과 Geocoding 이 별개 상품이라 앱에 각각 추가해야 한다 → 시열님이 콘솔에서 추가(2026-07-14 17:26).
  - **프로드 실측(2026-07-14, headless Chrome 1회 세션)**: 지오코딩 200, 네이버 타일 Contact 15·홈 26장, 구글 폴백 0, 해당 세션에서 수집된 CSP 위반 리포트 0건, 핀 위치 대흥로 338 정확. (전 브라우저·전 사용자 무위반을 뜻하지는 않는다 — 실사용 위반은 /api/csp-report 로 계속 수집된다.)
  - 교훈: 로컬 http 출처에서의 검증 결과를 프로드에 그대로 대입하면 안 된다 — SDK 가 http 출처에선 `http://` + `map.naver.net` 을, https 프로드에선 `pstatic.net` 을 쓴다. **CSP 가 걸린 외부 SDK 의 최종 확인은 배포 환경(https)에서 할 것.**
- 참고: 프로드 `/mypage` 가 비로그인에도 200 으로 보이는 건 정상 — Next 가 스트리밍 중 리다이렉트를 RSC 페이로드(`/auth?redirect=/mypage;307;`)로 내보내기 때문. 실브라우저에선 `/auth` 로 튕긴다(E2E 확인).

### 17단계 — 법적 의무 이행: 탈퇴·수신거부 (2026-07-14)
법적고지(#106)를 넣고 나니 **문서가 약속하는데 코드에 없는 기능**들이 드러났다. 문서가 거짓 진술 중이던 상태.
- **회원탈퇴 (#113, PR #114)**: 이용약관 §2·개인정보처리방침 §6 이 "마이페이지에서 탈퇴 가능"이라 명시하는데 기능이 없었음. **핵심 함정**: `inquiries.user_id` 가 `on delete cascade` 라 그냥 지우면 문의가 삭제되고 이는 방침에 적은 "소비자 불만·분쟁 기록 3년 보존"(전자상거래법)과 충돌 → migration `0018` 로 `set null` 전환(orders 는 이미 set null 이라 5년 보존 OK). 글은 보존·작성자만 익명화. 비밀글 read 정책 재정의(작성자 소실 후에도 비공개 유지 — anon 조회로 실측).
  - 안전장치: "회원탈퇴" 타이핑 확인 + **진행 중 주문(pending/paid/shipping) 차단**(계정 사라지면 배송·환불 대응 불가) + 뉴스레터 구독 동반 파기.
  - 실브라우저 E2E: 계정·배송지 삭제 / 주문·문의 user_id=null 보존 / 오타 거절 / 진행중 주문 거절 / 탈퇴 후 세션 무효.
  - CodeRabbit 후속(PR #115): `InquiryDetail.user_id` 타입을 nullable 로 정렬. FK `NOT VALID` 권고는 스킵(이미 적용·소규모 테이블, 근거 코멘트 남김).
- **뉴스레터 수신거부·어드민 (#116, PR #117)**: #108 은 수집만 만들었다 — **정보통신망법 §50(수신거부 수단 의무)** 미이행 + 대표님이 구독자를 볼 수 없음(service_role 전용). migration `0019` `unsubscribe_token`(uuid) — 링크에 이메일을 넣으면 **남의 주소로 임의 해지**가 가능해 추측 불가 토큰 사용. `/newsletter/unsubscribe?token=`(비로그인·noindex) + `/admin/newsletter`(구독자·수신동의/거부 카운트). **재구독 시 토큰 미갱신** — 갈아엎으면 기발송 메일의 해지 링크가 죽는다. E2E 전 경로 검증.

### 16단계 — 미완성 표면 정리 (2026-07-13)
"남은 작업 없음"으로 판단했다가 실제 코드를 훑어 **미완성 표면 4개**를 발견하고 전부 처리. (교훈: 큐가 비었다고 선언하기 전에 TODO·플레이스홀더·하드코딩을 직접 grep 할 것.)
- **홈 Featured 가짜 상품 (#104, PR #105)**: `const featured=[{세라믹 볼, 86,000원}...]` 하드코딩이라 DB 와 무관하게 고정 노출, 카드 클릭 시 상세가 아닌 /shop 으로 이동, **어드민이 상품을 바꿔도 홈 반영 안 됨**(대표님이 홈을 제어 못 함). `getFeaturedProducts`: is_monthly 우선 + 부족분 최신 폴백, ProductCard 재사용(실사진·실가격·상세 링크). getProducts 에 limit 옵션.
- **법적고지 3종 (#106, PR #107)**: 결제를 받으면서 약관·개인정보처리방침·환불정책 전무 + 푸터 사업자정보 없음(전자상거래법 §10·§21, 개인정보보호법). `/legal/terms·privacy·refund` 신설 — 개인정보 수집항목·위탁사(토스·Supabase·Vercel 국외이전)·보유기간은 **실제 구현 기준**으로 작성, 환불 조항도 실제 동작(배송 전 직접 취소/이후 문의)과 일치. `lib/site.ts` business 상수. **사업자등록번호·통신판매업신고번호·전화는 빈 값 → 렌더 생략**(허위 표시가 미표시보다 위험).
- **뉴스레터 껍데기 폼 (#108, PR #109)**: `<form>` 에 action 없어 제출해도 아무 일 없었음(사용자 기만). migration `0017 newsletter_subscribers`(service_role 전용 — 이메일 집합이라 유출 피해 큼) + 서버 액션(개인정보 동의 **서버 재검증**·동의 시각 기록·IP 5/시간 rate limit·upsert 멱등) + useActionState 폼. **실브라우저 E2E**: required 우회해도 서버가 동의 없이 거절, 동의 후 DB 행 생성, anon INSERT `42501` 차단·SELECT 빈 배열. 0017 원격 적용 완료.
- **지도 플레이스홀더 (#110, PR #111)**: 홈·Contact "Map will be here". 지도 SDK 는 API 키(👤)+CSP 확장 필요 → 키 없이 쓸모 있는 `MapCard`(주소+네이버·카카오·구글 바로가기, 외부 스크립트 0 → CSP 무변경). 키 받으면 카드 내부만 교체. **Contact 에 남아 있던 dl 마크업 위반**(홈은 #99 에서 수정)도 함께 고침 — Lighthouse a11y 100.

### 14단계 — 의존성·CSP 강제·SEO (2026-07-13)
- **Dependabot 첫 주간 배치 처리**: #91 minor·patch 그룹(supabase-js·lucide·radix) 머지. 메이저 3종은 `@dependabot ignore this major version` — #92 typescript 7(네이티브 컴파일러, Next 16.2 빌드 실패), #94 eslint 10(eslint-config-next 미지원, lint 실패), #93 @types/node 26(런타임 드리프트).
- **@types/node 20→24 (PR #95)**: Vercel Node 24 런타임과 타입 정렬 — 26 대신 런타임 동일 메이저로 고정.
- **CSP 2단계 강제 전환 (#58 close, PR #96)**: `Content-Security-Policy-Report-Only` → `Content-Security-Policy`(정책 문자열 동일). 근거 — 관찰기간(07-05~) 위반 보고 없음 + 실도메인 미연결이라 전환 리스크 최소 시점. report-uri 유지로 강제 후에도 위반 수집. **⚠ 프로드에서 결제위젯·이미지 업로드·소셜 로그인 1회 수동 확인 권장**(위반 시 이제 실차단).
- **SEO: OG 이미지+JSON-LD (#16 회차, PR #97)**: 링크 공유 카드 공백 해결 — `app/opengraph-image.tsx`(ImageResponse 1200×630 브랜드 톤, 내장 폰트 라틴 전용이라 영문 구성), 상품 상세 og:image=실사진. JSON-LD — 레이아웃 OnlineStore(주소·인스타), 상품 Product(KRW 가격·재고 availability, `<` 이스케이프로 script 탈출 차단). 감사에서 문제없음 확인: sitemap·robots·metadata 커버리지·next/image·next/font·skip-link.
- 참고: 프로드 최신 확인 — PR #90(인스타 피드)은 07-05 18:46 프로드 배포에 이미 반영돼 있었음(머지 타임스탬프 검증).
- **Lighthouse 실측 1차 (#16 회차, PR #99)**: 프로드 headless Chrome 측정 — 홈 89/96/100/100, /shop 71/100/100/100, 상품 77/100/100/92. 수정 3건: ① shop 첫 줄 카드 4개 `priority`(LCP lazy 지연 제거) ② 홈 Visit `dl`→`ul`(dt/dd 없는 dl 위반, a11y 96→100 재측정 확인) ③ 상품 meta description 빈값 버그 — `??` 가 빈 문자열 통과 → `||`(SEO 92 원인, JSON-LD 동일). 저ROI 보류: unused CSS/JS·bf-cache·shop TTFB(콜드스타트, 정식 배포 후 재평가). 후속(PR #100): CodeRabbit 지적 — Next 16에서 이미지 `priority` deprecated → 단일 LCP(히어로·상품 메인·헤더 로고)는 `preload`, 그리드 첫 줄은 `loading="eager"`(LCP 후보 복수 시 preload 비권장, 문서 근거). JSON-LD description 도 meta 와 동일 폴백.
- **Rate limit (#101, PR #102)**: 무방비 3곳 차단 — `/api/log-error`(무인증 DB insert → 테이블 팽창·비용, IP 10/분)·`/api/csp-report`(로그 폭주, IP 20/분)·문의 작성(도배, user.id 5/시간 — IP 우회 불가). `lib/rate-limit.ts` 2단 백엔드: Upstash env 있으면 Redis REST(INCR+EXPIRE NX 1왕복, SDK 무의존), 없으면 인메모리 폴백(인스턴스별 독립이라 느슨하나 무방비보다 나음 — env 추가만으로 승격). Redis 장애 시 fail-open. 실측 검증: 11번째 429·다른 IP 독립. 주문은 기존 DB 가드(#53)로 중복 적용 안 함, 리뷰는 unique 제약으로 한정.
- **nonce 전환 보류 결정(#16 코멘트)**: nonce 는 전 페이지 동적 렌더링 강제(정적 프리렌더 포기 = TTFB 악화) + CSP 강제 직후 결제 미검증 상태라 리스크 이중 적재 → 정식 배포 후 재평가. XSS 완화층(React 이스케이프·Zod·사용자 HTML 미렌더)은 기존 유지.

### 10단계 — 어드민 역할(role) 권한 분리 (2026-07-04, 진행중)
- 대표님(클라이언트)이 직접 상품·게시판 관리하도록 어드민/일반 회원 분리. (호칭: 이후 문서엔 "대표님")
- **role 기반 권한 (PR #47, `feat/admin-role-gating`)**: migration `0008` `profiles.role`(user/admin).
  - ⚠ **권한 상승 차단**: profiles `own profile update` RLS로 사용자가 자기 role 을 admin 으로 self-update 가능했음 → 테이블 UPDATE 회수 + `name` 컬럼만 재부여. role 변경은 service_role 만.
  - requireAdmin: role='admin' 체크(+ADMIN_EMAILS 부트스트랩 fallback), isAdmin() 헬퍼. auth store/provider가 로그인 시 role 읽어 isAdmin 플래그. 헤더 Admin 링크(어드민만).
  - **어드민 계정 생성**: `admin@gmail.com`(Admin API로 생성, role=admin 승격). ⚠ 비번 123456 = 개발용, 운영 전 반드시 교체.
  - **보안 실테스트 통과**: 사용자 JWT role 자가변경 → 403 차단, name 수정 허용, 비로그인 /admin → 307 /auth. tsc·lint·build clean.
- 대표님 계정은 배포 사이트서 직접 회원가입(기본 user) → 이후 role='admin' 승격 필요(가입만으론 어드민 X).
- **Vercel 운영 env**: `SUPABASE_SERVICE_ROLE_KEY` 배포 환경변수 등록+재배포 → 배포 사이트 어드민 쓰기 활성. 프로덕션 도메인(`wabi-sabi-nu.vercel.app`)은 재배포에도 불변.
- **어드민 UX (PR #48)**: 대표님 피드백 — 이미지 첨부 영역 호버 무반응. 파일 입력 버튼형 스타일(file: variant)+cursor-pointer, 링크형 버튼(저장/지정/노출/삭제/이미지추가) hover 색 전환. 어드민 전 페이지 일관 적용.
- **상품 등록 폼 버그 (PR #49)**: 대표님 피드백 — 등록 시 입력값 소실 + 이미지 조용한 실패.
  - 원인: ①React 19 서버 액션 후 폼 자동 리셋+피드백 부재 ②업로드 실패 무시(continue) — 주원인 버킷 5MB 제한 < 폰 사진.
  - 수정: 버킷 12MB 상향(인프라). uploadProductImages 실패 수집, 액션 ActionResult 반환(useActionState). ProductCreateForm — 실패 시 값 유지/성공 시만 초기화, pending·결과 메시지. ProductImageAdder 행별 인라인 결과. test 상품 4건 정리.
  - **배포 검증 통과**: 등록 성공 메시지·shop/상세/장바구니 이미지 렌더 확인(운영 사이트).
- **장바구니→체크아웃 버그 (PR #50)**: 로그인 상태인데 주문하기 시 로그인 페이지 노출.
  - 원인: cart 주문하기가 `/auth?redirect=/checkout` 하드코딩(초기 TODO 잔재) + /auth 가 로그인 사용자 미감지.
  - 수정: cart→/checkout 직행(비로그인은 checkout 가드가 처리). /auth 로그인 상태면 목적지로 replace. redirect 내부경로만 허용(open redirect 차단).
- **장바구니 배지 (PR #51)**: 배지가 수량 합계(1종×2=2)로 표시 → "담긴 상품 종류 수"(items.length)로 변경.

### 9단계 — 운영 준비: 브랜드 로고·이미지 업로드 (2026-07-01, 완료)
- 더미 게시판(공지3·문의2·리뷰2) 전부 삭제(운영 클린 시작). 실상품 데이터는 추후 입력 예정.
- **브랜드 로고 (PR #45, `feat/brand-logo`)**: 매장 실물 로고 에셋 반영. KakaoTalk 원본(003/004 풀·001 워드마크·005 심볼)에서 Pillow로 투명화·트림·리사이즈 → `public/brand/`(logo-mark·logo-wordmark·icon). 헤더(わ→브러시 심볼)·히어로(텍스트→심볼+워드마크 이미지)·파비콘(기본 favicon.ico 제거→app/icon.png). 검증: build clean, 이미지 200.
- **상품 이미지 업로드 (PR #46, `feat/admin-image-upload`)**: Storage 버킷 `product-images`(public·5MB·png/jpg/webp). `lib/storage.ts`(upload/delete, service_role). createProduct 다중 업로드→images / addProductImages·removeProductImage(스토리지 동기삭제). 어드민 목록 썸네일+삭제+추가 폼. next.config `serverActions.bodySizeLimit:15mb`. **파이프라인 실테스트**: 업로드→공개URL 200→shop next/image 렌더 200.
- 참고: 매장 방문사진 10장은 채팅 전용(디스크 미저장)이라 미반영. `보안_체크리스트.md`는 민감정보 생기면 추적해제 예정.

### 8단계 — 대표님(김종순) 피드백 반영 (2026-06-30~07-01, 완료)
- 출처: 대표님(김종순) KakaoTalk 피드백(Shop IA·로고).
- **카테고리 재구성 (PR #39, `feat/shop-categories-monthly`)**:
  - `tableware/objects/craft/gifts`(4) → `plate/bowl/cup/cutlery/life/gift/craft`(7).
  - **이 달의 상품**: 종류와 직교 → `products.is_monthly` 플래그로 분리(접시면서 동시에 monthly 가능). All=필터없음, monthly 탭=`is_monthly` 필터.
  - 변경: `lib/site.ts`(categories 7+MONTHLY_SLUG), `queries/products.ts`(monthly 분기), `shop/page.tsx`(탭), `admin/products`(체크박스·toggleMonthly), migration `0004`+seed/apply_all.
  - DB: 원격 Supabase에 0004 적용 + 더미 8상품 remap(monthly 3) 완료.
  - **테스트 통과**: Shop 9탭 필터 전부 정상(직교 검증 포함), tsc·lint·build clean.
- **로고 축소 (PR #40, `fix/hero-logo-scale`)**: 홈 히어로 `WABI-SABI` h1 `text-2xl/3xl`→`text-xl/2xl` (わび-さび 대비 비중↓).
- **공지 게시판 (PR #41, `feat/notice-board`)**: migration `0005` notices+RLS공개읽기. `/notice`(목록)·`/notice/[id]`(상세)·`/admin/notices`(등록·삭제)·푸터 링크. DB 0005 적용+공지 3건 시드. **테스트 통과**(목록·상세 200, 없는 id 404, tsc·lint clean).
- **게시판 범위 확정**(user): 문의=비밀글+관리자답변+로그인필수 / 리뷰=별점(1~5)+상품상세노출(구매검증·사진 제외).
- **문의 게시판 (PR #43, `feat/inquiry-board`)**: migration `0006` inquiries(is_secret·answer). RLS — 공개글 누구나/비밀글 작성자만(비작성자엔 목록서도 숨김), 작성=로그인 본인, 답변=service_role. `/inquiry`·`/inquiry/new`(로그인필수)·`/inquiry/[id]`·`/admin/inquiries`. **테스트 통과**(비로그인 목록=공개만, 비밀 상세 404, new→auth 307). ※#42는 base브랜치 삭제로 닫혀 #43으로 재생성.
- **리뷰 게시판 (PR #44, `feat/review-board`)**: migration `0007` reviews(rating1~5·author_name 스냅샷·상품당 1인1리뷰 unique). RLS — 공개 읽기/로그인 본인 작성·삭제/어드민 service_role 삭제. 상품상세(`/shop/[id]`)에 평균별점+목록+작성폼, `/review` 전체목록, `/admin/reviews`, 푸터 링크. 컴포넌트 `Stars`·`ReviewSection`. **테스트 통과**(상품상세·/review 200, 별점 렌더, unique 충돌=1인1리뷰 검증).
- **머지 완료**: #39·#40·#41·#43·#44 전부 squash 머지 → main (#42는 base삭제로 #43 재생성). 충돌(apply_all·footer·admin nav) 해소. main 통합 build clean(전 라우트 정상). DB 0004~0007 적용 완료.
- **대표님 피드백 전 항목 완료**: 카테고리7+monthly·로고축소·게시판3종(공지/문의/리뷰).
- 푸터 고객안내 nav: 공지사항·문의·리뷰. 어드민 nav: 상품·주문·공지·문의·리뷰.

### 7단계 — 결제위젯·버그픽스·비기능 보강 (2026-06-21)
- 토스 결제창 → **결제위젯**(인페이지) 전환 (#32).
- 버그픽스: /checkout 인증 로딩 레이스(로그인인데 /auth 튕김) (#33), 결제 성공 새로고침 멱등 (#35).
- 비기능(#16): 동적 사이트맵 상품 포함(#34), 로딩 스켈레톤(#36), /checkout 색인 제외(#37).
- 결제 E2E 설정 완료: 마이그 0003 적용·토스 test키 입력 확인.

#### 보안 자가점검 (1인 개발 — Claude 상시 점검)
- ✅ 시크릿: `.env.local` gitignore, anon=publishable(공개 안전), service_role 미보관.
- ✅ RLS: 전 테이블 보호, 카탈로그만 공개읽기. 주문 금액 서버 재계산(클라 변조 차단). 검색 supabase-js 파라미터화(SQLi 없음). React 자동 이스케이프(XSS 없음). 어드민 env 허용목록+service_role 서버전용.
- ⚠️ TODO(운영 전): ①`confirm_order` RPC 직접호출 가능 → 토스 웹훅+service_role 확정으로 대체 ②rate limit(Upstash) 미적용 ③방치된 pending 주문 정리 잡 ④소셜 provider·라이브 결제키.

### 6단계 — 퍼블리싱 완료 + 핵심기능 실연동 (2026-06-19~20)
- Supabase 실연동: 프로젝트(zeqtfrwjnlckyinjxjcu) 8테이블+RLS+상품 시드.
- 기능 PR(전부 CI+CodeRabbit 통과 후 머지): #17 a11y · #18 장바구니 · #19 상품조회 · #20 인증 · #21 상품상세 · #22 마이페이지 · #23 검색·정렬 · #24 위시리스트 · #25 관련상품 · #26 주문내역 · #27 체크아웃UI · #28 브랜드 · #29 소셜로그인 · #30 어드민.
- **퍼블리싱 전 페이지 완료**(18라우트): 홈·shop·상세·about·contact·cart·checkout·auth(+callback)·mypage(+orders/wishlist)·admin(+products/orders)·404/error/robots/sitemap.
- 결제 PG=토스페이먼츠 확정(실 연동은 후속). 어드민 쓰기=service_role(키 추후).
- 남은 이슈: #10(결제 백엔드)·#16(비기능 지속). 16건 중 14건 클로즈.

### 5단계 — SEO·에러처리 + GitHub 연동 (2026-06-18~19)
- not-found(404)·error 바운더리·robots.ts·sitemap.ts 추가 (11라우트).
- git을 프로젝트 루트로 통합(web/.git 제거 후 root init), .env 미추적 확인.
- GitHub: repo `ParkSiYeol3/wabi-sabi`(private) 생성·push(`gh repo create`). 라벨 10종, WSB 이슈 16건(#1~#16) 등록.
- PR 흐름 시연: `feat/a11y-skip-link` → PR #17(스킵 링크, WCAG 2.4.1). main은 미병합 상태(리뷰 대기).
- .github PR/이슈 템플릿 + docs/github-issues-plan.md.

---

## 4. 현재 코드 구조 (`web/`)

```
web/
├── src/
│   ├── app/
│   │   ├── layout.tsx        # 폰트·메타데이터·AuthProvider
│   │   ├── page.tsx          # (기본 스캐폴딩 — 미수정)
│   │   └── globals.css       # 디자인토큰 (@theme)
│   ├── components/
│   │   └── auth-provider.tsx # 세션→Zustand 동기화 (env 없으면 스킵)
│   ├── lib/supabase/
│   │   ├── client.ts         # 브라우저 클라이언트
│   │   ├── server.ts         # 서버 클라이언트 (async cookies)
│   │   └── proxy.ts          # 세션 갱신 헬퍼 (env 없으면 스킵)
│   ├── store/
│   │   └── auth.ts           # Zustand 인증 스토어
│   └── proxy.ts              # Next 16 proxy 엔트리 (matcher 설정)
├── supabase/
│   ├── migrations/
│   │   ├── 0001_init.sql     # 8테이블
│   │   └── 0002_rls.sql      # RLS 정책
│   └── seed.sql              # 카테고리 4개
├── .env.local                # (gitignore) Supabase 키 — 아직 빈값
└── .env.example              # 키 템플릿
```

### DB 스키마 (8테이블)
- **확정:** profiles(auth.users 확장+자동생성 트리거), products, orders, order_items, gift_options
- **⚠️ 초안(확정 필요):** categories, wishlist, addresses
- RLS: categories/products 공개읽기, 나머지 소유자 전용.

---

## 5. 기능 요구사항 진행 (WSB) — 2026-07-17 기준

| 그룹 | ID | 상태 |
|------|-----|------|
| 회원 | WSB-001~006 | ✅ 완료 — 가입·로그인·소셜·프로필·배송지·**회원탈퇴**(#113, 주문 5년·문의 3년 익명화 보존) |
| 상품 | WSB-007~012 | ✅ 완료 — 목록·검색·카테고리·정렬·상세·갤러리·관련상품·품절·**재입고 알림**(#166) |
| 주문 | WSB-013~019 | ✅ 완료 — 장바구니(서버 동기화)·결제(토스)·재고 동시성(0010)·취소/환불(0011)·배송완료(0020)·주문 상세(#137)·**저장 배송지 불러오기**(#162) |
| 브랜드 | WSB-020~022 | ✅ 완료 — 홈·About·Visit(지도)·뉴스레터·인스타 피드. 소개 문구는 **어드민에서 편집**(0026) |
| 어드민 | WSB-023~026 | ✅ 완료 — 상품·주문·공지·문의·리뷰(신고/숨김)·뉴스레터·콘텐츠·감사로그·에러로그·**대시보드 요약**(0024·0025) |
| 게시판 | 3종 | ✅ 완료 — 공지(0005)·문의(0006, 비밀글+답변)·리뷰(0007, 구매자만 0021 + 신고/숨김 0022·0023) |

**남은 것은 코드가 아니라 외부 의존**(토스 심사·실상품 사진·대표님 반품정책 확정). 브랜드 화면은 #197~#228 로 재편 — 홈=철학 여정(헬릭스+侘寂選), About=고르는 기준.

---

## 6. ⚠️ 미해결·확정 필요 (블로커)

**해결 완료**
1. ~~Supabase 프로젝트 미생성~~ → 프로젝트 `zeqtfrwjnlckyinjxjcu` 가동. 마이그 **0001~0028** 적용.
2. ~~휴무일 충돌~~ → 수요일 휴무.
3. ~~결제 PG 미정~~ → 토스페이먼츠 연동(결제창 방식).
4. ~~nav 명칭~~ → Shop / About / Contact.
5. ~~categories 컬럼~~ → 7종+is_monthly.
6. ~~게시판 3종 스키마~~ → 공지·문의·리뷰 완료.
7. ~~사업자등록번호·통신판매업신고번호~~ → **2026-07-16 대표님 확인·입력 완료**(411-74-00574 / 2026-충남천안-1000). 푸터·법적고지 노출 중.
8. ~~도메인 미정~~ → **wasa.kr 구매**(2026-07-16, 호스팅케이알 2년, 등록자=대표님) → **2026-07-19 개통 완료**(A `@`→76.76.21.21 · www CNAME · Vercel Primary=apex · 함수 리전 icn1).
9. ~~메일 발송 개통~~ → **Resend 도메인 Verified + 실발송 200**(2026-07-19). `noreply@wasa.kr` 로 주문·배송·문의답변·재입고 알림 발송 가능.

**진행 중 — 외부 의존 (2026-07-19 기준)**
- 🔴 **토스 전자결제 심사**(👤 대표님 명의로 신청 완료, 2026-07-19): 정산계좌·신청인 명의가 일치해야 해 대표님이 직접 접수. 승인 후 시열님이 live 키 전환 — `NEXT_PUBLIC_TOSS_CLIENT_KEY`(live_ck)·`TOSS_SECRET_KEY`(live_sk) **Production 환경만** 교체 → 웹훅 `https://wasa.kr/api/webhooks/toss` 등록 → Redeploy → 소액 실결제+취소 1회 검증. 심사 기간 중 `test` 더미 상품 비공개 권장(심사역 사이트 검토 대비).
- 🟡 **실상품 사진**(👤): 사진 등록 상품 일부가 테스트 이미지(찻잔세트에 도롱뇽 등). `test` 상품 정리 필요. — 홈은 #225 로 상품 카드가 빠져 사진 의존이 줄었지만, shop·상세는 그대로 필요.
- 🟡 **법적고지 반품 배송비 금액·회수 방법**(👤 대표님) · 개인정보보호책임자 지정(미기재 시 대표자 폴백) · **About "고르는 기준" 카피 검토**(#228 초안).
- 🟡 **운영 설정**(👤): DMARC 레코드 · `CRON_SECRET` · Supabase Password Requirements · 이메일 인증 ON(Resend SMTP).

---

## 7. 다음 단계

**토스 승인 직후**
1. live 키 Production 교체 → 웹훅 등록 → Redeploy
2. 소액 실결제 1회 + 즉시 취소 — 주문 생성·웹훅 수신·재고 차감·취소 환불까지 한 바퀴 확인
3. 실패 시 로그: Vercel 함수 로그 + `admin/error-logs` + 토스 개발자센터 결제내역 3중 대조

**정식 오픈 전**
- 실상품 사진·데이터 정리(`test` 상품 삭제), 시드 콘텐츠 → 실콘텐츠
- 대표님 카피 검토 반영(About 고르는 기준 · 반품 정책 문구)
- **`/security-review` 1회** + 모바일 실기기 전수 + 백업·사고대응 절차 확인
- 접근성 전수 점검, Lighthouse 저ROI 항목(unused CSS/JS·TTFB)

**오픈 후 재판단**
- **CSP nonce 전환**(#58 후속) — 33단계 참조. 정적 렌더 포기 비용 > 현재 이득이라 보류.
- 보류(대표님 조율): 엔소 로고 교체 · KR 폰트 Noto Serif KR 통일 · 공방/작가 소개 섹션(자료 수급 후)
- 홈 상품 카드 복원이 필요해지면 #213~#224 시점 `helix-journey` 회수(53단계 참조)

> 보안 메모: Figma 토큰·Supabase 키는 `.env.local`(gitignore)에만. 채팅 노출 토큰(Resend 키 등)은 작업 후 revoke 권장.
