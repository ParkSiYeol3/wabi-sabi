# WABI-SABI 보안 체크리스트 (정식 오픈용)

> 실사업 결제 이커머스(wasa.kr). 한국 개인정보보호법·전자상거래법 + 결제 보안 대상.
> 상태: ✅완료 / 🔧진행중 / ⬜미착수 / 👤사용자자원필요
> 최종: **2026-07-19** (마이그 0001~0032 적용 기준, wasa.kr 라이브)

---

## 0. 이미 적용된 기반 보안 ✅
- [x] **RLS** 전 테이블 — 소유자 전용: profiles/orders/order_items/gift_options/wishlist/addresses/cart_items/**restock_subscriptions**(0027). 공개읽기: products/categories/notices/**site_content**(0026, 쓰기는 service_role만). 조건부: **reviews**(0021 구매자만 insert, 0023 공개=hidden false OR 본인), **inquiries**(비밀글은 작성자만). service_role 전용(정책 없음): admin_audit_log(0013)·client_error_log(0014)·newsletter_subscribers(0017)·**review_reports**(0022, 신고자 신원 비공개).
- [x] **결제 금액 서버 재계산** (createPendingOrder가 DB 가격으로 재계산 — 클라 변조 차단)
- [x] **시크릿 격리**: anon=publishable(공개안전), service_role 미보관, `.env*` gitignore
- [x] **XSS 방어**: React 자동 이스케이프. `dangerouslySetInnerHTML` 은 JSON-LD 1곳뿐 — `<` 이스케이프로 script 탈출 차단(#16). 메일 HTML 은 `escapeHtml` 경유.
- [x] **SQL 인젝션 방어**: Supabase 파라미터 바인딩(ilike 포함)
- [x] **카드정보 비취급**: 토스페이먼츠 위임 → PCI-DSS 직접 범위 회피
- [x] **HTTPS**: Vercel 자동
- [x] **CI 코드리뷰**: CodeRabbit + `npm audit`·lint·tsc·build 게이트

> **이 프로젝트에서 반복 검증된 원칙**: UI·서버 액션 검증만으론 못 막는다. 사용자는 PostgREST 로 DB API 를 직접 호출할 수 있다(실측: 미구매자 리뷰 insert 201 → 0021 로 403). **쓰기 제약은 반드시 RLS 에.** 신규 테이블마다 anon 직접 공격을 실측해 확인한다.

---

## 1. 🔴 P0 — 결제 보안 (최우선)
- [x] **결제 확정 서버 전용화** (#52, 2026-07-05) — 구 `confirm_order` drop(PUBLIC EXECUTE 우회까지 실테스트로 확인 후 제거). `confirm_order_paid`(금액검증+멱등) service_role 전용. 승인 금액=DB total_price(클라 불신). 사용자 JWT 호출 404/403 검증됨.
- [x] **토스 웹훅** (#52) — `POST /api/webhooks/toss`. 페이로드 불신, 토스 API(시크릿) 재검증이라 위조 무해. 👤 남은 설정: 토스 개발자센터 웹훅 URL 등록.
- [x] **방치 pending 정리** (#53) — cron `/api/cron/cleanup-pending`(24h→cancelled, CRON_SECRET 보호, 03:00 KST). 👤 Vercel env `CRON_SECRET` 추가.
- [x] **재고 동시성·중복결제** (#56, 2026-07-05) — migration 0010: 확정 시 주문·상품 행 FOR UPDATE + 재고 검증, 부족 시 자동 취소·환불(초과판매 차단). 웹훅 무한 재시도 차단(final). 적용·검증 완료.
- [ ] **라이브키 분리** 👤 — test↔live env 분리, 가맹점 계약 후 전환. successUrl/웹훅 도메인 운영값
- [x] **취소·환불 처리** (#57, 2026-07-05) — 배송 전(paid) 전액 취소: RPC(잠금·재고 복원) 먼저→토스 환불 나중(배송 경합 봉쇄), 멱등. 주문 내역 취소 버튼. 부분취소는 후속(운영 요구 시).

## 2. 🔴 P0 — 접근 통제
- [x] **주문 생성 남용 가드** (#53) — DB 기준 rate limit: 사용자당 pending≥5 or 1시간 10건 차단(Upstash 불요). 로그인/가입 rate limit은 Supabase Auth 기본 제한 의존 — 강화 필요시 Upstash 추후.
- [x] **어드민 권한 정식화** (#47, 2026-07-04) — `profiles.role`(user/admin) + requireAdmin. **권한상승 차단**: profiles UPDATE 회수 후 name만 재부여(사용자 role 자가변경 403 실테스트). 감사로그는 미착수.
- [x] **service_role 키 운영 보관** — Vercel env 등록(서버 전용), 클라 노출 없음.
- [x] **open redirect 차단** (#50) — /auth redirect 파라미터 내부경로만 허용(`//host` 포함 차단).
- [x] **주문 쓰기 서버 전용화** (#62, P0, 2026-07-05) — 0003 RLS 의 order_items 직접 insert 로 주문 항목 조작 가능(확정은 total_price 만 검증). 0012 로 orders/order_items/gift_options 사용자 insert 회수 + createPendingOrder service_role 쓰기. **적용·실테스트 완료**: 사용자 JWT insert 3종 42501 차단, 본인 SELECT·service_role 쓰기 정상.
- [ ] **Vercel WAF / Attack Challenge Mode** 👤 — 봇·DDoS (Vercel Pro)
- [x] **어드민 액션 감사로그** (#78, 2026-07-05) — 0013 `admin_audit_log`(service_role 전용, RLS 정책 없음). 상품·주문·공지·문의·리뷰 액션 배선, `/admin/audit` 조회. 로깅 실패해도 원 작업 진행.
- [x] **어드민 개발계정 비번 교체** (2026-07-05) — `admin@gmail.com` 123456 → 강력 랜덤(Admin API). 구 비번 거부·신 비번 로그인 검증. test@test.com 은 유지.
- [x] **리뷰 구매 검증** (#126, 0021, 2026-07-15) — 0007 insert 정책이 `auth.uid()=user_id` 만 봐서 **미구매자가 PostgREST 직접 insert 로 리뷰 작성 가능**(실측 201). 별점은 상품 평균에 반영돼 판매 직결 → RLS 에 구매 조건(paid·shipping·delivered 주문에 해당 상품). **재공격 403 차단 확인**, pending 주문 우회도 봉쇄.
- [x] **리뷰 모더레이션 RLS** (#141, 0022·0023, 2026-07-15) — soft-hide(`reviews.hidden`) + 신고(`review_reports`). 공개 read 는 `hidden=false OR 본인`. **신고 insert = 로그인 본인·자기 리뷰 신고 불가·중복 차단**, `review_reports` 는 select 정책 없음 → **신고자 신원 비공개**. 실측: 숨김 리뷰 anon 조회 0 · 신고내역 anon `[]` · 자기외 insert 401.
  - 신고 사유는 **서버에서 enum 재검증** — Server Action 은 UI 를 거치지 않고 직접 호출 가능해 `<select>` 제약만으론 임의 텍스트가 저장된다.
- [x] **어드민 요약 RPC 권한 제한** (#143·#145, 0024·0025, 2026-07-16) — `admin_dashboard_summary` 는 `security definer` 라 RLS 를 우회한다 → **execute 를 service_role 로만**(`revoke from public, anon, authenticated`). 실측: anon 호출 `401 permission denied`(매출·미답변 수 노출 차단).
- [x] **편집 콘텐츠 쓰기 차단** (#160, 0026, 2026-07-16) — `site_content` 공개 read / **write 정책 없음**(service_role 만). 실측: anon write 401, anon read 200.

## 3. 🟡 P1 — 앱 하드닝
- [x] **보안 헤더** (#55, 2026-07-05) — HSTS·X-Frame-Options DENY·nosniff·Referrer-Policy·Permissions-Policy 적용(실서빙 검증).
- [x] **CSP** (#58, 2026-07-13 close) — 1단계 Report-Only(07-05) 관찰 기간 위반 0 확인 → **2단계 강제 전환 완료**(`Content-Security-Policy`). 허용목록: 토스·Supabase·네이버지도(`oapi.map.naver.com`·`maps.apigw.ntruss.com`·`*.pstatic.net`·`*.map.naver.net`)·구글맵 frame. report-uri 유지로 강제 후에도 위반 수집.
  - ⚠️ **교훈(#119)**: 지도 타일이 한 장도 안 떴는데 원인은 CSP `script-src` 에 `*.pstatic.net` 누락(스타일 JSON 을 JSONP `<script>` 로 로드). **외부 SDK 는 로컬(http)에서 검증 불가** — 출처별로 다른 호스트/스킴을 쓴다. https 배포 환경에서 확인할 것.
  - 🔒 **nonce 전환은 보류**(2026-07-17 검토, 33단계): Next 16 nonce 는 **동적 렌더 페이지에만** 주입된다. /about·/legal/* 등 정적 페이지가 있어 nonce 를 켜면 그 페이지 스크립트가 차단되거나 **전 페이지 동적 렌더 강제**(TTFB·비용↑). 얻는 건 `unsafe-inline` 제거인데 XSS 표면은 이미 React·JSON-LD 이스케이프로 차단됨 → **정식 오픈 후 트래픽·리스크 보고 재판단**.
- [x] **입력 검증(Zod)** (2026-07-05) — 1차 결제·주문(#60, 중복 상품 id 거부), 2차 어드민(#73, 음수 재고 차단·numField 0폴백 제거), 3차 사용자 게시판·주소(#75). 공용 `lib/validation.ts`(uuidSchema·parseUuid·numField).
- [ ] **이메일 인증 ON**(운영) 👤 — dev OFF였음, 운영 전 ON + 커스텀 SMTP(Resend)
- [🔧] **비밀번호 정책·MFA** — 정책 강화(2026-07-05): 회원가입 8자+영문·숫자 필수, 폼 힌트 표시. 👤 Supabase 대시보드 Password Requirements 동일 기준 설정 권장(서버 강제). MFA·이상징후 차단은 후속.
- [x] **의존성 취약점** (#63, 2026-07-05) — Dependabot(npm 주간·Actions 월간) + CI `npm audit --omit=dev --audit-level=high` 게이트.
- [x] **CSRF** (2026-07-05 점검) — Server Actions(Next 16.2.10) Origin=Host 기본 검증, `serverActions` 에 allowedOrigins 확대 없음(same-origin 유지). 외부 POST 3종: cron(CRON_SECRET Bearer), 토스 웹훅(페이로드 불신·토스 API 재검증 → 위조 무해), csp-report(무인증 정상·content-type 415 가드 추가). **취약점 없음.**

## 4. 🟡 P1 — 법적 컴플라이언스 (한국 필수)
- [x] **개인정보보호법** (#106, 2026-07-13) — `/legal/privacy` 개인정보처리방침. 수집·이용 동의(뉴스레터는 동의 서버 재검증 + 동의시각 기록, 0017). **보관·파기 정책 구현**(#113, 0018): 회원탈퇴 시 계정·배송지·위시·장바구니·리뷰 삭제, **주문 5년·문의 3년 익명화 보존**(전자상거래법 보존의무와 파기요구 충돌 해소 — `inquiries.user_id` cascade→set null). 접근권한 통제·감사로그(0013).
  - [ ] 개인정보 **보호책임자 지정**(👤 대표님) — `lib/site.ts` privacyOfficer 빈값, 미기재 시 대표자 폴백.
- [x] **전자상거래법** (#106) — `/legal/terms` 이용약관 · `/legal/refund` 청약철회·환불정책 · **§10 사업자정보 표시 완료**(2026-07-16): 상호·대표자·주소·전화(0507-1430-3085)·이메일·**사업자등록번호 411-74-00574**·**통신판매업신고 2026-충남천안-1000**. 렌더는 값이 있을 때만(허위표시 금지).
  - **청약철회 기산점**(0020, #124): 수령일(`delivered_at`) 기록 + 7일 마감 안내. **날짜는 KST 고정**(`formatDateKST`) — 서버가 UTC 라 법적 기산점이 하루 어긋나면 곧 분쟁.
  - [ ] 반품 배송비 금액·회수 방법 확정(👤 대표님)
- [x] **개인정보 처리**: 비밀번호 bcrypt(Supabase 자동), 민감정보 최소수집(재입고 알림도 **이메일 별도 수집 없이 계정 이메일 사용** — 0027), 결제대행 위탁(토스)
- [ ] **본인인증**(필요시): 휴대폰/실명 인증 — 현 매출 규모에선 미적용

## 5. 🟢 P2 — 운영·모니터링
- [🔧] **모니터링**: 클라이언트 에러 수집 완료(2026-07-05) — 0014 `client_error_log`(service_role 전용) + `POST /api/log-error`(content-type·크기 가드) + error.tsx·global-error.tsx 전송 + `/admin/errors` 조회. Sentry(외부, 👤)·결제실패 알림·Supabase Audit Logs 잔여.
- [ ] **백업·복구**: Supabase 자동백업 확인, 사고대응 절차
- [🔧] **정기 점검**: **RLS 전수 우회 매트릭스 실측 완료(2026-07-18, 유출 0)**. 출시 직전 `/security-review` 1회 + 라이브 결제 전환 후 실결제 검증 잔여. 외부 모의해킹(pentest)은 규모 확대 시.
- [ ] **키 로테이션**: API 키 주기적 교체 정책

---

## 진행 로그
- 2026-06-21: 체크리스트 작성. P1 보안헤더·Zod·RLS감사·Dependabot 착수 예정. P0 결제웹훅·rate limit은 사용자 자원(토스 웹훅/service_role/Upstash) 확보 후.
- 2026-07-04: 어드민 role 분리+권한상승 차단(#47). 상품 업로드 실패 노출(#49). open redirect 차단(#50). Vercel service_role env 등록.
- 2026-07-05: **P0 결제 보안 핵심 완료** — 확정 서버 전용화+웹훅(#52), pending 정리 cron+주문 남용 가드(#53). 구 confirm_order PUBLIC EXECUTE 우회 실테스트로 발견·drop. 남은 P0: 재고 동시성, 라이브키, 환불 플로우. 남은 설정👤: 토스 웹훅 등록, CRON_SECRET.
- 2026-07-05(2): 매장정보 확정 반영(#54). 보안 헤더 5종(#55) — P1 잔여는 CSP·Zod·이메일인증·Dependabot.
- 2026-07-13: **CSP 2단계 강제 전환**(#58 close, PR #96) — 관찰기간 위반 0 + 실도메인 미연결이라 전환 리스크 최소 시점. 법적고지 3종·푸터 사업자정보(#106). 뉴스레터 동의 서버 재검증(#108). 죽은 도메인 링크 제거(#135) — `wasa.kr` 하드코딩 7곳이 미등록 도메인을 가리켜 sitemap·robots·메일 링크가 전부 접속 불가였음 → `lib/site-url.ts`(server-only, 프로토콜 정규화).
- 2026-07-14: 회원탈퇴(#113, 0018) — 파기요구 vs 전자상거래법 보존의무 충돌 해소(주문 5년·문의 3년 익명화). 뉴스레터 수신거부 토큰(#116, 0019 uuid — 이메일 링크에 id 를 쓰면 임의 해지 가능).
- 2026-07-15: **리뷰 구매 검증**(#126, 0021) — PostgREST 직접 insert 로 미구매자 리뷰 작성 가능(실측 201)했던 구멍 차단. 배송완료·청약철회 기산점 KST 고정(#124, 0020). 주문/배송/문의답변 메일(#129·#133) — **문의 답변 메일에 본문 미포함**(비밀글 평문 유출 방지), 중복 발송은 확정 RPC 가 `confirmed` 일 때만 발송으로 차단.
- 2026-07-15(2): **리뷰 모더레이션**(#141, 0022·0023) — 신고자 신원 비공개, 자기 리뷰 신고 차단, 신고 사유 서버 enum 재검증.
- 2026-07-16: **어드민 요약 RPC**(0024·0025) `security definer` → execute service_role 한정(anon 401 실측). 편집 콘텐츠(0026) write 차단. **사업자등록번호·통신판매업신고번호 입력 완료**(#106 close) → 전자상거래법 §10 이행. 도메인 `wasa.kr` 구매(등록자=대표님).
- 2026-07-16(2): 재입고 알림(#166, 0027·0028) — 이메일 별도 수집 없이 계정 이메일 사용(검증·스팸 회피). CodeRabbit 이 **발송 실패에도 구독을 삭제해 재시도 불가**해지는 Critical 을 지적 → 성공분만 해제.
- 2026-07-17: **CSP nonce 전환 검토 후 보류** — Next 16 nonce 는 동적 렌더에만 주입되는데 정적 페이지가 있어 전 페이지 동적 렌더를 강제하게 됨(TTFB·비용↑). XSS 표면은 이미 차단돼 이득 < 비용 → 오픈 후 재판단. 문서 3종(진행기록·보안·디자인) 최신화. 폼 접근성 전수(#176).
- 2026-07-18: **홈 성능 캐시 도입 시 anon 공개 클라이언트 격리 확인**(#178) — `lib/supabase/public.ts` 는 공개 데이터 전용, 실측으로 orders·addresses 격리(`[]`) 확인. **OWASP Top 10:2025 대조** — A01(권한통제)=RLS, A03(공급망)=npm audit CI+Dependabot(next/postcss moderate 2건은 패치 비현실+빌드타임 한정이라 실질 영향 없음, 업스트림 대기), Misconfig=service_role server-only.
- 2026-07-18: **RLS 전수 우회 매트릭스 실측** — anon JWT 로 전 18개 테이블에 SELECT/INSERT/UPDATE/DELETE 4종 공격.
  - **SELECT**: 공개 read(products·categories·notices·reviews·site_content)만 노출, 나머지 12개 전부 0건 격리(profiles 포함 — 정책이 `auth.uid()` 기반으로 작동함을 증명).
  - **INSERT**: 18개 전부 401 차단(anon write 정책 없음).
  - **UPDATE/DELETE**: 공개 테이블에도 write 정책이 없어 대상 0행(변경 불가).
  - **결과: 유출 0 · 의도치 않은 write 0.** 신규 테이블(review_reports 0022·site_content 0026·restock_subscriptions 0027) 포함 전부 안전. 소유자 테이블은 `auth.uid()=user_id` 정책이라 인증 사용자도 자기 uid 만 접근(구조적 IDOR 방어).
- 2026-07-18(2): **보안 더블체크 5방향 — 취약점 0.**
  - ① **클라 번들 시크릿 노출 검사**: 빌드 `.next/static` 전량(34파일 1MB) grep — service_role·Resend·Toss 시크릿 **0건**, anon 키만 정상 노출. `service_role`·`VERCEL_PROJECT_PRODUCTION_URL` 문자열 흔적조차 없음.
  - ② **인증된 타인 IDOR**(A01 핵심): 로그인 JWT 로 소유자 테이블 조회 → 본인 것만(타인 소유 0건), service_role 전용 테이블(admin_audit_log·client_error_log·newsletter·review_reports)은 인증 JWT 로도 격리(0건). 어드민 권한은 service_role 로만 행사되는 설계 확인.
  - ③ **어드민 서버 액션 가드**: admin/*/actions.ts 16개 함수 전수 `requireAdmin` 보유(누락 0).
  - ④ **security definer RPC 권한**: admin_dashboard_summary·confirm_order_paid·cancel_paid_order 를 anon/authenticated 로 직접 호출 → 전부 `42501` 차단(401/403). **구 취약 `confirm_order` 는 drop 확인(404)**, `handle_new_user` 는 trigger 전용(RPC 미노출).
  - ⑤ **무인증 엔드포인트**: cron 은 `CRON_SECRET` 미설정 시 fail-closed(프로드 실측 401), 토스 웹훅은 페이로드 불신·토스 API 재검증, log-error 는 content-type(415)·크기(413)·rate limit 가드.
- 2026-07-18(3): **rate limit 저장소 Supabase 화(#188)** — 기존 인메모리 폴백은 서버리스 인스턴스별 독립이라 Vercel 에서 상한이 느슨(콜드스타트 리셋). `rate_limits` 테이블(RLS on·정책 없음, service_role 만) + `check_rate_limit` RPC(원자적 고정창, security definer, service_role execute)로 **인스턴스 간 공유 상한** 확보(Redis 불요). 백엔드 3단 Redis→Supabase→인메모리. 커버: newsletter·inquiry·log-error·csp-report + **reportReview(사용자당 시간 20건, brigading 방어)**. 실측: anon RPC/테이블 `42501` 차단·카운터/창 정상.

- 2026-07-19: **도메인 개통 보안 후속 — 실도메인 검증 완료.**
  - **CSP**: wasa.kr 응답 헤더 실측 정상(토스·지도 호스트 포함). **지도**: 네이버 SDK 가 신규 출처 거부 → 구글 폴백으로 안전 동작(설계 의도), 네이버 클라우드 콘솔에 wasa.kr/www 등록 후 복귀 확인. **결제위젯**: 실도메인 테스트 결제 1회 정상(#119 교훈 이행).
  - **메일 스푸핑 방지**: Resend 도메인 인증 완료 — DKIM(resend._domainkey)·SPF(send, amazonses)·MX 레코드 전파 실측, `noreply@wasa.kr` 실발송 200 확인. **DMARC 미설정(권장 백로그)** — 첫 메일 스팸 분류 가능성, `_dmarc TXT v=DMARC1; p=none;` 추가 권장.
  - Resend API 키가 **발송 전용(restricted)** 인 것 확인 — 도메인 관리 권한 없음(최소 권한 원칙 부합).
  - 결제 후 서버 장바구니 삭제(#216)는 RLS 로 본인 행만 — 권한 경계 변화 없음.
- 2026-07-19(2): **E2E 회귀 안전망 + 공격면 축소.**
  - **E2E 스모크(#222)**: Playwright 5시나리오가 main push 마다 프로덕션 대상 실행 — **비로그인 사용자가 `/checkout` 에 도달하면 `/auth?redirect=` 로 튕기는지**를 매 배포 검증(인증 게이트 회귀 감지). 결제 위젯 이후는 실결제 위험이라 자동화 범위 밖 — 라이브 전환 후 수동 1회.
  - **모바일 viewport(#224)**: `minimum-scale=1` 은 축소만 차단, 확대는 허용 — WCAG 1.4.4(200% 확대) 유지. 접근성 후퇴 없음.
  - **홈 공격면 축소(#225~#226)**: 홈이 상품 카드를 철학 멘트(정적 상수)로 교체하면서 `products` 쿼리가 사라짐 → 홈은 이제 `site_content` 1건만 읽는다. 공개 캐시(`lib/supabase/public.ts`)에 실리는 데이터가 줄어 캐시 오염 표면도 축소.
  - **토스 전자결제 심사 접수**(2026-07-19, 👤 대표님 명의) — 정산계좌·신청인 명의 일치 요구로 대표님이 직접 접수. **정산 계좌 = 사업자등록증 대표자 명의**여야 하며, 개발자(시열님) 명의 계좌·신청은 반려 사유이자 세무상 부적절. 승인 후 live 키는 **Vercel Production 환경에만** 주입(프리뷰·개발에 라이브 시크릿 유입 금지).

---

## 정식 오픈 전 남은 것 (요약)
1. ~~도메인 DNS → 실도메인 검증~~ → **2026-07-19 완료**(CSP·지도·결제 실도메인 검증)
2. 👤 **토스 심사 승인 대기 중**(대표님 명의 접수 완료) → 라이브 키 Production 전환 + 웹훅 URL 등록 + 실결제 1회 검증
3. 👤 **이메일 인증 ON**(운영) — 도메인 인증 완료됐으므로 Supabase 커스텀 SMTP(Resend) 전환 가능
4. 👤 Supabase Password Requirements 서버 강제 · CRON_SECRET · DMARC 레코드 추가
5. 👤 대표님: 개인정보보호책임자 지정 · 반품 배송비·회수 방법 확정
6. 출시 전 `/security-review` 1회 + 백업·사고대응 절차 확인
